# journal - reorg-coherence: every mismatch against the canonical state

**Written 2026-08-19 by the repository-reorganization pass** (`docs/agents/PROMPT_repo_reorganization.md`, TASK 3). Six read-only agents swept the whole corpus - `docs/`, `docs/agents/`, `src/`, `src/nbgen/`, `scripts/`, `notebooks/`, `tests/` and the root markdown/HTML - one kill-list or canon group each.

**Classification key.** `CURRENT_CLAIM` = presented as true now, **a defect that must be fixed**. `INSIDE_SUPERSEDED_BLOCK` = inside a strike-through / superseded / WITHDRAWN / REFUTED / dated-amendment block: correct house style, no action. `KILL_LIST_DEFINITION` = the file is itself a register of retired terms: no action. `QUOTED_AS_RETIRED` = quoted in order to say it was retired: no action. `UNRELATED_COINCIDENCE` = the digits match but mean something else.

This project deliberately preserves superseded material. **A retired number inside a struck block is not a defect** - that distinction is why 193 raw hits reduce to 133 real ones.

## 1 - Totals

| sweep group | raw hits | **CURRENT_CLAIM** | superseded block | kill-list def | quoted as retired | coincidence |
|---|--:|--:|--:|--:|--:|--:|
| uncited-bands | 29 | **18** | 4 | 1 | 4 | 2 |
| ratios-bands | 27 | **18** | 4 | 0 | 2 | 3 |
| loads-kmin | 44 | **16** | 14 | 3 | 6 | 5 |
| canon-numbers | 13 | **13** | 0 | 0 | 0 | 0 |
| embargo-convention | 20 | **18** | 2 | 0 | 0 | 0 |
| stale-status | 60 | **50** | 6 | 2 | 1 | 1 |
| **TOTAL** | **193** | **133** | 30 | 6 | 13 | 11 |

## 2 - Where the 133 live

| file | CURRENT_CLAIM defects |
|---|--:|
| `progress_map.html` | 18 |
| `src/nbgen/make_nb18.py` | 16 |
| `notebooks/18_musle_construction.ipynb` | 13 |
| `docs/PROGRESS.md` | 7 |
| `presentation_guide.html` | 6 |
| `docs/00_INDEX.md` | 5 |
| `scripts/build_report_pdf.py` | 5 |
| `docs/54_c3_1_closure_and_c4_entry_status.md` | 5 |
| `docs/31_phase_c_workplan.md` | 5 |
| `docs/37_c3_closure.md` | 4 |
| `src/nbgen/make_nb19.py` | 4 |
| `scripts/build_deck.py` | 4 |
| `docs/40_sdr_evidence.md` | 4 |
| `docs/59_cross_implementation_comparison.md` | 3 |
| `scripts/c3/ls2d.py` | 2 |
| `docs/39_contradiction_audit.md` | 2 |
| `notebooks/19_c3_gate_and_c4_setup.ipynb` | 2 |
| `docs/42_c4_guards.md` | 2 |
| `docs/47_c4_entry_verdict.md` | 2 |
| `docs/24_presentation_outline.md` | 2 |
| `CLAUDE.md` | 2 |
| `docs/35_qpeak_preregistration.md` | 1 |
| `docs/agents/journal_c42-prereg.md` | 1 |
| `scripts/c3/ls2d_variants.py` | 1 |
| `docs/34_observed_enso_contrast.md` | 1 |
| `data/processed/ls_stratified_report.md` | 1 |
| `docs/00_objectives_and_hypotheses.md` | 1 |
| `docs/14_presentation_plan.md` | 1 |
| `docs/55_c43_verdict.md` | 1 |
| `docs/30_phase_c_plan.md` | 1 |
| `docs/16_forcing_pipeline_audit.md` | 1 |
| `docs/18_hydrology_journal.md` | 1 |
| `docs/progress_journal.md` | 1 |
| `src/mgb_sediment.py` | 1 |
| `src/nbgen/make_nb15.py` | 1 |
| `notebooks/15_ssc_quality_gate.ipynb` | 1 |
| `src/nbgen/make_nb16.py` | 1 |
| `notebooks/16_observed_enso_contrast.ipynb` | 1 |
| `src/nbgen/make_nb17.py` | 1 |
| `notebooks/17_runoff_signatures.ipynb` | 1 |
| `README.md` | 1 |

**Read this table structurally, not as a scoreboard.** `src/nbgen/make_nb18.py` (16) and `notebooks/18_musle_construction.ipynb` (13) are *the same defects counted twice* - the generator holds the source, the notebook holds the generated copy and the executed output. The same pairing holds for nb15/16/17/19. `progress_map.html` (18) is the live status tracker and is simply a week behind.

## 3 - Every CURRENT_CLAIM defect, with the prescribed fix

### sweep: uncited-bands - 18 defects

| # | file:line | retired term | the offending text | old -> new |
|--:|---|---|---|---|
| 1 | `src/nbgen/make_nb18.py:2634` | under-erosive | print(f'\ncombined: the model is {COMB_LO:.2f}x - {COMB_HI:.2f}x under-erosive') | Replace with a direction-free bracket: print(f'\ncombined MAGNITUDE bracket under reading A: {COMB_LO:.2f}x - {COMB_HI:.2f}x; under reading B the sign INVERTS to 1.33x-1.49x HIGH. DIRECTION WITHDRAWN (docs/37 A1.9) - the interval contains 1.') . This is the exact repair docs/37 A1.7 item 7 asks for ('the summary line must say direction unknown, not "under-erosive"'). |
| 2 | `src/nbgen/make_nb18.py:2670` | under-erosive | ("4' basin-mean gross HILLSLOPE erosion rate is consistent with published levels", 'NOT MET', f'model is {COMB_LO:.2f}x-{COMB_HI:.2f}x under-erosive; Leg A alone {23.7/E_HA:.2f}x-{26.5/E_HA:.2f}x'), | Clause 4' was superseded by 4" (docs/37 A1.9). Change the label to `4" the quantity the MUSLE sum represents is pinned, and the rate is consistent with published levels of THAT quantity`, the status from 'NOT MET' to 'NOT ESTABLISHED', and the reason string to 'reading A gives 1.03x-2.27x low, reading B 1.33x-1.49x high; the interval contains 1, so the clause cannot be evaluated (docs/37 A1.9)'. Then update the VERDI |
| 3 | `src/nbgen/make_nb18.py:2738` | under-erosive | **1.03x to 2.27x under-erosive**. Closing Leg A with the coefficient alone would need $\alpha \approx 23.7$-26.5, outside the pre-registered expected band at both ends. | Replace '**1.03x to 2.27x under-erosive**' with '**a magnitude bracket of 1.03x-2.27x under reading A, which inverts to 1.33x-1.49x HIGH under reading B - the direction is UNKNOWN (docs/37 A1.9)**', and drop or re-frame the alpha sentence, since docs/35 §6 RULE 0 plus A1.9 forbid arguing a fit from a withdrawn direction. |
| 4 | `src/nbgen/make_nb18.py:3212` | under-erosive | 1. **The level.** The replacement closure clause fails: the model is 1.03x-2.27x under-erosive against three published levels, and 2.03x-2.27x on the only leg with a like-for-like denominator. | Replace with: '1. **The level.** The replacement closure clause (4") is **NOT ESTABLISHED**, not failed: the same arithmetic gives 1.03x-2.27x low under reading A and 1.33x-1.49x high under reading B, and Leg A's denominator is NOT like-for-like - SWAT Ch. 4:1 calls this equation's output a sediment *yield* (docs/37 A1.9).' |
| 5 | `src/nbgen/make_nb18.py:2615` | THE leg that counts (Leg A as like-for-like) | 'like-for-like denominator - THE leg that counts' | docs/37 A1.7 item 7 names this exact string as part of the unfixed A1.9 defect. Replace with 'reading-A denominator; NOT like-for-like - the comparator is a RUSLE gross erosion while SWAT Ch. 4:1 defines this equation's output as a yield (docs/37 A1.9)'. |
| 6 | `notebooks/18_musle_construction.ipynb:4076` | under-erosive | "print(f'\\ncombined: the model is {COMB_LO:.2f}x - {COMB_HI:.2f}x under-erosive')\n", | Do NOT hand-edit. Fix src/nbgen/make_nb18.py:2634, regenerate with python3.10 src/nbgen/make_nb18.py, then re-execute nb18 (timeout=-1 registered - budget for it). This mirrors generator line 2634. |
| 7 | `notebooks/18_musle_construction.ipynb:4112` | under-erosive | "     f'model is {COMB_LO:.2f}x-{COMB_HI:.2f}x under-erosive; Leg A alone '\n", | Mirrors generator line 2670. Fix in src/nbgen/make_nb18.py, regenerate, re-execute. |
| 8 | `notebooks/18_musle_construction.ipynb:4212` | under-erosive | "**1.03x to 2.27x under-erosive**. Closing Leg A with the coefficient alone would need\n", | Mirrors generator line 2738. Fix in src/nbgen/make_nb18.py, regenerate, re-execute. |
| 9 | `notebooks/18_musle_construction.ipynb:4940` | under-erosive | "1. **The level.** The replacement closure clause fails: the model is 1.03x-2.27x under-erosive\n", | Mirrors generator line 3212. Fix in src/nbgen/make_nb18.py, regenerate, re-execute. |
| 10 | `notebooks/18_musle_construction.ipynb:3999` | under-erosive | "combined: the model is 1.03x - 2.27x under-erosive\n", | Executed cell OUTPUT - it can only be cleared by re-executing nb18 after the generator fix. Listed so the fixer knows a generator edit alone leaves the stale string on disk. |
| 11 | `notebooks/18_musle_construction.ipynb:4031` | under-erosive | "                      model is 1.03x-2.27x under-erosive; Leg A alone 2.03x-2.27x\n", | Executed cell OUTPUT (the clause-4' scoreboard). Cleared only by re-executing nb18 after the generator fix. |
| 12 | `docs/00_INDEX.md:174` | under-erosive / 0.05-0.30 | **A retired gate is neither a pass nor a fail**; the replacement is a gross-erosion-**rate** clause, and it is **failed** — the model is under-erosive by **1.03–2.27×**. | Replace 'and it is **failed** — the model is under-erosive by **1.03–2.27×**' with 'and it is **NOT ESTABLISHED** (clause 4″): `docs/37` **A1.9** withdrew the direction — the residual spans **2.27× low … 1.49× high** and the interval contains 1.' Add `docs/37` A1.9 to the pointer cell. (The SDR-band half of this row is fine — it is explicitly named 'uncited here'.) |
| 13 | `docs/35_qpeak_preregistration.md:704` | 0.05 – 0.30 (SDR band) used as a live comparator | (b)'s magnitude question is **not** closed: an implied SDR of 0.579 – 0.740 sits above the 0.05 – 0.30 range quoted for a basin of this size, i.e. gross erosion is still 1.93 – 14.8× too low. | This sentence sits OUTSIDE the §9.4 WARN block directly above it and outside amendment item 9 (which targets only the struck 'implied SDR 1.37 – 2.22'). Strike it in place: '~~an implied SDR of 0.579 – 0.740 sits above the 0.05 – 0.30 range quoted for a basin of this size, i.e. gross erosion is still 1.93 – 14.8× too low~~ → **STRUCK, §9.4: the 0.05 – 0.30 band is retired as UNCITABLE (`docs/40` §8; `docs/47` §4.3) a |
| 14 | `scripts/c3/ls2d.py:513` | mountainous LS 2-10 | print(f"\nGATE 2  basin distribution (published mountainous range ~2-10 for the median)") | Rename and de-gate: print(f"\nDIAGNOSTIC 2  basin distribution (the '~2-10 mountainous' band is UNCITED and RETIRED in both directions, docs/37 §1 decision 4 — printed as a scale reference only; it passes and fails nothing)"). src/mgb_sediment.py:261-262 and :664 already state the band 'may never be graded CITED' and 'is uncited and retired'; this script is the last place still calling it a GATE (recorded as an open  |
| 15 | `scripts/c3/ls2d.py:666` | mountainous LS 2-10 | label="published mountainous range 2–10")   # green axvspan over log10(2)..log10(10) | Change the legend label to 'retired, UNCITED "mountainous 2–10" — scale reference only' (the wording already used in src/nbgen/make_nb18.py:1178, which is clean). Same journal_lens-dangling-refs.md §11 finding. |
| 16 | `docs/00_INDEX.md:256` | published 2–10 | \| LS2D build (Desmet & Govers, 30.2 M cells at 90 m, median LS 12.8 vs published 2–10 — reported, not adjusted) \| `journal_c31-ls2d.md` + tracker \| **not yet** — expected in the C3 closure doc (37) \| | Two edits in one row. (1) 'vs published 2–10' → 'vs the since-retired, UNCITED "2–10" band'. (2) The 'promoted?' cell is stale: change '**not yet** — expected in the C3 closure doc (37)' to '**yes** — `docs/37` §1 decision 4 retires the uncited 2–10 band (`docs/39` §... records the promotion)'. Note the same row-block at :258 also carries the stale '82/82 tests' (now 154) — out of this hunt's scope but in the same ta |
| 17 | `progress_map.html:902` | published expectation of 2–10 | per-cell median <b>12.77</b> against a published expectation of 2–10, i.e. 1.25–1.28× high and <b>reported unadjusted</b> … only 889 of 8,672 minibacias have a median inside 2–10. | This 2026-08-11 C3.1–C3.4 node presents the band as a live 'published expectation' with no retirement note, while its sibling node at :726 already carries one. Append the same clause: ' <b>Superseding this comparison:</b> the "2–10" band is <b>uncited</b> and was RETIRED in both directions (docs/37 §1 decision 4) — it passes and fails nothing.' (The same node also prints the stale <b>82/82 tests</b>; now 154 — differ |
| 18 | `docs/agents/journal_c42-prereg.md:76` | under-erosive | - **G6** the retired-SDR rule: a retired gate is neither a pass nor a fail; the live replacement is the gross-hillslope-erosion-**rate** clause (docs/40 §8.2), currently FAILED (under-erosive 1.03–2.27× at the adopted default). | Low severity (a journal, non-authoritative per docs/00_INDEX), but this is a restatement of the LIVE guard set a C4 session reads as operative. Append a dated note: '(⚠ 2026-08-19: direction WITHDRAWN by docs/37 A1.9 later the same day — G6's live form is clause 4″ NOT ESTABLISHED, residual 2.27× low … 1.49× high.)' |

*Sweep summary:* Swept the whole repo for the uncited-band and mislabel family. Two of the four terms are fully closed; two carry live defects.  CLOSED, verified on disk — no action: - **min(m,0.5) mislabelled "Buarque eq. 14"**: zero current claims. All five registered sites are corrected (docs/35 §9.4.1, docs/37 A3.3.2, docs/43 Amendment 2, make_nb18.py:1404, make_nb19.py:2608), src/mgb_sediment.py:261-262 states the cap "may never be graded CITED", and docs/agents/journal_sweep-phasec.md §B.1 plus docs/54 §T2b both record the defect as closed. docs/37:1533 and docs/46:178 retain the string only as verbatim quotations inside their own correction blocks. - **SDR 0.05–0.30**: ~60 hits, all but one either struck, labelled retired/UNCITED/UNCITABLE, or inside docs/40 (the document that retires it).  LIVE DEFECTS — 18 sites, in three clusters:  1. **nb18's withdrawn "under-erosive" direction (11 sites) — the largest and most consequential.** `src/nbgen/make_nb18.py` §6.4/§7 still print clause 4′ as a like-for-like DIRECTED result at :2615, :2634, :2670, :2738, :3212, and `notebooks/18_musle_construction.ipynb` mirrors it at :3999, :4031, :4076, :4112, :4212, :4940 (two of those are *executed outputs*, so a generator edit alone will not clear them). This is not a newly-found defect — `docs/37` A1.7 item 7 records it as **"NOT FIXED by A1.9's pass, deliberately"** and names the exact strings and the required repair ("the summary line must say **direction unknown**, not *under-erosive*"); progress_

### sweep: ratios-bands - 18 defects

| # | file:line | retired term | the offending text | old -> new |
|--:|---|---|---|---|
| 19 | `src/nbgen/make_nb18.py:2904` | band 2.0-9.9 | ax[1].axvspan(2.0, 9.9, color=CB['amber'], alpha=0.14,               label='the SAME band rescaled for our $LS$ level') | The amber span is the docs/35 6.1 band rescaled by the RETIRED bracket [0.333, 0.421] (5.9*0.333=1.965, 23.6*0.421=9.936). Replace with the registered rescaling: `ax[1].axvspan(1.4836, 10.1938, ...)` for the bracket form, or `ax[1].axvspan(1.4836, 5.9345, ...)` for the adopted-POINT form docs/37 A3.2 uses. Same numbers already printed correctly by this generator at line 1544. Edit the generator, then regenerate (neve |
| 20 | `src/nbgen/make_nb18.py:2900` | alpha reference 3.9-5.0 | (4.45, r'like-for-like ref for OUR $LS$')]: | 4.45 is the arithmetic midpoint of the retired 11.8*[0.333,0.421] = 3.93-4.97 band (mean 4.4486). Replace `4.45` with `2.9672` (11.8 * f_LS at the adopted point, docs/37 A3.2 / docs/46 1.0: 2.967 - 5.097 for the bracket). |
| 21 | `src/nbgen/make_nb18.py:2919` | band 2.0-9.9 | pre-registered expected range for $\alpha$ and the amber band is the same range rescaled for our measured topographic level. | Prose asserts the amber (2.0-9.9) band as the current rescaling. After fixing the axvspan, add the bracket by name, e.g. "... rescaled by the registered f_LS in [0.25146, 0.43194] (docs/46 1.0)". |
| 22 | `src/nbgen/make_nb18.py:2926` | band 2.0-9.9 | pre-registered band (5.9-23.6) and its rescaled counterpart (2.0-9.9) overlapping only partially, | Replace "(2.0-9.9)" with "(1.484-10.194; 1.484-5.934 at the adopted POINT)" — the same numbers this generator already prints at line 1544/1341. |
| 23 | `src/nbgen/make_nb18.py:2927` | alpha reference 3.9-5.0 | with the like-for-like reference for our own topographic level at about 4.45. | Replace "at about 4.45" with "at 2.9672 (bracket 2.967 - 5.097)" — docs/37 A3.2, docs/46 1.0, docs/51 9. Line 1558 of the same file already states 2.9672. |
| 24 | `notebooks/18_musle_construction.ipynb:4518` | band 2.0-9.9 | "ax[1].axvspan(2.0, 9.9, color=CB['amber'], alpha=0.14,\n", | Executed copy of the generator defect. Do NOT hand-edit: fix src/nbgen/make_nb18.py:2904 then rerun the generator and re-execute the notebook. |
| 25 | `notebooks/18_musle_construction.ipynb:4514` | alpha reference 3.9-5.0 | "                (4.45, r'like-for-like ref for OUR $LS$')]:\n", | Executed copy. Fix src/nbgen/make_nb18.py:2900 (4.45 -> 2.9672), regenerate, re-execute. |
| 26 | `notebooks/18_musle_construction.ipynb:4538` | band 2.0-9.9 | "pre-registered expected range for $\\alpha$ and the amber band is the same range rescaled for our\n", | Executed copy of make_nb18.py:2919. Fix the generator, regenerate, re-execute. |
| 27 | `notebooks/18_musle_construction.ipynb:4546` | band 2.0-9.9 | "pre-registered band (5.9-23.6) and its rescaled counterpart (2.0-9.9) overlapping only partially,\n", | Executed copy of make_nb18.py:2926. Fix the generator, regenerate, re-execute. Note the SAME notebook already prints the correct 1.4836-10.1939 three cells earlier — it self-contradicts. |
| 28 | `notebooks/18_musle_construction.ipynb:4547` | alpha reference 3.9-5.0 | "with the like-for-like reference for our own topographic level at about 4.45.\n", | Executed copy of make_nb18.py:2927. Fix the generator (4.45 -> 2.9672), regenerate, re-execute. |
| 29 | `progress_map.html:560` | band 2.0-9.9 | <text x="204" y="130" fill="#a879e6" font-size="10.5">2.0 – 9.9 — the band after the LS correction: no help, it still contains them</text> | Inside the `guardblind` SVG, which carries NO superseded marker (unlike the `factorchain` SVG at line 383, which does). Replace "2.0 – 9.9" with "1.484 – 10.194 (1.484 – 5.934 at the adopted POINT)" and redraw the purple bar (x1=88.2..x2=198.6) on the new log-axis endpoints. ("floor 3.9" at line 553 is NOT a defect — it is docs/35 6.1's registered hard-stop floor, explicitly left UNCHANGED by docs/35 9.4.) |
| 30 | `progress_map.html:726` | 2.37x-3.00x | <b>Superseding all of the above:</b> the published '2–10' band this node compares against is <b>uncited</b> and was RETIRED (docs/37 §1 decision 4); the real LS problem is the <b>formulation level</b>, measured at 2.37–3.00× the reference on this same grid | In the C3.1 node's `risk:` field. Replace "measured at 2.37–3.00× the reference on this same grid" with "measured at 2.3151×–3.9768× the reference on this same grid (3.9768× at the adopted point; docs/47 §4.3, docs/51 §2)". |
| 31 | `progress_map.html:754` | 2.37x-3.00x | (1) <b>Do NOT stack the upward candidates</b> (C revision ×2–5, f_peak ×2.1) on top of an LS that is 2.37–3.00× too high for its own α, then read the sum as agreement with the anchor | In the "four conventions" decision node's `risk:` field, which carries no supersede marker. Replace "2.37–3.00×" with "2.3151–3.9768×". |
| 32 | `progress_map.html:781` | band 2.0-9.9 | Two more: leaning on the LS-corrected band does not help either (an SDR = 1.0 fit still lands inside ≈ 2.0 – 9.9); and G3.1's per-station land-class leverage table was computed at the PRIOR C | In the C3.9/docs-42 node's `risk:` field. Replace "≈ 2.0 – 9.9" with "≈ 1.484 – 10.194 (≈ 1.484 – 5.934 at the adopted POINT)" and re-check the containment claim: at the adopted-POINT form the deposition-free 6.83–8.73 (5.67–7.25 at the adopted C, docs/47 §2.5 C1) no longer sits inside, so the sentence's conclusion may invert. |
| 33 | `progress_map.html:789` | band 2.0-9.9 | Leaning on the LS-corrected band does not help — an SDR = 1.0 fit still lands inside ≈ 2.0 – 9.9. And the sharpest form, from docs/37 A1.9: the α that <b>‘works’</b> under the yield reading | In the C4.0 entry-condition node's `risk:` field. Same replacement as line 781: "≈ 2.0 – 9.9" -> "≈ 1.484 – 10.194 (≈ 1.484 – 5.934 at the adopted POINT)", and re-check the containment conclusion. |
| 34 | `docs/37_c3_closure.md:1258` | SE 0.1644 ln / +/-38 % Pi band | Π (the level) is identifiable with SE = 0.465/√8 = **0.1644 ln = ±38 % at 95 %**; **β is identifiable** (SE 0.020, 95 % half-width 0.039 against a band half-width of 0.10) | Amendment A2.4 item 2, unstruck and unamended. This site is explicitly recorded as OWED by docs/42 §9.7 row 7 ("OWED to docs/37's owner") and docs/48 §5 row 8 ("docs/37 is closed to this pass, so this is a pointer, not an edit"). Strike the phrase and replace: `SE = ~~0.465/√8 = 0.1644 ln = ±38 %~~ -> **0.6936 ln** (estimator b) / **0.4775 ln** (estimator a); the band is the station bootstrap Π̂ × [0.29, 3.73] (docs/ |
| 35 | `docs/39_contradiction_audit.md:192` | 2.37x-3.00x / x0.333-x0.421 | \| LS **formulation** level vs the α = 11.8 reference \| ours area-wtd **39.812** vs source-faithful **16.775** = **×0.421** (×0.333 with the Desmet–Govers `L`) ⇒ our LS is **2.37×–3.00×** high \| ... \| ✔ (measured on all 30,235,916 basin cells at 90 m) | docs/00_INDEX lists docs/39 as LIVE, and this row carries a ✔ (agrees/verified) verdict with no strike. Wrap in a dated superseded note: "[SUPERSEDED 2026-08-12 — the bracket is f_LS ∈ [0.25146, 0.43194] erosion-weighted ⇒ 2.3151×–3.9768×, a POINT at 3.9768× (docs/46 §1.0, docs/47 §4.3, docs/51 §2); the area-weighted proxy upper end is 0.42136300143291305, not 0.42148.]", leaving the original readable. |
| 36 | `scripts/c3/ls2d_variants.py:52` | x0.421 | V4p_buarque_2015_cap  V1 + V2a + V3 — the ×0.421 row exactly as published, kept so the                         prior number stays reproducible. | The V4/V4' labels are swapped, the same defect docs/49 §7 row 1 and docs/51 §7 (b) fixed in docs/46 §3.1 but which survives in this harness docstring. `V4_buarque_2015` (V1+V2b+V3, the eq.-14 STEP) is the published ×0.421 row; `V4p` (V1+V2a+V3, the min(m,0.5) CAP) had never been measured. Move the phrase "the ×0.421 row exactly as published, kept so the prior number stays reproducible" to the V4_buarque_2015 line (51 |

*Sweep summary:* Swept docs/, docs/agents/, src/, src/nbgen/, scripts/, notebooks/, tests/, root *.md, progress_map.html and presentation_guide.html for the retired ratios and bands (2.37 / 3.00x / x0.333 / x0.421 / alpha ref 3.9-5.0 / band 2.0-9.9 / hard stop 11.8-14.9 / SE 0.1644 / +/-38 % Pi band / sigma_r 0.465), searching both hyphen and en-dash and both x and multiplication-sign forms.  18 CURRENT_CLAIM defects in 6 files, clustering into four independent problems:  (1) NOTEBOOK 18's SECTION 7.2 EQUIFINALITY FIGURE - the biggest one, and self-contradicting. src/nbgen/make_nb18.py:2900, 2904, 2919, 2926, 2927 draw and describe the amber alpha band as 2.0-9.9 and the like-for-like reference as 4.45. Both are the RETIRED [0.333, 0.421] bracket (5.9*0.333=1.965 / 23.6*0.421=9.936 -> "2.0-9.9"; 11.8*[0.333,0.421] arithmetic mean = 4.4486 -> "4.45"). The SAME generator already prints the registered replacements at lines 1341/1544/1558 (1.484-10.194 bracket, 1.4836-5.9345 and 2.9672 at the adopted POINT), so cells three apart disagree. The executed copy carries all five at notebooks/18_musle_construction.ipynb:4514, 4518, 4538, 4546, 4547. Fix the generator only, then regenerate and re-execute - never hand-edit a nb10-19 notebook. This was independently confirmed by docs/agents/journal_refute-t6-8.md ("VERDICT: I COULD NOT KILL IT"), which is a refuter journal, not an applied fix.  (2) progress_map.html - four live sites: the `guardblind` SVG at :560 ("2.0 - 9.9 - the band after the LS correct

### sweep: loads-kmin - 16 defects

| # | file:line | retired term | the offending text | old -> new |
|--:|---|---|---|---|
| 37 | `progress_map.html:752` | 104.8 / 82.8 Mt/yr proxy loads | 0 LS formulation ⚠ UNRESOLVED, and it points the\n   WRONG way:            ×0.421 … 0.333\n                         ≈104.8 … 82.8   1.37 … 2.22 ⇒ impossible | The panel's own closing note says only "the SDR column above was RETIRED by docs/40 and the chain does not stop at 248.730" — it does not touch the LS row. Strike the LS row in place and replace with: "0 LS formulation — DECIDED 2026-08-12 (docs/37 A3, ADOPT-SOURCE buarque_2015_dg): f_LS = 0.25146 erosion-weighted ⇒ 299.5387 × 0.25146 = 75.32 Mt/yr. ~~×0.421 … 0.333 ⇒ ≈104.8 … 82.8, SDR 1.37 … 2.22 impossible~~ — SUP |
| 38 | `progress_map.html:366` | 248.730 Mt/yr labelled ADOPTED, no cp_revision | <text x="514.5" y="100" fill="#2ecc71" font-size="10.5" text-anchor="end" font-weight="600">248.730 Mt/yr — ADOPTED · ×7.593</text> | 248.730 is no longer adopted and the label carries no cp_revision. Change to "248.730 Mt/yr @ prior_2026_08_11 — SUPERSEDED · ×7.593". The figure's HISTORICAL caption sits ~20 SVG rows below and does not neutralise the word ADOPTED printed on the bar itself. |
| 39 | `docs/39_contradiction_audit.md:192` | 104.8 / 82.8 Mt/yr proxy loads | \| LS **formulation** level vs the α = 11.8 reference \| … ⇒ our LS is **2.37×–3.00×** high \| … \| ✔ (measured on all 30,235,916 basin cells at 90 m) … it takes 248.730 → 104.8/82.8 Mt/yr, implied SDR 1.37–2.22 \| | docs/39 has no supersession banner anywhere (whole file checked). Add a dated header banner or strike this row in place: "~~it takes 248.730 → 104.8/82.8 Mt/yr, implied SDR 1.37–2.22~~ → SUPERSEDED 2026-08-12: f_LS ∈ [0.25146, 0.43194] erosion-weighted ⇒ 299.5387 → 75.3235 / 129.3840 Mt/yr engine re-runs (docs/47 §4.3, docs/51 §2); the implied-SDR reading is retired outright (docs/40)." Also drop the ✔ mark from the  |
| 40 | `docs/37_c3_closure.md:1255` | k_min 0.0209 /km | of the basin; `k_min` **0.0096 → 0.0209 /km** (2.2× worse than `docs/42` assumed, **9.7×** worse than the all-18 guard that will judge the fit); surviving CAL-CAL nested pairs **3 → 1**. | Append a dated pointer: "⚠ A-P4 (docs/42 §9.7, 2026-08-12): 0.0209 /km was computed at the retired σ_r = 0.465; the CAL-8 figure is 0.0838 /km (≈173× over 61.5 km) and the all-18 figure 0.0066–0.0069 /km. The 2.2× / 9.7× ratios and the conclusion are unchanged." Do not silently overwrite the number. |
| 41 | `docs/37_c3_closure.md:1193` | k_min 0.0209 /km | §4.1–§4.2 (fit set CAL 13 → **CAL 8**; `k_min` 0.0096 → **0.0209 /km**) and this document's A1.3.4 | Same dated pointer as :1255, or one A3-level note covering both: "every k_min printed in A2 is at the registered σ_r and is superseded by docs/42 §9.7 A-P4 (CAL 8 → 0.0838 /km)." |
| 42 | `docs/37_c3_closure.md:1281` | k_min 0.0209 /km | \| `docs/42` §9 — three amendments (fit set CAL 8 + `k_min` 0.0209; the ARRANCAPLUMAS conflict decided explicitly; deposition reported as a bound, 2 free + 1 bounded) \| **OWED — blocking on C4's start.** | The debt is discharged and the number moved. Replace with "fit set CAL 8 + `k_min` ~~0.0209~~ → 0.0838 /km (docs/42 §9 A-P1…A-P4, ENACTED)" and change the disposition cell from OWED to DISCHARGED (docs/42 §9.5, §9.7). |
| 43 | `src/nbgen/make_nb19.py:3048` | k_min 0.0209 /km | ('k_min on the CAL 8 reproduces the documented 0.0209 /km',\n     abs(KMIN['CAL 8 (achievable)'] - 0.02092) < 5e-4), | The gate immediately above labels its all-18 twin "the SUPERSEDED documented 0.00216 /km (corrected: 0.0065-0.0069 /km, docs/45 s8.1.4)". Give this one the same label: 'k_min on the CAL 8 reproduces the SUPERSEDED documented 0.0209 /km (corrected: 0.0838 /km, docs/42 s9.7 A-P4)'. Assertion value unchanged; then regenerate nb19. |
| 44 | `notebooks/19_c3_gate_and_c4_setup.ipynb:4513` | k_min 0.0209 /km | ("'k_min on the CAL 8 reproduces the documented 0.0209 /km',\n" | Generated cell — do not hand-edit. Fix src/nbgen/make_nb19.py:3048 and re-run the generator, then execute with `python -m nbconvert`. |
| 45 | `src/nbgen/make_nb19.py:2267` | k_min 0.0209 /km | 'a fitted k would be reporting noise with a decimal point '\n                                      '(k_min 0.0209 /km)'], | This sits in section 5.3, outside the "CORRECTED, 2026-08-12" block that covers section 4.3. Change to '(k_min 0.0838 /km at the measured residual sd; 0.0209 /km as registered — docs/42 s9.7 A-P4)'. The precondition's conclusion is unchanged and in fact strengthened. |
| 46 | `notebooks/19_c3_gate_and_c4_setup.ipynb:3281` | k_min 0.0209 /km | "                                      '(k_min 0.0209 /km)'],\n", | Generated cell — fix src/nbgen/make_nb19.py:2267 and regenerate; never hand-edit notebooks 10–19. |
| 47 | `scripts/build_report_pdf.py:249` | 299.54 Mt/yr without cp_revision | "The basin-total gross hillslope erosion at the reference parameters is 299.54 million tonnes per year.", BODY)) | This is the PDF deliverable. Extend: "…is 299.54 million tonnes per year, quoted at the adopted convention (williams_m3 runoff volume, US-customary K) and the adopted cover-factor revision cp_revision='cited_central_2026_08_11'; at the prior revision the same run reads 248.73 Mt/yr." Absolute flux only — t/km²/yr stays embargoed (docs/23 §13.2). |
| 48 | `scripts/build_deck.py:442` | 299.5 Mt/yr without cp_revision | "Basin gross hillslope erosion at reference parameters: 299.5 Mt/yr — quoted with its convention, " "NEVER as a per-area yield (catchment-area embargo, docs/23 §13.2)" | The bullet claims the convention travels with the number but names none. Change to "Basin gross hillslope erosion, williams_m3 + us_customary, cp_revision cited_central_2026_08_11: 299.5 Mt/yr (prior revision 248.7) — never as a per-area yield (docs/23 §13.2)". |
| 49 | `docs/59_cross_implementation_comparison.md:844` | 299.54 Mt/yr without cp_revision | **66.53 % of the model's gross erosion — 199.29 of 299.54 Mt/yr — is upstream of no usable SSC station**, including **801.1 km** of channel | Add the revision tag: "199.29 of 299.54 Mt/yr (gross hillslope, `cp_revision='cited_central_2026_08_11'`)". docs/59 names cp_revision at :333 and :582 for the C table but never attaches it to either load quote. |
| 50 | `docs/59_cross_implementation_comparison.md:945` | 299.54 Mt/yr without cp_revision or convention | 1. **66.53 % of modelled erosion — 199.29 of 299.54 Mt/yr — is upstream of no usable SSC station** (`docs/42` G9) | Weaker than :844 — this one drops "gross" as well. Change to "66.53 % of modelled gross hillslope erosion — 199.29 of 299.54 Mt/yr at `cp_revision='cited_central_2026_08_11'` —". |
| 51 | `docs/PROGRESS.md:90` | 299.5387 Mt/yr without cp_revision | ### C3 — MUSLE hillslope erosion 🟡 **BUILT & RUN — C3 OPEN** (docs/37 A1–A3) · C3.1 LS *formulation* ENACTED (A3: ADOPT-SOURCE `buarque_2015_dg`); basin erosion 299.5387 Mt/yr; C3.5 still 🔴 | Change "basin erosion 299.5387 Mt/yr" to "basin gross hillslope erosion 299.5387 Mt/yr @ cp_revision='cited_central_2026_08_11'". |
| 52 | `docs/42_c4_guards.md:171` | 248.696 / 248.730 Mt/yr without cp_revision | one read-only run of `src/mgb_sediment.py` at adopted defaults (basin total **248.696 Mt/yr**, mass ledger `exact = True`; `docs/37` §2's 248.730 differs only in the days-per-year divisor). | §4's provenance line names no cp_revision, and the disclosure that §4.1 was measured at the prior revision lives ~890 lines away at §9.7.4 row 21. Add inline: "(basin total 248.696 Mt/yr at `cp_revision='prior_2026_08_11'` — see §9.7.4 row 21, flag F8; the adopted level is 299.5387 Mt/yr)". |

*Sweep summary:* Swept the whole repo for the retired proxy loads (104.8 / 82.8 / 126.1 / 126.2 / 99.7 / 99.8 Mt/yr), the retired k_min constants (0.00216 / 0.0209 / 0.0104 /km), "2.12x over 348.4 km", and every appearance of 248.7298/248.730/248.73 and 299.5387/299.539. The numbered docs 35, 42, 43, 45, 46, 47, 48, 50, 51 are in very good shape: every proxy load and every retired k_min there is either struck through, sitting inside a dated [WARN] AMENDMENT block, or quoted explicitly as the "registered" column of a registered-vs-measured table. Sixteen CURRENT_CLAIM defects survive, in four clusters. (1) progress_map.html:752 — the "four conventions" decision panel still prints "0 LS formulation UNRESOLVED ... x0.421 ... 0.333 / 104.8 ... 82.8 / 1.37 ... 2.22 impossible"; its own amendment note explicitly scopes itself to the SDR column and the 248.730 endpoint, so the LS row and its proxy loads are uncovered, and progress_map.html:366 still labels 248.730 Mt/yr "ADOPTED". Per the tracker's own RULE 0 this is the status authority, so these are the most expensive. (2) docs/39_contradiction_audit.md:192 carries "248.730 -> 104.8/82.8 Mt/yr, implied SDR 1.37-2.22" in a row marked verified, and docs/39 has no supersession banner anywhere in the file. (3) The CAL-8 k_min 0.0209 /km, corrected to 0.0838 /km by docs/42 §9.7 A-P4, is still current in docs/37 A2 (:1193, :1255, :1281) and in nb19's generator/notebook (make_nb19.py:2267/3048, notebooks/19:3281/4513) — note the generator labels the all-

### sweep: canon-numbers - 13 defects

| # | file:line | retired term | the offending text | old -> new |
|--:|---|---|---|---|
| 53 | `docs/54_c3_1_closure_and_c4_entry_status.md:71` | V4_dg / buarque_2015_dg | 2. **The adopted variant is NOT a committed product.** `urh_ls2d_variants.csv` has **no `V4_dg` column** and `urh_ls2d.csv` may not be overwritten … confirmed on disk (its columns are `V0_ours_2026_08 … V5_L_dg96_fd`; no `V4_dg`). | FALSE ON DISK NOW. `data/processed/urh_ls2d_variants.csv` header reads `…,V3_s_ws78,V4_buarque_2015,V4_dg,V4p_buarque_2015_cap,V5_L_dg96_fd` — ACT 1 materialised the column. Add a dated superseded note: '~~confirmed on disk … no V4_dg~~ → CLEARED by ACT 1 (2026-08-12): V4_dg is in urh_ls2d_variants.csv; see docs/35 §9.5.1 and docs/46 §10 ("Blockers 2 and 3 were cleared by ACT 1 / ACT 2").' |
| 54 | `docs/54_c3_1_closure_and_c4_entry_status.md:72` | buarque_2015_dg / V4_dg (engine default) | 3. **ACT 2 — the default switch — is NOT DONE and cannot yet be drafted.** ADOPT-SOURCE is *determined and recorded but not exercisable*; `src/mgb_sediment.py` defaults remain `ls2d_column = "ls2d_hs"`, `urh_ls2d = "urh_ls2d.csv"`. | FALSE. `src/mgb_sediment.py:925` now reads `ls2d_column: str = "V4_dg",  # ADOPT-SOURCE buarque_2015_dg (docs/37 A3, docs/54)` and :935 records 'switched in by ACT 2 (2026-08-12)'. Replace with a dated note: '~~ACT 2 is NOT DONE … defaults remain ls2d_column = "ls2d_hs"~~ → **ACT 2 EXECUTED 2026-08-12**: the engine default is now `ls2d_column = "V4_dg"` on `urh_ls2d_variants.csv` (docs/35 §9.5.1, docs/46 §10, docs/55 |
| 55 | `docs/54_c3_1_closure_and_c4_entry_status.md:88` | V4_dg (ACT 1 / ACT 2) | **ACT 2** — switching `ls2d_column` and `urh_ls2d` to the source variant — is a **separate dated act** … and its trigger is **ACT 1**: materialising the `V4_dg` column into a committed product (blocker 2). Switching a default by name before ACT 1 is forbidden | Written in the future tense for two acts that have since been performed. Append a dated line to §5: '**Update 2026-08-12: ACT 1 and ACT 2 have both been executed** (docs/35 §9.5.1); this section describes the ownership rule, not the current engine state.' |
| 56 | `progress_map.html:582` | V4_dg | <b>(2) a physical block</b> — <code>urh_ls2d_variants.csv</code> has <b>no <code>V4_dg</code> column</b> (checked on disk this pass) … <b>(4)</b> the separate, dated default-switch act, which belongs to the C3.1 owner and <b>has not been performed</b>. | This is the LIVE banner (`DATA.banner`), not the superseded one at :587, so it reads as current status. Both clauses are now false: V4_dg exists in `urh_ls2d_variants.csv` (ACT 1) and the engine default was switched to it (ACT 2, `src/mgb_sediment.py:925`). Demote this banner to `banner_superseded_2026_08_12` and write a new one carrying the post-ACT-2 state (C4.3 ran and RAILED — docs/55; C5 complete 18/18 median 3. |
| 57 | `docs/47_c4_entry_verdict.md:960` | V4_dg | \| **2** \| **The adopted variant is NOT A COMMITTED PRODUCT.** … \| **Read on disk this pass.** `urh_ls2d_variants.csv` header = `mini,urh,n_cells,area_km2,area_frac,V0_ours_2026_08,…,V4_buarque_2015,V4p_buarque_2015_cap,V5_L_dg96_fd`; … **No `V4_dg` in eithe | The file's own header banner directs readers to §9 for current status, so a stale §9 row is quotable as live. Add a dated row-level annotation: '⚠ CLEARED 2026-08-12 by ACT 1 — `urh_ls2d_variants.csv` now carries `V4_dg` (header re-read); see docs/35 §9.5.1.' Do not delete the original text. |
| 58 | `docs/47_c4_entry_verdict.md:961` | buarque_2015_dg / V4_dg (engine default) | \| **3** \| **ACT 2 — the default switch — is NOT DONE and cannot yet be drafted.** … `src/mgb_sediment.py` defaults still `ls2d_column = "ls2d_hs"`, `urh_ls2d = "urh_ls2d.csv"`. \| | Same treatment as row 2: '⚠ CLEARED 2026-08-12 by ACT 2 — `src/mgb_sediment.py:load_geometry` now defaults to `ls2d_column = "V4_dg"` on `urh_ls2d_variants.csv` (docs/35 §9.5.1, docs/46 §10).' |
| 59 | `scripts/build_report_pdf.py:391` | 3.05 (C5 median rate ratio) | the model's median (3.05×) sits squarely inside the observed 3–5× and matches the smoother rating-based estimator (b) closely. | Overstates the agreement and contradicts the owning doc AND the figure caption five lines above it (:386, 'sits close to (b)'s 2.84–2.95 and below (a)'s 4.62'). docs/56 §1: 'the modelled median (3.05) sits at the **lower edge** of the observed ~3–5 band.' Change 'sits squarely inside the observed 3–5×' -> 'sits at the lower edge of the observed 3–5x band'. This is a public deliverable (MGB-SED_complete_report.pdf). |
| 60 | `scripts/build_report_pdf.py:249` | 299.5387 Mt/yr | The basin-total gross hillslope erosion at the reference parameters is 299.54 million tonnes per year. | Quoted without its convention and without its cp_revision, in the outward-facing PDF — the one rule CLAUDE.md and docs/00_INDEX:173 state as absolute ('Never quote a load without its convention *and* its `cp_revision`'). scripts/build_deck.py:442 at least says 'quoted with its convention'. Change to: '… is 299.54 million tonnes per year, at the williams_m3 / us_customary volume convention and cp_revision = cited_cent |
| 61 | `docs/PROGRESS.md:90` | 299.5387 Mt/yr | C3.1 LS *formulation* ENACTED (A3: ADOPT-SOURCE `buarque_2015_dg`); basin erosion 299.5387 Mt/yr; C3.5 still 🔴 (musle.py absent) | Bare load with no cp_revision. Change 'basin erosion 299.5387 Mt/yr' -> 'basin gross hillslope erosion 299.5387 Mt/yr at cp_revision = cited_central_2026_08_11 (prior revision: 248.7298)'. |
| 62 | `docs/42_c4_guards.md:171` | 248.7298 / 299.5387 (cp_revision label) | Measured 2026-08-11 … one read-only run of `src/mgb_sediment.py` **at adopted defaults** (basin total **248.696 Mt/yr**, mass ledger `exact = True`; `docs/37` §2's 248.730 differs only in the days-per-year divisor). | 'at adopted defaults' now names the *prior* cp_revision level; the adopted default is 299.5387. docs/42's own §9.7 row 21 and flag F8, and docs/48 §2.5, both record this as the probable cause of §4.1's non-reproducing Lw table and mark a recomputation OWED — but §4's body is unannotated and quotable as-is. Add in place: '⚠ 2026-08-12: "adopted defaults" here means the **prior** `cp_revision` (`prior_2026_08_11`); at  |
| 63 | `docs/34_observed_enso_contrast.md:204` | 22/22 | 22 of 22 station-ratios exceed 1.0. There are no counter-examples. | The headline denominator is contradicted by two of its own downstream artifacts and docs/34 carries no pointer to either. `notebooks/16_observed_enso_contrast.ipynb` (generator `src/nbgen/make_nb16.py:2384`) prints a comparison row 'docs34 = 22 of 22 / measured = 24 of 24 — DISAGREES in the denominator only; unanimity is confirmed', and docs/39 §K7 + progress_map:877 record '22 of 22 is a POOLED count across estimato |
| 64 | `src/nbgen/make_nb18.py:1499` | V4_dg / buarque_2015_dg | names2 = ['V0 ours\n(the engine input\nTODAY)', 'V4 hybrid\n(his 3 levers,\nour $L$)', 'V4_dg source read\nWHOLE (ADOPTED,\nnot exercised)'] | Figure axis labels are stale after ACT 2 and contradict this same generator's own code comment at :697 ('ACT 2 (2026-08-12) moved the engine default to V4_dg; this notebook is a V0 record'). Change 'V0 ours (the engine input TODAY)' -> 'V0 ours (the PRIOR engine input; this notebook is pinned to it)' and 'V4_dg source read WHOLE (ADOPTED, not exercised)' -> 'V4_dg source read WHOLE (ADOPTED — engine default since ACT |
| 65 | `src/nbgen/make_nb18.py:915` | V4_dg / buarque_2015_dg | ('ls2d_hs', 'hillslope-limited (upslope area capped at 1 km2) - ADOPTED'), | Labels the V0 column as ADOPTED; the adopted LS field is V4_dg (`ls_formulation = buarque_2015_dg`, f_LS = 0.25146 erosion-weighted). Change the note to 'hillslope-limited (upslope area capped at 1 km2) — V0, the PRIOR engine default; superseded as the default by V4_dg (ACT 2)'. Regenerate; the executed string is at notebooks/18_musle_construction.ipynb:1334. Same label at :972 ('ls2d_hs\n(ADOPTED)') and :1073. |

*Sweep summary:* Per-number tally (occurrences seen / verdict).  r ≈ 0.57: ~45 sites (CLAUDE.md, docs/16,18,21,22,24,25,31,33,36,38,45,55,56,58,59, PROGRESS, progress_map, scripts/build_deck+build_report_pdf+generate_report_figures, nb10/13/14/15/18) — ALL AGREE; the El Niño per-config band 0.556–0.572 and CAL r 0.5564 are different statistics and are labelled as such, and docs/33:430-436 already journals the brief's 'CAL r = 0.518' vs the on-disk 0.5564. \|\| H2E F = 0.25931 / 0.25930593639066796: ~75 sites — ALL AGREE, to 1e-8 everywhere; docs/39:81 already audits this and marks it ✔. \|\| f_LS = 0.25146: ~380 hits across 68 files — ALL AGREE, always erosion-weighted, never swapped with the area figure. Area-weighted 0.2446790094097074: ~50 hits — ALL AGREE; docs/47 §4.3's 0.24466/0.42135 pair is the adjudicated 'third support' (docs/46 §10 amd 2), annotated not replaced. \|\| 299.5387 Mt/yr: 167 '299.5387' + 80 full-precision + 49 '299.539' + 40 '299.54' — ALL AGREE. Two disclosed re-derivations (docs/47:431 299.53867, 4.97e-6 rel; journal_x59-inputs:141 299.4977, 0.0137 %) both state their own deviation. 248.7298: 130 '248.730' + 66 + 31 — ALL AGREE; 248.696 (docs/42) is the days-per-year-divisor variant and is reconciled in docs/42 §9.7 / docs/48 §2.5. The only defects are missing cp_revision qualifiers (hits above). \|\| condition number: ~35 sites, all 'inf' / '∞'; the 5,682 composition-form value is a distinct quantity and is labelled every time. NO disagreement. \|\| α ≈ 0.48 unconst

### sweep: embargo-convention - 18 defects

| # | file:line | retired term | the offending text | old -> new |
|--:|---|---|---|---|
| 66 | `scripts/build_report_pdf.py:249` | Mt/yr load without convention + cp_revision | "hillslope erosion at the reference parameters is 299.54 million tonnes per year.", BODY)) | docs/37 §A1.5 rule 6: "NEVER quote a load without its convention AND its cp_revision." The word `cp_revision` appears nowhere in scripts/build_report_pdf.py. Change to: "...hillslope erosion at the reference parameters (volume_convention=williams_m3, k_unit_system=us_customary, cp_revision='cited_central_2026_08_11') is 299.5387 million tonnes per year — an unfitted lower bound, and never a per-area yield." |
| 67 | `scripts/build_deck.py:442` | 299.5 Mt/yr "quoted with its convention" — but no convention given | "Basin gross hillslope erosion at reference parameters: 299.5 Mt/yr — quoted with its convention, " "NEVER as a per-area yield (catchment-area embargo, docs/23 §13.2)" | The bullet asserts compliance without complying — it names neither convention nor cp_revision, and `cp_revision` appears nowhere in scripts/build_deck.py. Replace the first clause with: "Basin gross hillslope erosion: 299.5387 Mt/yr @ williams_m3 / us_customary / cp_revision='cited_central_2026_08_11' — " and keep the "NEVER as a per-area yield" clause. |
| 68 | `scripts/build_report_pdf.py:432` | "Absolute sediment yields (tonnes per km² per year)" | "...which is more robust than any single larger dataset would be. Absolute sediment yields (tonnes per " "km² per year) are deliberately not reported, because the catchment areas are unreliable and are " "under embargo; all results are stated as ratios or as b | Terminology inversion in the PDF deliverable: t/km²/yr is the SPECIFIC (area-normalised) yield, and absolute flux is exactly what this project DOES report. As written a reader concludes absolute fluxes are embargoed. Change "Absolute sediment yields (tonnes per km² per year)" -> "Area-normalised sediment yields (tonnes per km² per year)". |
| 69 | `docs/40_sdr_evidence.md:148` | 248.730 Mt/yr labelled "adopted conventions" | \| gross **hillslope** erosion, MUSLE, basin decade 2009–2018 \| **248.730 Mt/yr** \| `docs/37` §2; `src/mgb_sediment.py`, adopted conventions \| | 248.7298 Mt/yr is the PRIOR cp_revision ('prior_2026_08_11'); the adopted one is 299.5387 Mt/yr. `cp_revision` appears zero times in docs/40. Change the provenance cell to "`docs/37` §2; `src/mgb_sediment.py`, volume_convention=williams_m3 + k_unit_system=us_customary @ **cp_revision='prior_2026_08_11'** — superseded as the level by 299.5387 Mt/yr at cp_revision='cited_central_2026_08_11'; see §0", and add a one-line |
| 70 | `docs/40_sdr_evidence.md:149` | 967.46 t/km²/yr = 9.675 t/ha/yr | \| — as a basin-mean rate over 257,097 km² \| **967.46 t/km²/yr = 9.675 t/ha/yr** \| computed here \| | A model specific-erosion rate that is NOT labelled model-internal — the one thing docs/23 §13.2 requires for any area-normalised figure that is allowed to exist. docs/40 §8.3 even claims only "§7 Leg B's model figure is labelled model-internal throughout", so the doc knows the gap. Change the quantity cell to "— as a **MODEL-INTERNAL** basin-mean specific erosion over the model's own 257,097 km² (NOT a gauge-referenc |
| 71 | `docs/40_sdr_evidence.md:491` | basin-mean gross hillslope erosion is 967.46 t km⁻² yr⁻¹ | **~690 t km⁻² yr⁻¹**. Our **basin-mean gross hillslope erosion is 967.46 t km⁻² yr⁻¹** — only | Same defect as :149, inside §7 Leg C, where it is put directly beside published gauge-derived yields (690 / 2,200 t km⁻² yr⁻¹) and so is most likely to be read as a comparable yield. Insert the label: "Our **model-internal basin-mean specific erosion (model erosion over model area, NOT a gauge-referenced yield) is 967.46 t km⁻² yr⁻¹**" — matching the wording already used for Leg B at :484. |
| 72 | `docs/40_sdr_evidence.md:459` | Ours is 9.675 t ha⁻¹ a⁻¹ | Tan, Liu & Lu (2024) report **23.7 – 26.5 t ha⁻¹ a⁻¹** basin-average RUSLE hillslope erosion. Ours is **9.675 t ha⁻¹ a⁻¹**. | Unlabelled model specific erosion AND a prior-cp_revision value used in a live comparison table (rows at :462-463 propagate it). Change to "Ours, model-internal, at cp_revision='prior_2026_08_11', is **9.675 t ha⁻¹ a⁻¹** (**11.6508** at the adopted cp_revision — see §0)". |
| 73 | `src/nbgen/make_nb18.py:2634` | "the model is 1.03x - 2.27x under-erosive" (withdrawn direction) + withdrawn 2,200 t/km²/yr max-yield leg | print(f'\ncombined: the model is {COMB_LO:.2f}x - {COMB_HI:.2f}x under-erosive') | COMB_LO/COMB_HI are built at :2632-2633 from `2200.0/BASIN_T_KM2_YR`, i.e. Leg C's max-yield form, which docs/40 §0.2 and docs/37 A1.9 WITHDREW as invalid at basin scale; and the directed word "under-erosive" is itself withdrawn (docs/37 A1.9.2, docs/47 §396). `A1.9` / `WITHDRAWN` appear ZERO times in make_nb18.py — there is no in-place caveat. Fix: drop the max form from the combined bracket, print both readings (er |
| 74 | `notebooks/18_musle_construction.ipynb:3999` | executed output: "combined: the model is 1.03x - 2.27x under-erosive" | "combined: the model is 1.03x - 2.27x under-erosive\n", | Executed-output twin of make_nb18.py:2634 (see also :4031, :4212, :4940 in this notebook). Do NOT hand-edit — notebooks 10-19 are generated. Fix src/nbgen/make_nb18.py, rerun the generator, then re-execute nb18 with `python -m nbconvert` (nb18 registers timeout=-1, so budget for a long run). |
| 75 | `src/nbgen/make_nb18.py:2738` | narrative "1.03x to 2.27x under-erosive" and "1.888x short" of the 2,200 t/km²/yr maximum | **1.03x to 2.27x under-erosive**. Closing Leg A with the coefficient alone would need | The `reading(...)` prose (approx. lines 2730-2745, plus the §7 summary at :3212) states the withdrawn direction as fact and keeps Leg C's max form as "**1.888x short** of the in-basin maximum of 2,200". Rewrite to docs/37 A1.9.2-A1.9.3's wording: legs printed under BOTH readings, Leg C max form marked WITHDRAWN (as make_nb19.py:650 already does correctly), summary reading "direction unknown". |
| 76 | `docs/PROGRESS.md:90` | basin erosion 299.5387 Mt/yr, no convention, no cp_revision | ### C3 — MUSLE hillslope erosion 🟡 **BUILT & RUN — C3 OPEN** (docs/37 A1–A3) · C3.1 LS *formulation* ENACTED (A3: ADOPT-SOURCE `buarque_2015_dg`); basin erosion 299.5387 Mt/yr; C3.5 still 🔴 (musle.py absent) | Append the pair: "basin erosion 299.5387 Mt/yr @ williams_m3 / us_customary / cp_revision='cited_central_2026_08_11' (unfitted α, β — a lower bound)". docs/PROGRESS.md already carries the yield embargo at :34 and :168 but never names a cp_revision. |
| 77 | `data/processed/ls_stratified_report.md:3` | 299.5387 Mt/yr "(adopted defaults)" | Basin erosion gate: **299.5387 Mt/yr** (adopted defaults). | "adopted defaults" is not the required convention + cp_revision pair. Fix in the generator, not the artifact: scripts/c3/ls_stratified_report.py:268 — change the f-string to "(volume_convention=williams_m3, k_unit_system=us_customary, cp_revision='cited_central_2026_08_11')". Same wording change applies to the sibling gate strings in scripts/c3/ls_erosion_weights.py:26, :85 and :134. |
| 78 | `docs/24_presentation_outline.md:382` | "always quoted with its convention *and* `cp_revision`" — but neither is given | - Basin gross hillslope erosion at reference parameters: **299.5 Mt/yr** (always quoted with its   convention *and* `cp_revision`; **never** a yield). | The outline states the rule instead of obeying it, and scripts/build_deck.py:442 inherited exactly that defect. Change to: "Basin gross hillslope erosion: **299.5387 Mt/yr** @ `williams_m3` / `us_customary` / `cp_revision='cited_central_2026_08_11'`; **never** a per-area yield." |
| 79 | `docs/59_cross_implementation_comparison.md:844` | 199.29 of 299.54 Mt/yr, no adjacent convention/cp_revision | 199.29 of 299.54 Mt/yr — is upstream of no usable SSC station**, including **801.1 km** of channel | Low severity: docs/59 names `cp_revision='cited_central_2026_08_11'` once at :582 (a table row about R1's C factor) but the two G9 coverage sentences that quote the load (:844 and :945) carry neither convention nor cp_revision. Add "(@ `cp_revision='cited_central_2026_08_11'`)" after "299.54 Mt/yr" in both, or add a one-line convention declaration to §0 covering every load in the document. |
| 80 | `scripts/build_deck.py:163` | "among the world's highest specific sediment yields" — uncited external yield claim | "Magdalena–Cauca: 257,097 km², among the world's highest specific sediment yields", | An external (someone else's) specific-yield claim used as framing on slide 2 of the deliverable deck, with no attribution — the hunt rule allows a cited external yield only if clearly attributed. The support exists in-repo: docs/40 §4.1 row M4 (Restrepo A. 2015, RACCEFYN 39(151):250 — 690 t km⁻² año⁻¹ at Calamar, highest on the continent vs Amazon 167, Orinoco 158). Change to "...among the world's highest specific se |
| 81 | `docs/24_presentation_outline.md:26` | "among the world's highest specific sediment yields" — uncited | - Magdalena–Cauca: **257,097 km²**, among the world's highest specific sediment yields. | Same uncited external-yield framing as scripts/build_deck.py:163 (this outline is what the deck was built from). Attribute it: "...among the world's highest specific sediment yields (Restrepo 2015; docs/40 §4.1 M4 — a *published* yield, not one we compute)." |
| 82 | `docs/00_objectives_and_hypotheses.md:9` | "carries one of the largest specific sediment yields in South America" — uncited | Andes to the Caribbean and carries one of the largest specific sediment yields in South America. | Uncited external specific-yield claim in the objectives document. Add the attribution used elsewhere in the repo: "...(Restrepo & Syvitski 2006; Restrepo 2015 — see docs/40 §4.1)". No number needs to be quoted; the point is that it is *their* yield, not ours. |
| 83 | `docs/14_presentation_plan.md:17` | "one of the world's largest specific sediment yields" — uncited | **2. Problem & motivation (1 slide max).** Magdalena = one of the world's largest specific sediment yields; strong **ENSO**-driven interannual variability; | Same uncited external-yield framing; the plan already cites Restrepo two bullets later for the ENSO link. Change to "Magdalena = one of the world's largest specific sediment yields **(Restrepo 2015 — published, ours is embargoed)**". |

*Sweep summary:* EMBARGO ITSELF: CLEAN. I found no gauge-referenced t/km²/yr anywhere in the repo. Of ~130 hits on the yield-unit terms, the overwhelming majority are (a) affirmations OF the embargo, (b) model-internal specific erosion explicitly labelled as such, or (c) attributed published yields (Restrepo 2015 550/690/710; Latrubesse & Restrepo 2014 1,485; Restrepo et al. 2006 128-2,200/690; Tan et al. 2024 23.7-26.5 and 1.3-16.9 t/ha/a). Enforcement is real and mechanical: src/mgb_sediment.py:313 and src/mgb_transport.py:179/:843 state that nothing in those modules divides by an area; tests/test_sediment.py:8 and tests/test_transport.py:17-18 assert the same; notebooks 18 and 19 carry integrity assertions ("no gauge-referenced t/km2/yr yield is reported anywhere in this notebook", nb18:5027, nb19:4473); nb15 §8.4, nb16 and nb17 define specific erosion and mark it embargoed for gauge-referenced results. progress_map.html and presentation_guide.html state the embargo and never breach it. docs/34, 42, 43, 45, 46, 48-57, 59 each carry an explicit in-force declaration.  THE DEFECTS ARE ON THE CONVENTION/cp_revision SIDE, and they cluster in three places.  (1) THE TWO DELIVERABLE BUILDERS. scripts/build_report_pdf.py and scripts/build_deck.py both quote 299.5x Mt/yr and the string `cp_revision` appears in NEITHER file. build_deck.py:442 is the sharpest case — it literally says the number is "quoted with its convention" while naming no convention. build_report_pdf.py:432 additionally inverts the

### sweep: stale-status - 50 defects

| # | file:line | retired term | the offending text | old -> new |
|--:|---|---|---|---|
| 84 | `docs/55_c43_verdict.md:123` | C5 not started | - **C5** (the ENSO out-of-sample application, 2011 / 2015–16) — not started; the strictly out-of-sample windows are untouched by this fit. | CONFIRMED at line 123, in §6 "Owed and NOT done" — a live owed-list, and its two sibling bullets were already updated with "DONE (2026-08-13)" / "DONE (2026-08-12)", so this one reads as still-owed. Replace with: "- **C5 — DONE (docs/56).** The ENSO out-of-sample application ran on the frozen configuration: contrast REPRODUCED, 18/18 stations, median rate ratio 3.05x (1.62-4.85). The windows were out-of-sample to thi |
| 85 | `CLAUDE.md:115` | C5 not started / C4.3 BLOCKED | · **C4: C4.1/C4.2 landed, and C4.3 — the sediment calibration search — is BLOCKED** (docs/47, `C4.3-BLOCKED-UNTIL-LS-LANDS`; ...) · **C5 not started.** | CLAUDE.md is the always-loaded instruction file — this is the highest-impact stale status in the repo. Replace the C4/C5 clauses with: "**C4.3 RAN — docs/55: RAILED / EXPLORATORY, NOT adopted** (estimator (a) median KGE_ln −0.118, (b) +0.139, same sign; alpha wants ~0.48, below the box floor; design-matrix condition number inf, only Pi identifiable) · **C5 COMPLETE — docs/56: the ENSO contrast is REPRODUCED, 18/18, m |
| 86 | `CLAUDE.md:82` | C4.3-BLOCKED-UNTIL-LS-LANDS | - `docs/47_c4_entry_verdict.md` — **the authority on whether stage C4.3 may start, and it says NO:** `C4.3-BLOCKED-UNTIL-LS-LANDS`. Read it before touching sediment calibration. | docs/47 legitimately OWNS the token, but CLAUDE.md presents it in the present tense as a live gate on work. Rewrite: "- `docs/47_c4_entry_verdict.md` — the C4.3 **entry verdict** as of 2026-08-11: `C4.3-BLOCKED-UNTIL-LS-LANDS`. **The block has since been discharged and C4.3 ran — the outcome is `docs/55` (RAILED / EXPLORATORY, not adopted).** Read 47 for the entry contract and the guard set, 55 for what happened." Ad |
| 87 | `docs/00_INDEX.md:35` | C4.3 ... formally BLOCKED / C5 remain | deep), and calibration + the ENSO experiment (C4, C5) remain — with **C4.3, the calibration search itself, formally BLOCKED** until the LS level lands (`docs/47`; still in force as of 2026-08-12 per `docs/37` A3.4). | This is the INDEX's one-paragraph project statement — the first thing a newcomer reads. Replace "calibration + the ENSO experiment (C4, C5) remain — with C4.3 ... formally BLOCKED ..." with: "C4.3 ran and its fit is **RAILED / EXPLORATORY, not adopted** (`docs/55`), and C5 — the ENSO experiment — is **COMPLETE and is the headline result**: the model reproduces the observed contrast, 18/18 stations, median rate ratio  |
| 88 | `docs/00_INDEX.md:175` | C4.3-BLOCKED-UNTIL-LS-LANDS | \| **May C4 start, and under what constraints?** \| ⚠ **CORRECTED 2026-08-12 — the answer for the search stage is now NO.** ... **The block is still in force**: upheld by `docs/46` §6.4, `docs/51` §4, `docs/53`, and `docs/37` **A3.4** | WHERE-IS-IT row answering a question people actually ask, in the present tense. Re-answer the row: "**It started and it finished.** `docs/47`'s `C4.3-BLOCKED-UNTIL-LS-LANDS` was the entry verdict of 2026-08-11 and it was discharged (LS adopted: `ls_formulation = buarque_2015_dg`, f_LS = 0.25146 erosion-weighted). C4.3 then ran as a Branch-B first run: **RAILED / EXPLORATORY, NOT adopted** (`docs/55`)." Keep the arith |
| 89 | `docs/00_INDEX.md:258` | 82/82 tests | \| Sediment engine build + test/mass-ledger evidence (82/82 tests, ledger closed exactly) \| `journal_c34-sediment-engine.md` + tracker \| **not yet** — expected in 37 \| | "82/82 tests" is the build-time count and is stale twice over (82 -> 140 -> 154). Change to "(82/82 tests **at build time; the suite is now 154 passed**, ledger closed exactly)". The "not yet — expected in 37" column is also stale: docs/37 §A3.9 now carries the pytest evidence (`docs/37`:2395, "pytest 154 passed"). |
| 90 | `progress_map.html:581` | C4.3 is BLOCKED / 140 passed | updated:"2026-08-12 · ... REVERSED: C4.3 is BLOCKED, not gated — docs/47 C4.3-BLOCKED-UNTIL-LS-LANDS is in force ... Engine tests MEASURED TODAY: 140 passed, 1 warning ... Next free document number is 54." | progress_map.html is the file RULE 0 designates as the LIVE tracker, so every stale claim in it is maximally load-bearing. Rewrite DATA.updated for the post-C5 state: C4.3 RAN (docs/55, RAILED/EXPLORATORY, not adopted); C5 COMPLETE (docs/56, 18/18, median 3.05x); B5 (docs/57), ceiling bound (docs/58), cross-implementation (docs/59); engine tests **154 passed**; next free document number is 60. |
| 91 | `progress_map.html:582` | C4.3 IS BLOCKED / ENGINE TESTS: 140 passed | banner:"<b>🔴 C4.3 IS BLOCKED — NOT "GATED". ...</b> ... <b>the sediment calibration search may not start.</b> ... <b>ENGINE TESTS: 140 passed, 1 warning</b>, measured on this box today with <code>python3.10 -m pytest tests/ -q</code> | Replace the whole banner. It is the first thing the tracker renders and it now states the opposite of the project's headline. New banner should lead with C5: the model REPRODUCES the observed ENSO contrast, 18/18 stations, median rate ratio 3.05x (docs/56); C4.3 ran and is RAILED/EXPLORATORY, not adopted (docs/55); tests **154 passed**. Preserve the 2026-08-12 banner as another `banner_superseded_*` archive key, exac |
| 92 | `progress_map.html:107` | C4.3, the calibration search, is BLOCKED | <div style="margin-top:8px;color:#f3c9c4">Start with <b>C4</b> — <b>C4.3, the calibration search, is BLOCKED</b> (docs/47), the tracker said the opposite until 2026-08-12, and the LS landing on 2026-08-12 did <b>not</b> lift it. | "How to read this" reading-order panel. Change to: "Start with <b>C5</b> — the ENSO contrast is REPRODUCED, 18/18 stations, median rate ratio 3.05x (docs/56); then <b>C4.3</b>, whose fit RAILED and is EXPLORATORY, not adopted (docs/55)." |
| 93 | `progress_map.html:743` | 140 passed | n("C3.4 src/mgb_sediment.py + engine tests — ledger exact, suite GREEN at 140 passed","done","[CORRECTED 2026-08-12 — measured on this box with python3.10 -m pytest tests/ -q: <b>140 passed, 1 warning</b> in 149.75 s. ... | Node title and body both name 140 as the current suite size. Change the title to "suite GREEN at 154 passed" and add a dated line to the body: "[RE-MEASURED — python3.10 -m pytest -q -> **154 passed**; the 140 reading is the 2026-08-12 state and is kept as the record.]" |
| 94 | `progress_map.html:790` | 140 passed | 42 transport tests; suite was <b>138 passed</b> at build, and is <b>140 passed, 1 warning</b> today (measured 2026-08-12). | The word "today" makes this a current claim. Change to "...was <b>138 passed</b> at build, <b>140</b> on 2026-08-12, and is <b>154 passed</b> now." |
| 95 | `progress_map.html:803` | C4.3 ... BLOCKED | n("C4.3 search, report, verdict (upstream of Mompós only) — 🔴 BLOCKED","blocked","[STATUS CORRECTED 2026-08-12: this node read 'todo — everything it needs is now frozen'. It is <b>BLOCKED</b>. docs/47: <code>C4.3-BLOCKED-UNTIL-LS-LANDS</code> | Move the node status from "blocked" to "done" and retitle: "C4.3 search, report, verdict — RAN; RAILED / EXPLORATORY, NOT adopted (docs/55)". Keep the 2026-08-12 block text as a dated archived paragraph inside the body, since the block is why the fit had to be a Branch-B first run. |
| 96 | `progress_map.html:805` | C5 ... todo | n("C5 — the ENSO experiment (the deliverable)","todo","[CORRECTED 2026-08-12 — numbering has moved eleven more places since this said 40+ ... | Flip the node (and its four children C5.1–C5.4 at lines 806–813) from "todo" to "done" and record the result: 18/18 stations, median rate ratio 3.05x, geo-mean 3.06, range 1.62–4.85, robust across beta and both window pairs; write-up is `docs/56`, not "docs/40+". |
| 97 | `progress_map.html:944` | C4.3 is BLOCKED / C5 ahead | <b>🔴 C4.3 is BLOCKED, not gated</b> (docs/47 <code>C4.3-BLOCKED-UNTIL-LS-LANDS</code>; C4.1/C4.2/C4.2n are delivered) · C5 ahead. | Rendered progress footer. Change to "C4.3 RAN — RAILED / EXPLORATORY, not adopted (docs/55) · **C5 COMPLETE — contrast reproduced 18/18, median 3.05x (docs/56)**", and recompute the completion percentage after flipping the C4.3/C5 leaves. |
| 98 | `progress_map.html:615` | Phase C — sediment (ACTIVE) | n("Phase C — sediment (ACTIVE)","wip","[CORRECTED 2026-08-12] C0 ✅ · C1 ✅ · C2 ✅ · C2b closed · C3 still 🔴 OPEN at docs/37 A1, A1.9, A2 and A3 ... | Phase C is COMPLETE (C5 done, docs/56). Retitle the node "Phase C — sediment (COMPLETE)" with status "done", and state in the body that C3 remains formally OPEN on its closure conjunction while the phase's deliverable (C5) landed — the two are not the same claim. |
| 99 | `docs/PROGRESS.md:98` | C4.3 BLOCKED — sediment NOT calibrated | ### C4 — channel transport + sediment calibration 🔴 **C4.3 BLOCKED — sediment NOT calibrated** (docs/47 `C4.3-BLOCKED-UNTIL-LS-LANDS`; four blockers in docs/47 §9.2, the LS *level* is the binding one) | The file's own §Phase-C note (line 61-63) says "the stage headers are kept current", so this header is a current claim despite the doc-level SUPERSEDED banner. Change to: "### C4 — channel transport + sediment calibration ✅ **C4.3 RAN — RAILED / EXPLORATORY, NOT adopted** (docs/55; alpha wants ~0.48 below the box floor, condition number inf, only Pi identifiable)". |
| 100 | `docs/PROGRESS.md:103` | C5 ... ⬜ | ### C5 — the ENSO experiment ⬜ — **the deliverable, 1–2 sessions** | Change to "### C5 — the ENSO experiment ✅ **COMPLETE — the contrast is REPRODUCED** (docs/56: 18/18 stations, median rate ratio 3.05x, range 1.62–4.85)", and tick C5.1–C5.4 at lines 104–107. Line 107's "C5.4 write-up → `docs/35`" is doubly stale — the write-up is `docs/56`. |
| 101 | `docs/PROGRESS.md:63` | C4.3 🔴 BLOCKED — sediment NOT calibrated | > C3.1 enacted (C3 OPEN), C4.3 🔴 BLOCKED — sediment NOT calibrated.** | This is the "stage headers are kept current" note (lines 61-63). Update the as-of date and the state: "As of the C5 landing: C0/C1/C2 ✅, C2b ✅, C3.1 enacted (C3 OPEN), **C4.3 RAN — RAILED / EXPLORATORY, not adopted (docs/55)**, **C5 ✅ COMPLETE (docs/56)**." |
| 102 | `docs/PROGRESS.md:11` | C4.3 — the sediment calibration search — is formally BLOCKED | > OPEN** ([docs/37](37_c3_closure.md), four amendments), and **C4.3 — the sediment calibration search — is formally BLOCKED**: [docs/47](47_c4_entry_verdict.md), *"`C4.3-BLOCKED-UNTIL-LS-LANDS`. **C4.3 may not start.**"* | The "WIDENED 2026-08-12" banner corrects the tree in place and so is read as current. Append a further dated widening: "⚠ WIDENED AGAIN — the block was discharged; C4.3 RAN (docs/55, RAILED / EXPLORATORY, not adopted) and C5 COMPLETED (docs/56, 18/18, median 3.05x). The numbered docs now run to 59." Line 14's "the numbered docs now run to 53" is stale in the same breath. |
| 103 | `docs/30_phase_c_plan.md:174` | C4.3 is BLOCKED / C5 → not started | > **C3** → `docs/37` (**OPEN**, ...) and `docs/43` · **C4** → `docs/42` (guards), `docs/45` (pre-registration), `docs/47` (**C4.3 is BLOCKED**) · **C5** → not started. Read the stage text below as the original intent | This is the STATUS POINTER added 2026-08-12 to route readers to live owners — it must name the current owners. Change to: "**C4** → `docs/42` (guards), `docs/45` (pre-registration), `docs/47` (the entry verdict) and **`docs/55` (the C4.3 outcome: RAILED / EXPLORATORY, not adopted)** · **C5** → **`docs/56` (COMPLETE — contrast reproduced, 18/18, median 3.05x)**." |
| 104 | `docs/31_phase_c_workplan.md:29` | C4.3 is BLOCKED | > \| **C4** \| `docs/45` (C4.2 prereg) · **`docs/47` (the entry verdict)** \| C4.1/C4.2 landed; **C4.3 is BLOCKED — see the marker at C4.3 below** \| | CLAUDE.md instructs sessions to "check the stage-status pointer at the top and the ⛔ marker at C4.3 first", so this table is the operative status for anyone starting Phase C work. Change the C4 row outcome to "C4.1/C4.2 landed; **C4.3 RAN — `docs/55`: RAILED / EXPLORATORY, NOT adopted**" and update the "As of 2026-08-12" date stamp above it. |
| 105 | `docs/31_phase_c_workplan.md:30` | C5 \| — \| not started | > \| **C5** \| — \| not started \| | Change to "\| **C5** \| `docs/56` \| **complete** — the modelled ENSO contrast REPRODUCES the observed one: 18/18 stations, median rate ratio 3.05x (1.62–4.85), robust across beta and both window pairs \|". |
| 106 | `docs/31_phase_c_workplan.md:428` | ⛔ C4.3 IS BLOCKED — do not start this subtask | > ## ⛔ C4.3 IS BLOCKED — do not start this subtask. Marker added 2026-08-12. | This is the ⛔ marker CLAUDE.md sends sessions to read first; as written it forbids work that has already been done. Retitle the block "## ✅ C4.3 HAS RUN — this marker is the historical entry condition. Marker added 2026-08-12; discharged and superseded by `docs/55`." and open it with the outcome (RAILED / EXPLORATORY, not adopted) before the preserved 2026-08-12 reasoning. |
| 107 | `docs/31_phase_c_workplan.md:507` | C4.3 is BLOCKED | the figure set."* — **target corrected 2026-08-12.** Note C5 depends on C4, and **C4.3 is BLOCKED** (`docs/47`). | Inside the Stage C5 paste-prompt, which a session would copy verbatim. Replace the trailing note with "— **C5 has since been executed; its read-out is `docs/56`.** The paste-prompt is preserved as the original instruction." and correct the deliverable target from "the next free doc number (54)" to `docs/56`. |
| 108 | `docs/31_phase_c_workplan.md:603` | ⛔ C4.3 BLOCKED | > C0 ──► C3 (OPEN, docs/37) ──► C4.1 ✔ ──► C4.2 ✔ ──► ⛔ C4.3 BLOCKED (docs/47) ──► C5 | Redraw the gated dependency graph (added 2026-08-12) as "C0 ──► C3 (OPEN, docs/37) ──► C4.1 ✔ ──► C4.2 ✔ ──► C4.3 ✔ RAILED/EXPLORATORY (docs/55) ──► C5 ✔ CONTRAST REPRODUCED (docs/56)", and delete the following sentence "C5 depends on C4.3 and therefore inherits the block" (line 608). |
| 109 | `docs/54_c3_1_closure_and_c4_entry_status.md:60` | Why C4.3 STILL may not start | ## 4 — Why C4.3 STILL may not start: the four surviving blockers (`docs/47` §9.2, verbatim-owned) | docs/54 carries no supersession banner and its §4 heading reads as the live entry status (the word "STILL"). Add a dated header banner: "> **SUPERSEDED for C4.3 entry status, [date].** The four blockers were discharged — LS adopted (`buarque_2015_dg`, f_LS = 0.25146), the `V4_dg` column materialised by ACT 1 and made the engine default by ACT 2 — and C4.3 RAN. Outcome: `docs/55`, RAILED / EXPLORATORY, not adopted. Re |
| 110 | `docs/54_c3_1_closure_and_c4_entry_status.md:137` | 140 passed | **140 passed** (or record the new count) from executed output, never an exit code. | This is §7 residue item 3, an open instruction naming 140 as the number to confirm. Discharge it in place: "3. ~~Re-run the test suite — confirm still **140 passed**~~ → **DONE: `python3.10 -m pytest -q` -> 154 passed.**" (§3's line 56 "suite **140 passed** at that commit" is correctly scoped and can stay.) |
| 111 | `docs/16_forcing_pipeline_audit.md:68` | C4.3 ... formally BLOCKED | > For where Phase C stands **today** — including that stage **C4.3, the sediment calibration search, is formally BLOCKED** ([docs/47](47_c4_entry_verdict.md): *"`C4.3-BLOCKED-UNTIL-LS-LANDS`. **C4.3 may not start.**"*) | The struck §1 clauses above this are correct house style; this forward-pointer is not, because it says "today". docs/16 is a mandatory pre-read. Replace with: "For where Phase C stands today: **C4.3 ran — `docs/55` (RAILED / EXPLORATORY, not adopted) — and C5 is COMPLETE — `docs/56` (contrast reproduced, 18/18, median 3.05x).** The 2026-08-11 `C4.3-BLOCKED-UNTIL-LS-LANDS` verdict (`docs/47`) was the entry condition a |
| 112 | `docs/18_hydrology_journal.md:77` | C4.3 ... formally BLOCKED | > Phase C has since advanced to C3 (OPEN — [docs/37](37_c3_closure.md), four amendments) with **C4.3, the sediment calibration search, formally BLOCKED** ([docs/47](47_c4_entry_verdict.md): *"`C4.3-BLOCKED-UNTIL-LS-LANDS`. **C4.3 may not start.**"*). | Same shape as docs/16:68 — a "has since advanced to" pointer that has itself been overtaken. Replace with "Phase C has since completed: C3 remains OPEN on its closure conjunction (`docs/37`), C4.3 ran and RAILED (`docs/55`, EXPLORATORY, not adopted), and **C5 reproduced the observed ENSO contrast, 18/18 stations, median rate ratio 3.05x (`docs/56`)**." |
| 113 | `docs/progress_journal.md:8` | C4.3 may not start | > [docs/47](47_c4_entry_verdict.md) records *"`C4.3-BLOCKED-UNTIL-LS-LANDS`. **C4.3 may not start.**"* **The dated entries below are correct as history and are not edited** | The dated log entries below are correctly frozen; the 2026-08-12 "Range widened" banner is not — it names the current frontier and is now two stages behind. Update it: numbered docs run to **59** (44 never assigned); Phase C has reached **C4.3 (RAN — docs/55, RAILED / EXPLORATORY)** and **C5 (COMPLETE — docs/56)**. |
| 114 | `src/mgb_sediment.py:254` | C4.3 stays BLOCKED | fitted one is not either.  **C4.3 stays BLOCKED** - blockers 1 (Branch B / first-run on the adopted field), 4 (owed deliverables) and the LS **SHAPE** decision are untouched by this switch (docs/47, docs/54 §4). | Module docstring of the live sediment engine — read by anyone opening the file. Change to "**C4.3 has since RUN** as the Branch-B first run this paragraph anticipated; outcome `docs/55`: RAILED / EXPLORATORY, **not adopted** (alpha wants ~0.48, below the box floor; design-matrix condition number inf, only the product Pi is identifiable). The LS **LEVEL** is still UNVALIDATED and the LS **SHAPE** decision is still owe |
| 115 | `src/nbgen/make_nb15.py:54` | C4.3 is BLOCKED until the LS level lands | > (2026-08-11); and **C4.3 is BLOCKED until the LS level lands** (`docs/47`). None of §9's permitted/forbidden list has been relaxed | Inside the nb15 "Where things have moved since, downstream of this page" banner — a forward-status block, so it must carry current status. Replace with "**the LS level has since landed** (`ls_formulation = buarque_2015_dg`, f_LS = 0.25146 erosion-weighted), **C4.3 ran** (`docs/55`, RAILED / EXPLORATORY, not adopted) and **C5 is COMPLETE** (`docs/56`, contrast reproduced 18/18, median 3.05x)." Edit the GENERATOR then  |
| 116 | `notebooks/15_ssc_quality_gate.ipynb:24` | C4.3 is BLOCKED until the LS level lands | "> (2026-08-11); and **C4.3 is BLOCKED until the LS level lands** (`docs/47`). None of §9's\n", | Generated notebook — do NOT hand-edit. Fix src/nbgen/make_nb15.py:54 and regenerate (nb15 registers timeout=-1, so check the re-execute cost first, or regenerate source-only and re-execute deliberately). |
| 117 | `src/nbgen/make_nb16.py:52` | C4.3 is BLOCKED until the LS level lands | > 2026-08-11) and **C4.3 is BLOCKED until the LS level lands** (`docs/47`).""") | nb16's "Where things have moved since" banner also says "§10 hands forward to C5, and that hand-off has **not** been consumed" — both halves are now false. Replace with: "§10's hand-off to C5 **has been consumed**: `docs/56` reports the modelled contrast reproducing this page's observed one, 18/18 stations, median rate ratio 3.05x. C4.3 ran and RAILED (`docs/55`)." Mirror at notebooks/16_observed_enso_contrast.ipynb: |
| 118 | `notebooks/16_observed_enso_contrast.ipynb:26` | C4.3 is BLOCKED until the LS level lands | "> 2026-08-11) and **C4.3 is BLOCKED until the LS level lands** (`docs/47`)." | Generated notebook — fix src/nbgen/make_nb16.py:52 and regenerate; do not hand-edit. |
| 119 | `src/nbgen/make_nb17.py:56` | C4.3 is BLOCKED until the LS level lands | > **C4.3 is BLOCKED until the LS level lands** (`docs/47`); when it unblocks, C4 is held to `docs/42`'s guards G1-G9. | Replace with "**C4.3 has since run** under `docs/42`'s guards G1–G9 — outcome `docs/55`, RAILED / EXPLORATORY, not adopted; the guards' central read-out stands: alpha, C, LS, K units, volume convention, P and FG are seven ways of writing one product Pi, condition number inf." Mirror at notebooks/17_runoff_signatures.ipynb:30. |
| 120 | `notebooks/17_runoff_signatures.ipynb:30` | C4.3 is BLOCKED until the LS level lands | "> **C4.3 is BLOCKED until the LS level lands** (`docs/47`); when it unblocks, C4 is held to\n", | Generated notebook — fix src/nbgen/make_nb17.py:56 and regenerate; do not hand-edit. |
| 121 | `src/nbgen/make_nb18.py:1367` | C4.3 stays BLOCKED | > *shape* decision: settling the level is necessary and not sufficient) and **C4.3 stays BLOCKED** | Append to the same banner: "**[Since: C4.3 RAN — `docs/55`, RAILED / EXPLORATORY, not adopted — and the engine default has moved to `V4_dg` (ACT 2), so this notebook's V0 numbers are pinned-historical, not the engine's default.]**". Same generator also has it at lines 1470 (a print statement) and 1568; the executed notebook mirrors are notebooks/18_musle_construction.ipynb:2010, 2089, 2193, 2324. |
| 122 | `src/nbgen/make_nb18.py:3198` | 82 tests pass | scale identity $n^{2\beta-1}$ is reproduced to $3\times10^{-15}$; 82 tests pass. | §9 "What this stage established" item 1, asserted in the present tense as evidence the equation is implemented correctly. Change "82 tests pass" -> "the suite passes (**154 tests** at the current build; 82 when this stage was written)". Already logged as item (d) of docs/agents/journal_nbc1-nb18-musle.md:51 ("cells 78/83 say '82 tests pass' / 'the 82-test suite'") — check for a second occurrence phrased "the 82-test  |
| 123 | `notebooks/18_musle_construction.ipynb:4926` | 82 tests pass | "   scale identity $n^{2\\beta-1}$ is reproduced to $3\\times10^{-15}$; 82 tests pass.\n", | Generated notebook — fix src/nbgen/make_nb18.py:3198 and regenerate; do not hand-edit (nb18 registers timeout=-1). |
| 124 | `src/nbgen/make_nb19.py:2748` | C4.3 remains BLOCKED | touch. **C4.3 remains BLOCKED** (`docs/47`).""") | Present-tense status in nb19's C3-gate/C4-setup narrative. Change to "**C4.3 has since run** (`docs/55`): RAILED / EXPLORATORY, not adopted. Clause 2 of C3's closure still fails for the LS *shape* reason stated above." Note the same paragraph's "no engine default moved, so every number in this notebook is still at V0 with f_LS = 1.000" is now only true of this notebook, not of the engine — ACT 2 moved the default to  |
| 125 | `src/nbgen/make_nb19.py:2666` | C4.3 stays BLOCKED | print(f'   either.  C3 stays OPEN (clause 2 also needs the SHAPE decision) and C4.3 stays BLOCKED.') | This prints into executed cell output (notebooks/19_c3_gate_and_c4_setup.ipynb:3789 is the stored output, :3893 the source). Change the printed string to "C3 stays OPEN (clause 2 also needs the SHAPE decision); C4.3 has since RUN - docs/55, RAILED / EXPLORATORY, not adopted." and regenerate + re-execute nb19 (29 cells, previously 0 errors). |
| 126 | `presentation_guide.html:327` | Phase C blocked / Phase B not closed | <li class="find">Phase A complete · Phase B calibrated but <strong>not closed</strong> · Phase C blocked on sediment-concentration data.</li> | presentation_guide.html is the HTML twin of docs/27 + docs/28 but carries NONE of their 2026-08-12 corrections — docs/27:92 and docs/28:69/328 both strike exactly this sentence. Port the corrections wholesale. Here: "Phase A complete · Phase B **closed** at H2E, by decision at a measured input ceiling · Phase C **complete** — the ENSO contrast is reproduced, 18/18 stations." |
| 127 | `presentation_guide.html:552` | blocked on data quality | model</strong> &mdash; a result we can quantify. The sediment phase is built but blocked on data quality.</p></div> | docs/28's Part 0 struck this exact sentence with a dated correction ("⚠ CORRECTED 2026-08-12 — the last sentence. → The sediment phase has started."). Replace with: "The sediment phase is complete: 79/79 SSC stations classified, 18 usable, and the modelled ENSO contrast reproduces the observed one at 18 of 18 stations (median rate ratio 3.05x)." |
| 128 | `presentation_guide.html:1568` | Phase C ... is blocked | calibrated &mdash; three attempts &mdash; but <em>not closed</em>, and that is today's talk. Phase C, the sediment, is blocked on the quality of the sediment concentration data on the main stem. | This is the slide-3 spoken line that docs/27:92-99 marks ⚠ SUPERSEDED with a ⬇ SAY THIS INSTEAD replacement. Paste docs/27's replacement text in: four attempts, Phase B **closed** by decision, Phase C **started** and constrained rather than blocked (79 classified, 18 usable, one Magdalena-trunk station) — then extend it with the C5 result. |
| 129 | `presentation_guide.html:1423` | Phase C ... waits on data quality | <tr><td class="n">5</td><td>Then sediment</td><td>Phase C is <em>built and tested</em> &mdash; it waits on data quality, not on code.</td></tr> | The next-steps table is stale on all five rows (scripts/build_deck.py:414-421 already carries the corrected version). Sync it: items 1–4 DONE (CHIRPS merge bounded at +0.006 r, ceiling STRUCTURAL, docs/58; FAO-56 ET adopted in H2E; seeds expanded, H2E confirmed; Phase C DONE — exploratory fit, contrast reproduced 18/18, docs/55+56); only item 5, catchment areas, remains OWED. |
| 130 | `presentation_guide.html:2019` | It is blocked on two data issues | <p>"The module is built and tested; the satellite retrieval works. It is blocked on two data issues: the concentration measurements on the main stem, and the catchment-area disagreement on slide 16. | Q&A answer to "What happened to the sediment?". Rewrite: "The module ran. The calibration search railed and is reported as exploratory, not adopted — but the ENSO contrast, which is the question, is reproduced at 18 of 18 stations with a median rate ratio of 3.05x. One data issue still binds: the catchment-area disagreement, which is why we report absolute flux and ratios only, never t/km²/yr." |
| 131 | `presentation_guide.html:1399` | Calibration not closed / Three attempts | <tr><td><strong>Calibration not closed</strong></td><td>Three attempts, none meeting every criterion we set in advance.</td></tr> | docs/28:720 struck this with "⚠ CORRECTED 2026-08-12: four attempts, and the calibration IS closed — but by decision, not by passing". Port that wording: "**Calibration closed by decision, not by passing** — four attempts; H2E scores 3/9 on the pre-registered adequacy criteria and was adopted on the docs/29 rules." Same sentence recurs at line 1931 in the spoken script. |
| 132 | `scripts/build_deck.py:393` | The calibration is NOT closed — three attempts | "The calibration is NOT closed — three attempts, none meeting every criterion we set in advance", | Slide 16 "What we cannot yet claim", in a live regenerator whose slide 17 (line 420) already says "Phase C — DONE" and whose note (line 433) says "Phase C is DONE, not waiting on data quality" — so the deck contradicts itself. Change to "Phase B closed by DECISION, not by passing — four attempts, none meeting every criterion we set in advance (H2E scores 3/9)". Note the same bullet list's "+0.126 vs +0.026 KGE over c |
| 133 | `README.md:135` | calibration not started / Blocked / Not started | \| 2 \| Hydrological calibration on IDEAM discharge \| Mechanism understood (notebook 03); calibration not started \|  ... \| 3 \| Sediment calibration ... \| **Blocked**: no mainstem sediment data → tributary-calibration plan sent to advisor (Path A/B) \|  .. | README.md carries NO staleness banner and is dated 2026-07-27 (line 5); its phase table (lines 131-137) is stale on rows 2, 3 and 4. Rewrite: row 2 "**Complete** — Phase B CLOSED on H2E, F = 0.25931, at the r ≈ 0.57 rainfall-input ceiling"; row 3 "**Complete (exploratory)** — C4.3 ran and railed; fit not adopted (docs/55)"; row 4 "**Complete** — the modelled ENSO contrast reproduces the observed one, 18/18 stations,  |

*Sweep summary:* Swept the whole repo (docs/, docs/agents/, src/, src/nbgen/, scripts/, notebooks/, tests/, root .md/.html) for stale test counts and stale phase status. 62 hits classified: 49 CURRENT_CLAIM defects, 5 INSIDE_SUPERSEDED_BLOCK, 1 QUOTED_AS_RETIRED, 2 KILL_LIST_DEFINITION, 1 UNRELATED_COINCIDENCE (plus consolidated pointers to ~20 further correctly-governed sites named inside those entries).  THE SHAPE OF THE PROBLEM: the repo's status layer froze on 2026-08-12, the day the C4.3 block was reaffirmed. Everything written since — docs/55 (C4.3 RAN, RAILED/EXPLORATORY), docs/56 (C5 COMPLETE, contrast reproduced 18/18, median 3.05x), 57, 58, 59, and the 154-test suite — is absent from every status surface. The result is that the project's headline result is invisible in exactly the files a newcomer reads first, while a retired block reads as live.  HIGHEST IMPACT, in order: 1. CLAUDE.md:82 and :115 — the always-loaded instruction file says "C4.3 ... is BLOCKED" and "C5 not started". Every session starts from this. 2. progress_map.html — RULE 0 names it THE live tracker; it is stale in 8 places (banner, updated string, side panel, footer, the C4.3 node, the C5 node and its 4 children, two "140 passed" sites, "next free document number is 54"). 3. docs/00_INDEX.md:35 and :175 — the project statement paragraph and the "May C4 start?" WHERE-IS-IT row, both present the block as in force. 4. docs/31:29/30/428/507/603 — CLAUDE.md explicitly sends sessions to "check the stage-status pointer 

## 4 - What each sweep reports about its own coverage

Recorded so the sweep's coverage is auditable rather than implied:

- **uncited-bands** - Swept the whole repo for the uncited-band and mislabel family. Two of the four terms are fully closed; two carry live defects.  CLOSED, verified on disk — no action: - **min(m,0.5) mislabelled "Buarque eq. 14"**: zero current claims. All five registered sites are corrected (docs/35 §9.4.1, docs/37 A3.3.2, docs/43 Amendment 2, make_nb18.py:1404, make_nb19.py:2608), src/mgb_sediment.py:261-262 states the cap "may never be graded CITED", and docs/agents/journal_sweep-phasec.md §B.1 plus docs/54 §T2b both record the defect as closed. docs/37:1533 and docs/46:178 retain the string only as verbatim 
- **ratios-bands** - Swept docs/, docs/agents/, src/, src/nbgen/, scripts/, notebooks/, tests/, root *.md, progress_map.html and presentation_guide.html for the retired ratios and bands (2.37 / 3.00x / x0.333 / x0.421 / alpha ref 3.9-5.0 / band 2.0-9.9 / hard stop 11.8-14.9 / SE 0.1644 / +/-38 % Pi band / sigma_r 0.465), searching both hyphen and en-dash and both x and multiplication-sign forms.  18 CURRENT_CLAIM defects in 6 files, clustering into four independent problems:  (1) NOTEBOOK 18's SECTION 7.2 EQUIFINALITY FIGURE - the biggest one, and self-contradicting. src/nbgen/make_nb18.py:2900, 2904, 2919, 2926, 
- **loads-kmin** - Swept the whole repo for the retired proxy loads (104.8 / 82.8 / 126.1 / 126.2 / 99.7 / 99.8 Mt/yr), the retired k_min constants (0.00216 / 0.0209 / 0.0104 /km), "2.12x over 348.4 km", and every appearance of 248.7298/248.730/248.73 and 299.5387/299.539. The numbered docs 35, 42, 43, 45, 46, 47, 48, 50, 51 are in very good shape: every proxy load and every retired k_min there is either struck through, sitting inside a dated [WARN] AMENDMENT block, or quoted explicitly as the "registered" column of a registered-vs-measured table. Sixteen CURRENT_CLAIM defects survive, in four clusters. (1) prog
- **canon-numbers** - Per-number tally (occurrences seen / verdict).  r ≈ 0.57: ~45 sites (CLAUDE.md, docs/16,18,21,22,24,25,31,33,36,38,45,55,56,58,59, PROGRESS, progress_map, scripts/build_deck+build_report_pdf+generate_report_figures, nb10/13/14/15/18) — ALL AGREE; the El Niño per-config band 0.556–0.572 and CAL r 0.5564 are different statistics and are labelled as such, and docs/33:430-436 already journals the brief's 'CAL r = 0.518' vs the on-disk 0.5564. \|\| H2E F = 0.25931 / 0.25930593639066796: ~75 sites — ALL AGREE, to 1e-8 everywhere; docs/39:81 already audits this and marks it ✔. \|\| f_LS = 0.25146: ~3
- **embargo-convention** - EMBARGO ITSELF: CLEAN. I found no gauge-referenced t/km²/yr anywhere in the repo. Of ~130 hits on the yield-unit terms, the overwhelming majority are (a) affirmations OF the embargo, (b) model-internal specific erosion explicitly labelled as such, or (c) attributed published yields (Restrepo 2015 550/690/710; Latrubesse & Restrepo 2014 1,485; Restrepo et al. 2006 128-2,200/690; Tan et al. 2024 23.7-26.5 and 1.3-16.9 t/ha/a). Enforcement is real and mechanical: src/mgb_sediment.py:313 and src/mgb_transport.py:179/:843 state that nothing in those modules divides by an area; tests/test_sediment.p
- **stale-status** - Swept the whole repo (docs/, docs/agents/, src/, src/nbgen/, scripts/, notebooks/, tests/, root .md/.html) for stale test counts and stale phase status. 62 hits classified: 49 CURRENT_CLAIM defects, 5 INSIDE_SUPERSEDED_BLOCK, 1 QUOTED_AS_RETIRED, 2 KILL_LIST_DEFINITION, 1 UNRELATED_COINCIDENCE (plus consolidated pointers to ~20 further correctly-governed sites named inside those entries).  THE SHAPE OF THE PROBLEM: the repo's status layer froze on 2026-08-12, the day the C4.3 block was reaffirmed. Everything written since — docs/55 (C4.3 RAN, RAILED/EXPLORATORY), docs/56 (C5 COMPLETE, contrast
