# Adversarial audit of docs/31 (and docs/30) — 2026-08-10

Read-only review under the docs/31 "Review protocol": every suspected fault was classified
pointer-resolved / known-open / genuine **before** reporting; every citation below was
opened, and every number marked *recomputed* was recomputed from the primary artifact
(`_calib_cache/dds_*.npz` decoded with `calib_v2`'s own `inv`/`IS_LOG` transform — never
guessed — plus the shipped CSVs/npz). Decode script preserved in the session scratchpad;
all 14 archives are self-consistent (`f` = `arch_f[argmax]`, `x` = `arch_x[argmax]`).

Findings are ordered by expected damage to an executing session, which places the silent
misdirections above the one loud blocker (a session hits Finding 5 and knows immediately;
Findings 1–4 mislead without warning).

---

## 1 — Category-3 findings

### F1 — §0's "79, mapped to minibacias" is false against the file it cites; the task that would make it true is scheduled nowhere

**Severity: DISTORTS-A-RESULT** (phase-wide: C1, C2, C4 plan against a network ~3× its real size).

Clashing statements:

> docs/31 §0: "| SSC stations | 79, mapped to minibacias, `calibration_safe` is
> **geometry-only** (no SSC-quality gate) | `sediment_inventory.csv`, docs/19 |"

> docs/30 §2: "| SSC inventory | 79 stations, gauge→minibacia mapping done,
> `calibration_safe` flag exists (geometry-only — see gap below) |"

versus the cited source, measured (`data/processed/sediment_inventory.csv`, 79 rows):
**`minibacia` non-null = 28; `lat` non-null = 33; `mapping_action` = 51 NaN / 27 kept /
1 remapped; `calibration_safe` = 24** — exactly the state docs/19 records:

> docs/19 §3.7 (heading): "Only 28 of 79 sediment stations can be mapped; 46 have no
> coordinates at all — **CONFIRMED**"

> docs/19 §5.2 item 2: "**Coordinates + drainage areas for the 46 unmapped sediment
> stations** — extend `src/fetch_station_coords.py` to pull the IDEAM catalogue coordinate
> **and catalogue drainage area**, then re-snap by **drainage-area matching**. […] **Until
> this is done the sediment network is 24 stations, not 79.**"

`git log --all -- src/fetch_station_coords.py` shows a single commit (`b4a1230`); the
extension was never built. No C-stage subtask and no background item schedules it (B3
acquires external areas as a *discharge-network arbiter*, "unblocks yields only" — it never
mentions the 46 unmapped SSC stations). C1.6's own excluded-class evidence list — "(no
window coverage / no plausible rating / corrupt record)" — does not even contain
"no coordinates", confirming the plan was not written with the 46 dark stations in mind.

Why it misdirects: C1.6 gates "79/79 classified; the mainstem-vs-tributary split stated
(C4 needs the tributary list)"; C2.2 computes flux "per usable station"; C4.2 calibrates
on "the C1-usable tributary set". A session planning coverage against "79 mapped"
discovers mid-stage that the mappable ceiling is 28 (24 calibration-safe, 33 with a
discharge pairing), and nothing in the plan ever lights up the other 46.

Smallest fix: correct both table rows to "79 stations; 28 mapped / 33 with coordinates /
24 calibration-safe; 46 unmapped pending the docs/19 §5.2-item-2 coordinate+area fetch",
and either add that fetch as a subtask (C1.0, or background B5) or record the explicit
decision to run Phase C on the mapped subset.

### F2 — Phase C starts with docs/19 §5.2's decision items 1–2 neither taken nor recorded, against docs/21's explicit precondition

**Severity: DISTORTS-A-RESULT** (the project's central design choice is undefended in the
active plan; C5's write-up inherits it).

Clashing statements:

> docs/21 (opening paragraph): "Phase C (sediment) is unblocked as a data problem and
> bounded as a science problem (doc 19 §3.9); **it must not start before the doc 19 §5.2
> decisions are taken**."

> docs/19 §5.2 item 1: "**Decide the ENSO pairing, on the §3.8 numbers.** The locked
> 2011-vs-2015-16 contrast has **6 bridging stations** and **no mainstem anchor on the El
> Niño side**. 1997-98 vs 1999-2000 has **10 bridging stations** and ~260–293 mainstem days
> on *both* sides. This is a design decision with a data cost either way and **it must be
> taken explicitly, not inherited**"

> docs/19 §3.8 (heading): "ENSO calibration design: the 2011-vs-2015-16 contrast is the
> **weakest** of the four candidate pairings — CONFIRMED, and it is the reason a second
> ENSO cycle matters"

versus docs/30 §1, which records one decision only — "**Phase B closes on the
input-ceiling result, with H2E as the adopted configuration**" (the docs/24-item-17 scope
question) — and docs/31, which pre-registers *window boundaries within* the locked pairing
(C2.1) but never the pairing itself. Searched both docs in full: "bridging" absent, "1997"
absent, "Arrancaplumas" absent, the one-sided-mainstem constraint absent.

Coverage of the other §5.2 items, for completeness: item 4 (scripted masks) is absorbed by
C1.3's `sediment_daily_qc.csv`; item 5 (performance bar) by §0 + C4.2; item 3's forward
half is done (the v2 cells score through 2018-12-31, `calib_v2.CELLS`); items 1–2 are the
holes. This finding is **not** register #4 (stale "blocked on mainstem SSC" prose): it is
about two concrete §5.2 decisions that are unexecuted/unrecorded in the successor docs.

Why it misdirects: C2/C5 build the headline ENSO contrast on a pairing whose recorded
evidence calls it the weakest of four candidates with 6 bridging stations; the C5.4
write-up and any reviewer who opens docs/19 hit an undefended choice. C1.1's census will
re-measure coverage but cannot substitute for the recorded decision docs/19 demands.

Smallest fix: one recorded-decision paragraph in docs/30 §1 (or docs/31 §0): pairing kept
at 2011 vs 2015-16 because the forcing and gauge network span 2008–2018 (ERA5 P∩PET bound;
CLAUDE.md) and re-acquisition back to 1997 is out of scope; costs accepted and named (6
bridging stations, no El Niño-side mainstem anchor, 2–4-station 2016-17 per docs/19 §3.9b);
C1.1/C1.6 restate the measured counts.

### F3 — B1 prescribes a CHIRPS refit that neither primary record recommends, then closes the question permanently on its outcome

**Severity: DISTORTS-A-RESULT** (a bounded track, but its stopping rule would close the
CHIRPS question without the recorded fix ever being tested).

Clashing statements:

> docs/31 B1: "Refit the per-(elevation band × hydrographic zone) quantile maps **on the
> repaired series including `Inferido_seco` inferred-dry days** (the identified cause of
> the volume failure, docs/18 §15). […] Any gate fails → the negative result closes the
> CHIRPS question permanently"

> docs/18 §15.3: "**finishing the zero-suppression repair on the 139 residual stations is
> upstream of any usable CHIRPS merge**"; §15.4: "not until its volume can be held —
> either by **conditioning the quantile maps on inferred-complete records only**, or by
> **repairing the remaining rain-selective stations first**."

> docs/agents/journal_chirps-merge.md (Follow-up, the run's own record): "If the merge is
> retried: hold volume by fitting quantile maps **only on stations that pass the
> selectivity test (~1.00)**, or finish the zero-suppression repair on the **139 residual
> rain-selective stations** (doc 18 s9.3) FIRST - that repair is upstream."

(The error propagates from docs/21's Addendum, which uses B1's phrasing; docs/18 §15 and
the run journal — the primary records — agree with each other and not with it.)

Why it misdirects: per §15.3 the volume bias is carried by the 139 stations that are still
rain-selective *after* the repair — stations for which no `Inferido_seco` days exist to
include. Including inferred-dry days only dries the fit at already-repaired stations; the
recorded options remove (per-station conditioning) or repair the biased stations. B1 as
written is therefore expected to fail the volume gate again, and B1's own stopping rule
then closes the question permanently on a mis-specified attempt.

Smallest fix: reword B1's refit to the recorded option — "fit the quantile maps only on
stations passing the selectivity test (~1.00) (docs/18 §15.4; journal follow-up)" — and
demote "the identified cause" to "the identified mechanism: 139 residual rain-selective
stations transferred through reporting-day-conditioned maps (docs/18 §15.3)".

### F4 — C1.2's Gate contradicts its own method paragraph on what calibrates the selectivity null

**Severity: DISTORTS-A-RESULT** (a mis-calibrated null is exactly the circularity the
method paragraph exists to prevent; biased stations would pass).

Clashing statements, both inside docs/31 C1.2:

> method: "density alone does NOT define 'unbiased', because a densely-sampled station can
> still be flow-chasing (campaigns sent out when the river is high). The null pool is
> stations whose sampling dates are **calendar-regular** […] calendar-driven sampling is
> unbiased with respect to flow BY CONSTRUCTION, **whatever its density**."

> gate: "**Gate:** the null is calibrated (**dense stations ≈ 0.5**) before any station is
> flagged."

Why it misdirects: the Gate is the line a session checks off. Calibrating the null on
dense stations admits flow-chasing stations into the null pool, inflates its p99, and
lets genuinely biased stations through — the precise failure the paragraph above forbids.

Smallest fix: one word — the gate reads "(calendar-regular stations ≈ 0.5)".

### F5 — C3.5's cross-check input (`musle.py`) exists nowhere on disk and the plan records no way to obtain it

**Severity: BLOCKS-A-STAGE** (narrowly: subtask C3.5's gate cannot be executed as written;
the rest of C3 proceeds).

The choice, quoted:

> docs/30 §2: "| Team's second implementation | `musle.py`, `sediment.py`, RS-SSC
> retrieval — an independent cross-check, as in Phase B |"
> docs/31 C3.5: "Same sub-basin, same inputs, our module vs the team's `musle.py`"

Where I searched for it: `find c:/dev -iname "musle*"` (only this repo exists under
c:/dev; the sole hit is our own `src/build_sediment_gauges.py` on the `*sediment*.py`
pattern); grep of all docs for a path/URL — none recorded anywhere. The repo itself
documents the dependency as external:

> docs/20:43: "The `yb_*.png` figures come from the **team's second-implementation repo —
> the one input not rebuildable from this repo alone**"

Why it misdirects: docs/30 §2 lists implementation B as an asset in hand; a session
reaching C3.5 has no file, no path, and no acquisition step, and stalls on a gate-bearing
subtask.

Smallest fix: add to C3.5's **In** an acquisition line ("request implementation B's
`musle.py`/`sediment.py` from the team; record the drop location in docs"), or re-scope
C3.5 to compare against implementation B's published *numbers*, as docs/23 §13 did for
areas.

### F6 — C3.3 claims slope is "already in topology.npz"; it is not, nor in any shipped artifact

**Severity: COSMETIC** (option 2 is one of three; slope is derivable — but the
pre-registration would be made on a false premise about inputs in hand).

> docs/31 C3.3 option 2: "SCS-triangular: `qpeak = f(Qsur, t_c)` with time of
> concentration from reach length/slope (**already in `topology.npz`**)"

Measured: `model_inputs_v2/topology.npz` keys = centroid_lat/lon, downstream_id/idx,
hops_to_outlet, minibacia_id, n_upstream_links, outlet_idx, own_area_km2,
path_km_to_outlet, **reach_km**, topo_order_idx, upstream_area_km2 — **no slope**; also
absent from `parameters.npz`, `minibacia_soil_params.csv`, `minibacias.csv`. nb07 computes
slope transiently in the DEM chain but ships no per-reach/per-minibacia slope artifact.

Smallest fix: "reach length in `topology.npz`; slope to be derived from the nb07 DEM chain
(not yet a shipped artifact)".

### F7 — Four pointer failures, in a plan whose protocol makes pointers carry the justification

**Severity: COSMETIC** (each number is real somewhere; each pointer sends the verifier to
the wrong place).

(a) §0 dry-phase row sources "field LOOCV skill 0.429" to **docs/22 §4.7** — that section
carries the *per-window* LOO IDW skill, "r = 0.40 in the El Niño window against 0.45 in
La Niña"; the 0.429 is the all-period gauge-only LOOCV median over 287 gauges and lives in
docs/18 §12 (line 696), docs/26 §7, docs/21 §3. Two different statistics; a session
conflating them would misquote the El Niño-window field skill (0.40, not 0.429).

(b) §0 rating-curves row: "per-pair list in **docs/13**" (and C1.5's "(docs/13)") —
docs/13 contains **no R² values at all** (verified: no `R²`/`R2`/`median` in the file; it
is the pairing-candidates doc). The per-pair list is `data/processed/rating_curves.csv`
(33 pairs, columns code/name/n_pairs/a/b/R2). The *number* is right: median R² = **0.54**.

(c) C2.1: "the window definition was an open advisor question (**docs/21 open items:
'2011 vs 2010–2012'**)" — docs/21 §4's twelve-item table contains no window item (read in
full; grepped for 2010/2011/window). The question exists at **docs/28:258**: "Likely
question: *'Why not 2010–2012 for La Niña, since the event spanned it?'* > An open
question we flag for the advisor." Register #6's "where flagged: docs/21" inherits the
same wrong pointer.

(d) §0 literature-anchor row: "verify the exact figure and citation in **C2.5** before
quoting" — C2.5 is the commit step; the verification subtask is **C2.4** (register #5
cites C2.4 correctly).

Smallest fix: repoint (a)→docs/18 §12 / docs/26 §7, (b)→`rating_curves.csv` (nb06),
(c)→docs/28 slide-2 QA, (d)→C2.4.

### F8 — docs/30's C2 deliverable list invites exactly the ratio docs/31 C2.1 forbids

**Severity: COSMETIC** (executing sessions work from docs/31 by design; the parent doc
alone produces the forbidden statistic).

> docs/30 §3 C2: "compute observed suspended-sediment flux (concentration × same-day
> discharge) for La Niña 2011 vs El Niño 2015–16: **totals, ratios**, seasonal shape, at
> every usable station."

> docs/31 C2.1: "Window totals (t) may be reported per window for context but **NEVER as a
> wet:dry ratio**; a ratio of unequal-window totals is meaningless by construction."

Smallest fix: docs/30's line → "totals (context only), rate ratios per docs/31 C2.1".

### F9 — C1.3's "744 mg/L peak was real" has no repo anchor and no verify-marker

**Severity: COSMETIC.**

> docs/31 C1.3: "corroborate before deleting (the source paper's own rule — its 744 mg/L
> peak was real)"

grep for `744` across docs: only docs/31:139 itself (plus an unrelated "744 timesteps" in
docs/16). Unlike the Restrepo anchor — which §0 marks "to confirm" and register #5 tracks
— this literature figure is asserted as fact with no flag. Smallest fix: give it the same
verify-marker, or cite the paper section when C2.4 fetches the source anyway.

---

## 2 — Cleared items (what was checked, where, and what came back)

1. **docs/29 rule-(a) arithmetic — recomputed from all 14 `dds_*.npz`.** Every per-run
   best F, kc_mult, and recession median matches the read-out table to publication
   precision (e.g. H2E 20260901: F 0.259306, kc 1.6625, rec 1.0821). H1 mean 0.234435 /
   spread 0.050816; H2 mean 0.243577 / spread 0.022981; gap 0.009141 < 0.05082 →
   **NOT SEPARATED confirmed**. (The doc prints "gap 0.00915" — that is 0.24358 − 0.23443
   on the *rounded* means; full precision gives 0.00914. Verdict unaffected.) "Best run
   overall is an H1 seed" ✓ (H1 20260906, 0.260852); "every H1/H2 seed sits ≥ 1.896" ✓
   (min 1.8957); "H1 k_bas spans 43–394 d" ✓ (42.57–393.80 d).
2. **docs/29 rule-(b) — all three conditions recomputed and PASS.** kc 1.6625 (77.5 % of
   the raw (0.5, 2.0) range) and 1.8361 (89.1 %), both < 1.85/90 % ✓. Fleet-median
   recession ratio at the best evaluation — `arch_ks[argmax(arch_f)]` ÷ `K_OBS_CAL`
   rebuilt exactly as `Cell.__init__` builds it (obs side only: `model_inputs_v2/
   discharge.npz`, calibration-safe gauges, CAL 2012–14, `calib_v2.recession_fleet`) —
   = 1.0821 / 1.1100 ∈ [1/1.5, 1.5] ✓. |mean F_H2E − mean F_H2| = |0.253009 − 0.243577| =
   0.009432 ≤ 0.01 ✓ (per-bundle gauge sets differ, 61 vs 63 — handled per cell spec).
3. **Flux constant 0.0864** ✓: mg/L ≡ g/m³; ×86,400 s/day ÷ 10⁶ g/t = 0.0864 t/day per
   (m³/s · mg/L). Premise holds: the shipped `ssc_*_mg_l` columns are truly mg/L — docs/19
   §3.2/§6 record the CM Kg/m³→mg/L ×1000 conversion applied in `build_sediment_gauges`.
4. **§0 SSC-data row vs the file** ✓ measured: 269,337 rows; 1979-01-01→2018-12-31;
   columns code, date, ssc_mean_mg_l, ssc_surface_mg_l, approval, flag_corrupt, flag_zero,
   flag_flatline (+flatline_run_len). (The *count/columns* row is exact; the "mapped"
   claim in the neighbouring row is F1.)
5. **§0 MUSLE-K row** ✓: `minibacia_soil_params.csv` has `K` over 8,672 rows; nb09 states
   the t·ha·h unit and the Wischmeier method; `model_inputs_v2/parameters.npz` already
   carries `K_musle` (a bonus the plan doesn't mention).
6. **§0 areas row vs docs/23 §13.2** ✓ verbatim: "85 gauges", "beyond 2× | 31 of 85
   (36 %)"; B3's "31 disputed gauges" consistent.
7. **§0 dry-phase numbers vs docs/22 §4.7** ✓: "all 12 parameter configurations",
   "0.556 – 0.572"; anomaly r 0.476 ✓; inter-gauge 0.33 at 0–25 km ✓ (docs/21 §3 restates
   both). The 0.429 pointer is F7(a); the number itself is real (docs/18 §12: 287 gauges).
8. **§0 CHIRPS row vs docs/18 §15.1** ✓: pre-registered two-gate rule quoted; LOOCV 0.447
   > 0.429 PASS; volume 2,188.5 vs 2,036.4 ±1 % FAIL (recomputed: +7.47 % → "+7.5 %") ✓;
   "DO NOT ADOPT" ✓. B1's gate constants (1 %, 2,036.4 mm/yr 2009–17; r > 0.429) match.
   The *fix attribution* is F3.
9. **"Fagundes et al. 2026" and the −0.26…0.44 band** — consistent everywhere in-repo:
   docs/19 §3.9c ("Fagundes et al. (2026) report SSC KGE from −0.26 to 0.44 … 25 years of
   calibration"), docs/19 §6, docs/24:22, docs/14:63. Not externally checkable from here;
   internally coherent, and C4.2/docs/30 §4 quote it consistently.
10. **C0.1** ✓: `parameters_H1.csv` header is exactly `parameter,scope,value,prior,lo,hi,
    pos,railed`; gate values verified from the archive (kc 1.6625; k_int_frac 0.020142 at
    0.19 % of its log-range — railed=YES under nb14's own convention).
11. **C0.2** ✓: `Cell('H2E')` exists in `calib_v2.CELLS` (`et_stress='fao56'`,
    `theta_crit` 0.6 fixed, reads H2's cache); the 9.1e-9 precedent is real — docs/22:27
    ("median relative error of 9.1×10⁻⁹"), docs/20:114. Note the precedent was measured on
    q_sim reproduction; C0.2 transfers the same tightness to F — coherent, and stated as
    its own gate.
12. **C0.3** ✓: `metrics_fleet.csv` columns match the listed set exactly (kge, nse, r,
    alpha, beta, pbias, kge_gt0, kge_gt05, n, clim_kge, skill_over_clim, rec_ratio keyed
    by cell/config/period) and its period labels are exactly the six listed —
    `calib_v2.Cell.PERIODS` agrees. The engine implements fao56/theta_crit
    (`mgb_hydrology.py` docstring + validation, lines ~49–469). The <1e-15 mass-balance
    gate has measured precedent (docs/20:112: "measured 1.67e-17"). `rec_ratio` is a
    per-(cell, config, period) fleet statistic, so "per-period rec_ratio ≤ 1.5× everywhere"
    reads coherently as *every period row*, not per gauge.
13. **C0.5** ✓: (3652, 8672) is exactly 2009–2018 days × minibacia count;
    `data/processed/*` is gitignored (.gitignore:3) so "gitignored" holds. (Size note:
    three raw float32 fields are ~380 MB; "~250 MB" is plausible only as
    `savez_compressed` output — not checkable pre-hoc, flagged here rather than as a
    finding.)
14. **C1.3's corrected nulls vs docs/19 §3.4/§6** ✓ verbatim: within-year 0.030 % /
    within-14-day 0.234 % / flawed published 0.00037 %.
15. **C1.4 vs docs/17** ✓: the SNHT break list exists — §3.8: "24 strong discharge break
    candidates, 12 inside 2009–2017" (attribution uncertain, which era-segmentation
    tolerates: a false break splits an era, it does not pool across a real one).
16. **C1.5's expectation "median fleet R² ≈ 0.5"** ✓ against `rating_curves.csv`: median
    R² = 0.54 over 33 pairs (pointer to docs/13 is F7(b)).
17. **C2.1 comparability rule respected within docs/31** ✓: C2.2 computes the wet:dry
    ratio "on rates per the C2.1 comparability rule"; C5.2's "magnitude ratio" is
    sim-vs-obs within-window; the risk register carries no totals ratio. docs/30's
    phrasing is F8.
18. **C4.1/C4.2 grounding** ✓: docs/22 §4.6:196 ("celerity … a floodplain-storage
    surrogate for the Mompós reach"); docs/24:62 ("Muskingum X = 0"); CAL 2012–14 =
    `calib_v2.CAL_YEARS`; Klemeš out-of-sample design matches docs/24:78; the spin-up
    clarification is the same warm-up≠scored distinction docs/26 used for 2008.
19. **k_bas lower bound 5 d** ✓: `calib_v2.RAW_LO` k_bas = 5.0; module docstring records
    "lower bound 15 d → 5 d" with the observed 13.9 d fleet-median justification; docs/21
    attempt table "k_bas ≥ 5 d" agrees. (CLAUDE.md's compressed "k_bas bound below 15 d"
    is reconcilable — the bound moved below 15 — but is the phrase most likely to be
    misread; not a docs/31 fault.)
20. **B2's probe framing** ✓: the 0.02 floor is real (`RAW_LO`), the probe's config/seed/
    budget are well-formed; the "7 of 8" count nuance is in register item #3 below.
21. **Register #4 spot-confirmed** (docs/25:21 still reads "blocked on mainstem SSC
    quality; a separate phase") — known-open, not a finding.
22. **C0.4's premise** ✓: docs/24's attempt table (lines 90–94) indeed stops at attempt 3.
23. **Sensitivity windows** (C2.1: 2010-07→2011-06; 2015-10→2016-04) — consistent with
    docs/07's event description (strong 2010–2011 La Niña; 2011 the solid La Niña year);
    the unequal lengths are exactly what the rates-only rule exists for.

## 3 — Register items touched

- **#1 — RESOLVED, with evidence.** `parameters_H2.csv` (nb14's own adjudication) marks
  railed=YES on exactly three dims: `k_sup@global` (pos 0.99794), `k_int_frac@global`
  (pos 0.00942), and **regional `wm_mult@R2`** (5.680 of (0.25, 6), pos 0.98277) — i.e.
  **3 railed dims of the 18-dim search vector, of which 2 are among the 10 globals**.
  `parameters_H1.csv` marks exactly two, both global (`kc_mult` 0.98783, `lai_mult`
  0.00531) — no regional rail, which is why the two conventions agree on "2" for attempt 2
  and diverge only on attempt 3. So docs/26 §5's "F1 **0 of 10** railed → FAIL 2" is
  correct under its stated global-only denominator (its §5.1 names exactly k_sup and
  k_int_frac), and docs/24 slide 8's "3" is the all-dims count — wrong only under the
  "of 10" reading its own Config-B row ("3 of 10") establishes. One underlying vector,
  two denominators; nothing needs re-measuring. Smallest fix: docs/24 row 3 → "2 of 10
  global (+1 regional `wm_mult@R2`)", and docs/21:28-30's "unresolved one-count
  discrepancy" note can now close.
- **#3 — refined with the decoded values.** k_int_frac across the 8 v2-forcing seeds:
  H2 = 0.020731, 0.028141, 0.020502, 0.021171, 0.021289, 0.837625; H2E = 0.020142,
  0.020643. "Sits at the 0.02 floor in 7 of 8" holds under a raw-range criterion
  (< 1 % of raw range ⇔ < 0.0288) — the convention nb14's `pos` column uses for rails is
  transformed-space, under which the strict count is **6 of 8** (H2 20260902 at 0.0281
  sits 9 % up the log-range; H2 20260906 is near the *opposite* bound at 0.838). B2's
  motivation is unaffected either way; docs/29's wording could carry the threshold. Also:
  H1 has two near-floor seeds of its own (0.020435, 0.025044), so floor-crowding is not
  exclusive to the v2 forcing.
- **#5 — assisted, not resolved.** In-repo, docs/06:9 already records "Mean annual
  suspended sediment load **~145–169 Mt/yr**" at Calamar with the reference titles
  (Restrepo & Kjerfve, interannual variability 1975–1995 / revised estimates; Restrepo et
  al., 1980–2010 trends; Higgins et al.) — the plan's "~140–180" neither matches nor cites
  it. External lead from a web search (search-result level, paper not fetched — C2.4 still
  owns verification): Restrepo & Kjerfve (2000, J. Hydrology) estimate **144 Mt/yr** at
  Calamar (1975–1995), with ENSO-correlated interannual variability (La Niña raises, El
  Niño lowers — directly relevant to C2/C5). C2.4 should reconcile against docs/06 first.
- **#6 — pointer corrected.** The window question is real and was flagged *for the
  advisor*, but in docs/28:258 (slide-2 QA), not in docs/21's open items (searched in
  full). See F7(c). The bracketing mitigation itself is sound and stands on its own.
- **#2, #7 — confirmed as stated, nothing new**: kc 1.662/1.836 measured off-rail but
  above the ≤1.2 plausibility bar (docs/26 F4; docs/25 definition of done); H2E adopted
  from n=2 seeds exactly as its pre-registration allowed (docs/29 §3b, caveats).
- **#4 — confirmed** (docs/25:21 sample); resolving it is an edit pass, out of scope here.

## 4 — Verdict

The plan's quantitative core is trustworthy: every number feeding C0 reproduces exactly
from the primary archives (all 14 seeds, both decision rules, the schemas, the period
labels, the engine options), the pre-registration discipline is coherent, and the risk
register's mitigations are real — **C0 is safe to execute as written**. The plan is *not*
yet safe on the observational side: its ground-truth table overstates the mapped SSC
network 79-vs-28 against the very file it cites, the two docs/19 §5.2 decisions that
docs/21 makes preconditions (ENSO pairing; the 46-station coordinate fetch) are neither
taken nor scheduled, C1.2's gate contradicts its own null definition, and B1 would spend
its bounded budget on a refit the primary records don't recommend and then close the
question permanently. **Fix C1 first** — correct the §0 network row, add/decline the
coordinate-fetch task explicitly, and repair the one-word gate in C1.2 — then record the
pairing decision in docs/30 §1 and re-spec B1's refit; all five fixes are edits of a few
lines each, and nothing found here invalidates a completed result.
