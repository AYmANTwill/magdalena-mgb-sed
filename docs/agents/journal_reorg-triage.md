# journal - reorg-triage: the full inventory and its disposition

**Written 2026-08-19 by the repository-reorganization pass** (`docs/agents/PROMPT_repo_reorganization.md`, TASK 1). Seven read-only agents catalogued one subtree each; 466 entries. **This document decides nothing on its own - every `DELETE` below is held until the owner approves it**, per the prompt's instruction to present the triage and pause for human review before executing any DELETE.

**Disposition key.** `KEEP` = live: part of the current narrative, the pipeline, or the reproduction chain. `ARCHIVE` = historical record with audit value but not part of the live narrative - **moved to `docs/archive/`, never deleted**, with a one-line header naming what superseded it. `DELETE` = genuine dead weight only: byte-caches, build intermediates, stale-duplicate renders, duplicates whose information is recoverable. **When unsure, ARCHIVE.**

## 1 - Totals

| subtree | entries | KEEP | ARCHIVE | DELETE |
|---|--:|--:|--:|--:|
| docs-numbered | 65 | 48 | 17 | 0 |
| scripts | 29 | 21 | 1 | 7 |
| root | 36 | 20 | 1 | 15 |
| notebooks-figures-tests | 49 | 44 | 1 | 4 |
| src | 44 | 39 | 2 | 3 |
| docs/agents | 157 | 21 | 136 | 0 |
| data | 86 | 72 | 11 | 3 |
| **TOTAL** | **466** | **265** | **169** | **32** |

## 2 - The DELETE list, in full, each with its justification and what regenerates it

> **Nothing here is deleted yet.** Every entry was checked to be either a machine cache, a build intermediate, or a duplicate whose information survives elsewhere. Three entries carry a caveat and are called out under the table.

| # | path | why it is dead weight | regenerated by |
|--:|---|---|---|
| 1 | `scripts/c3/__pycache__/ls2d.cpython-310.pyc` | Python bytecode cache, already matched by .gitignore's `__pycache__/` and untracked - pure build residue. | python3.10 on next import of scripts/c3/ls2d.py |
| 2 | `scripts/c3/__pycache__/ls2d_defect_b.cpython-310.pyc` | Python bytecode cache, gitignored and untracked - pure build residue. | python3.10 on next import of scripts/c3/ls2d_defect_b.py |
| 3 | `scripts/c3/__pycache__/ls2d_variants.cpython-310.pyc` | Python bytecode cache, gitignored and untracked - pure build residue. | python3.10 on next import of scripts/c3/ls2d_variants.py |
| 4 | `scripts/c3/__pycache__/ls_erosion_weights.cpython-310.pyc` | Python bytecode cache, gitignored and untracked - pure build residue. | python3.10 on next import of scripts/c3/ls_erosion_weights.py |
| 5 | `scripts/c3/__pycache__/ls_stratified_report.cpython-310.pyc` | Python bytecode cache, gitignored and untracked - pure build residue. | python3.10 on next import of scripts/c3/ls_stratified_report.py |
| 6 | `scripts/c3/__pycache__/qpeak.cpython-310.pyc` | Python bytecode cache, gitignored and untracked - pure build residue. | python3.10 on next import of scripts/c3/qpeak.py (via src/mgb_sediment.py or tests/test_qpeak.py) |
| 7 | `scripts/c4/__pycache__/o5_calibration_profile.cpython-310.pyc` | Python bytecode cache, gitignored and untracked - pure build residue. | python3.10 on next import of scripts/c4/o5_calibration_profile.py |
| 8 | `MGB-SED_Magdalena_FIGURES_CORRECTED.pptx` | Stale one-off build under a non-canonical name (23 slides, 2026-08-14): zero references anywhere in the repo, no script writes this filename, gitignored and never tracked so nothing is lost. | python3.10 scripts/build_deck.py (`make deck`) — writes MGB-SED_Magdalena_FIGURES.pptx |
| 9 | `MGB-SED_Magdalena_ENSO_paperdepth.pptx` | Stale one-off build under a non-canonical name (28 slides, 2026-08-14, evidently a sandbox render of the current build_deck.py before it was committed): zero references anywhere in the repo, no script writes this filename, gitignored and never tracked. | python3.10 scripts/build_deck.py (`make deck`) — the current script builds the same 28-slide sequence, slides 2–28, and writes MGB-SED_Magdalena_FIGURES.pptx |
| 10 | `results/README.md` | Never-used placeholder stub (265 B, 2026-07-27) promising results/{hydrology,sediment,scenarios} that were never created — the actual outputs live in data/processed/ (c2, c3, c5_*), sim_baseline_v2/, sim_calibrated_v2/ and figures/. It is tracked, so git history retains it; deleting also makes .gitignore's results/*.tif and results/*.gtb dead lines. | nothing — plain placeholder; recoverable from git history if a reorganisation later wants results/ back as the deliverables destination |
| 11 | `_eq/bound.png` | Build intermediate: a mathtext-rendered equation PNG emitted by scripts/build_report_pdf.py's eq() into EQD, untracked AND not covered by .gitignore (so it pollutes git status). | scripts/build_report_pdf.py (function eq(), line 46) |
| 12 | `_eq/fls.png` | Build intermediate: equation PNG emitted by scripts/build_report_pdf.py, untracked and not gitignored. | scripts/build_report_pdf.py (function eq(), line 46) |
| 13 | `_eq/kge.png` | Build intermediate: equation PNG emitted by scripts/build_report_pdf.py, untracked and not gitignored. | scripts/build_report_pdf.py (function eq(), line 46) |
| 14 | `_eq/kgec.png` | Build intermediate: equation PNG emitted by scripts/build_report_pdf.py, untracked and not gitignored. | scripts/build_report_pdf.py (function eq(), line 46) |
| 15 | `_eq/kgemax.png` | Build intermediate: equation PNG emitted by scripts/build_report_pdf.py, untracked and not gitignored. | scripts/build_report_pdf.py (function eq(), line 46) |
| 16 | `_eq/musle.png` | Build intermediate: equation PNG emitted by scripts/build_report_pdf.py, untracked and not gitignored. | scripts/build_report_pdf.py (function eq(), line 46) |
| 17 | `_eq/ratio.png` | Build intermediate: equation PNG emitted by scripts/build_report_pdf.py, untracked and not gitignored. | scripts/build_report_pdf.py (function eq(), line 46) |
| 18 | `.pytest_cache/.gitignore` | pytest cache cruft; the directory self-ignores via this very file ('*'), and pytest's own README says do not version it. | python3.10 -m pytest (cache plugin) |
| 19 | `.pytest_cache/CACHEDIR.TAG` | pytest cache marker file, machine-local. | python3.10 -m pytest (cache plugin) |
| 20 | `.pytest_cache/README.md` | pytest's own boilerplate README that literally says 'Do not commit this to version control'. | python3.10 -m pytest (cache plugin) |
| 21 | `.pytest_cache/v/cache/nodeids` | Machine-local test-id cache; also STALE evidence — it still lists src/test_mgb_hydrology.py node ids from before the suite moved to tests/ and grew to 154 passing tests. | python3.10 -m pytest (cache plugin) |
| 22 | `.pytest_cache/v/cache/lastfailed` | Machine-local last-failed record naming tests/test_transport.py::test_the_partition_claim_does_not_survive_a_nan_run — contradicted by the canonical 154-passed state, so it is stale as well as untracked. | python3.10 -m pytest (cache plugin) |
| 23 | `notebooks/06_data_inventory.html` | Untracked, gitignored (.gitignore:16 notebooks/*.html) nbconvert export; NOT stale (54 cells match the .ipynb verbatim, exported 7 h after it) but nothing depends on it - docs/agents/journal_nbc1-nb06-inventory.md §1 already adjudicated it disposable; 1,248,822 B, mtime 2026-07-29 10:41:45. | python3.10 -m nbconvert --to html notebooks/06_data_inventory.ipynb (no automated producer exists in scripts/ or src/) |
| 24 | `figures/deck/[71 notebook-extracted PNGs: NN_cNNN_N.png for notebooks 01-14]` | Gitignored, fully regenerable, and STALE: all 71 carry mtime 2026-08-03 20:44, i.e. they predate the 2026-08-12/13 regeneration of notebooks 10-19 and contain NOTHING from notebooks 15-19 (the C1/C2/C2b/C3 work). | python scripts/extract_notebook_figures.py (documented in docs/20 §42) |
| 25 | `figures/deck/[20 gen_*.png charts]` | Gitignored derived charts (gen_attempts, gen_bfi, gen_c2_flux_timeseries, gen_c2_monthly_shape, gen_c2_ratio_dotplot, gen_c36_erosion_map, gen_c36_seasonal_cycle, gen_ceiling_bound, gen_enso_contrast, gen_h2_h1, gen_kge_rail, gen_kge_vs_r, gen_ls2d, gen_obs_contrast_detail, gen_peakgap, gen_peaks, gen_recession, gen_sensitivity_detail, gen_skill_clim, gen_ssc_coverage), mtimes 2026-08-10 to 2026-08-14, all rebuildable from committed results. | scripts/make_deck_charts.py (the 4 original gen_*), scripts/c2_consistency_figs.py, scripts/c2b/bfi_figure.py, scripts/c3/ls2d.py, scripts/generate_enso_figures.py (untracked) |
| 26 | `tests/__pycache__/ [10 .pyc files]` | Compiled bytecode caches (conftest, 7 test modules, plus duplicate plain/pytest-9.0.3 variants for test_sediment and test_transport); gitignored build residue, never a source of truth. | python3.10 -m pytest (CPython writes them automatically on import) |
| 27 | `src/README.md` | 335-byte July-2026 placeholder that is now false: it announces future files `preprocessing.py` / `urh.py` that were never written and claims "for now the reference implementations live in ../notebooks/" — src/ is today the frozen engine (mgb_hydrology/mgb_sediment/mgb_transport). Records no verdict; should be REWRITTEN in place as a module index rather than left as-is. | nothing — obsolete stub; replace with a written index of the 31 modules |
| 28 | `src/__pycache__` | Interpreter byte-cache, already gitignored (.gitignore `__pycache__/`). 13 files: baseflow, calib_v2, dhime_dates, forcing_npy, idw_forcing, merge_chirps_gauges, mgb_hydrology, mgb_sediment, mgb_transport, test_mgb_hydrology (pytest-9.0.3) .pyc, plus the numba on-disk cache pair mgb_hydrology._get_numba_router.locals._route-770.py310.1.nbc/.nbi. Records nothing. | CPython on next import; the .nbc/.nbi pair by numba's cache=True on first call to _get_numba_router |
| 29 | `src/nbgen/__pycache__` | Interpreter byte-cache, already gitignored. 2 files: make_nb18.cpython-310.pyc, make_nb19.cpython-310.pyc — an artefact of one generator importing another during a run, not project state. | CPython on next import |
| 30 | `data/processed/gauge_minibacia_ORIG_backup.csv` | Verified byte-identical to gauge_minibacia.csv except the `minibacia` column, whose pre-remap value is recorded per station in gauge_minibacia_remap_report.csv:original_minibacia - no information is lost. | data/processed/gauge_minibacia_remap_report.csv (original_minibacia column) joined onto gauge_minibacia.csv; the remap itself by src/fix_gauge_minibacia_mapping.py |
| 31 | `data/processed/soils_magdalena_merged_4326.gpkg-journal.DISABLED` | 512-byte orphaned SQLite/GeoPackage rollback journal, renamed to neutralise it after an interrupted write; no code references it. | nothing - transient GDAL artifact |
| 32 | `data/processed/_calib_cache/{H2_precip.npy,H2_pet.npy,H2_dates.npy}` | 278 MB pure machine cache of the v2 forcing, held only to avoid re-reading model_inputs_v2/forcing.npz; contains no record of any verdict. | src/calib_v2.py rebuilds it on the next search - re-run python3.10 src/report_h2e.py afterwards and confirm F still matches 0.25931 to 1e-8 before trusting the tree |

**The three that are not purely mechanical, called out so the decision is explicit:**

1. **`results/README.md`** is the only *tracked* file on the list. Deleting it is a repository change, not a housekeeping sweep; it is a July-2026 placeholder promising `results/{hydrology,sediment,scenarios}` directories that were never created, and git history retains it. It also strands two now-dead `.gitignore` lines (`results/*.tif`, `results/*.gtb`).
2. **`data/processed/gauge_minibacia_ORIG_backup.csv`** was verified byte-identical to `gauge_minibacia.csv` except the `minibacia` column, whose pre-remap value is recorded per station in `gauge_minibacia_remap_report.csv:original_minibacia`. The information is not lost, but this is the one deletion that touches an audit-adjacent artifact.
3. **`data/processed/_calib_cache/{H2_precip,H2_pet,H2_dates}.npy`** is 278 MB of pure forcing cache. Deleting it is safe but *costly to undo*: `src/calib_v2.py` rebuilds it on the next search, and `python3.10 src/report_h2e.py` must be re-run afterwards to confirm `F` still matches 0.25931 to 1e-8 before the tree is trusted again.

## 3 - ARCHIVE: what moves out of the live tree

`docs/agents/` is the bulk of it - **136 of its 157 catalogued files are process journals** whose findings have been absorbed into numbered documents. They move to `docs/archive/agents/` and keep every byte. The 21 that stay live are the ones an unfinished task still reads.

| path | superseded by | reason |
|---|---|---|
| `docs/02_data_sources.md` | docs/16_forcing_pipeline_audit.md §1 (forcing) + docs/20_reproduction_guide.md §2 (what exists now) | Self-banners 'STATUS — STALE': the acquisition-status column predates Phase A; roles table only. |
| `docs/03_methodology.md` | docs/30_phase_c_plan.md + docs/31_phase_c_workplan.md (and CLAUDE.md 'Phase status') | Self-banners 'STATUS — STALE (phase markers)'; its DONE/IN-PROGRESS tags predate Phases A-C. |
| `docs/05_data_collection_plan.md` | docs/15_domain_correction.md (domain) + docs/20_reproduction_guide.md §1 (environment) | Self-banners 'STATUS — STALE': its provisional bbox and pilot-first scope were both superseded by the locked domain and whole-basin run. |
| `docs/06_ideam_stations.md` | docs/19_sediment_qc_audit.md + docs/32_ssc_qc_audit.md §R6 | Historical Q1 literature scan (2026-07-27) of the IDEAM network; superseded by measured QC of the actual holdings. |
| `docs/09_report_outline.md` | scripts/build_report_pdf.py (MGB-SED_complete_report.pdf) + docs/24/27/28 for the delivered deck | Self-banners 'STATUS — STALE'; its [done]/[pending] tags predate Phase B and the report is now built by script, not from this outline. |
| `docs/11_discharge_download_tracker.md` | docs/17_discharge_qc_audit.md §1, §3 | Self-banners 'STATUS — HISTORICAL': the per-department download it tracks is complete (the codigo 21-29 basin rule survives in it). |
| `docs/12_sediment_data_status.md` | docs/19_sediment_qc_audit.md (QC) + docs/32_ssc_qc_audit.md §R6 (what is usable) | Self-banners 'STATUS — SUPERSEDED': every coverage claim was replaced by measurement; carries the stale 'Phase C blocked' framing. |
| `docs/13_rating_curve_pairs.md` | docs/32_ssc_qc_audit.md §R5 (30 eras) + docs/34_observed_enso_contrast.md §1.5 | Self-banners 'STATUS — HISTORICAL': first Q<->SSC pairing pass, replaced by the rating eras fitted under the frozen C1 registration. |
| `docs/14_presentation_plan.md` | docs/24_presentation_outline.md + docs/27_presentation_script.md + docs/28_presentation_explained.md | Self-banners 'STATUS — SUPERSEDED': the July methodology deck plan, replaced by the delivered August deck. |
| `docs/21_project_state_and_handoff.md` | docs/00_INDEX.md (narrative entry point) + docs/30/31 (Phase C) + progress_map.html (status) | Self-labels 'HISTORICAL SNAPSHOT (2026-08-03)'; its status is two phase-closures out of date (H2E adoption and all of Phase C postdate it). |
| `docs/25_hydrology_closeout_plan.md` | docs/26_phase3_refit.md + docs/29_seed_expansion.md (outcomes); docs/30_phase_c_plan.md §1 (the closing decision); docs/33 §8 (second closure) | Self-banners 'HISTORICAL: this plan was executed' — a closeout PLAN for a phase that has since closed twice (H2E, then again after C2b). |
| `docs/39_contradiction_audit.md` | docs/00_INDEX.md §7 (known documentation defects) + the owning docs that enacted each fix | A dated read-only audit (2026-08-11) whose scope explicitly excluded docs/37+ and whose file:line references have drifted; its findings were absorbed into the live defect register. |
| `docs/54_c3_1_closure_and_c4_entry_status.md` | docs/55_c43_verdict.md (C4.3 ran) + docs/37 A3 (C3.1 enactment, the primary it points at) + progress_map.html | A pointer-only status snapshot (2026-08-12) with no new numbers; its four 'surviving blockers', its uncommitted-changeset residue and its '140 passed' test count have all been overtaken. |
| `docs/PROGRESS.md` | progress_map.html (live tracker) + docs/00_INDEX.md §3 (document table) | Self-banners 'SUPERSEDED by progress_map.html'; its checklist tree and doc index carry pre-collision numbering and a 'next step is C0' line from 2026-08-10. |
| `docs/open_questions.md` | docs/32_ssc_qc_audit.md §R6, docs/30_phase_c_plan.md §1, docs/15_domain_correction.md; open registers now in docs/00_INDEX.md §4 | Self-banners 'SUPERSEDED; all three questions are resolved' (Q1 -> 19/32, Q2 -> 07 + 30 §1, Q3 -> 15). |
| `docs/progress_journal.md` | progress_map.html (status) + docs/30_phase_c_plan.md .. docs/59_cross_implementation_comparison.md (the Phase C record) | Dated chronology that stops at 2026-08-03 and is explicitly to be read 'never for current status'; the last 16 days of work are recorded only in docs/30-59. |
| `docs/era5_download_checklist.md` | docs/16_forcing_pipeline_audit.md §1, §3.5 (what exists) + docs/15_domain_correction.md (the box) | Self-banners 'HISTORICAL: the download completed'; its checkbox grid reads 0/108 and its domain (east edge -72.9) was corrected before the real download. |
| `scripts/calib_queue_runner.py` | docs/33 §8 / docs/26 addendum (Phase B closed on H2E; no queue is registered to run). Reproduction of the adoption is now `python3.10 src/report_h2e.py`. | Phase-B DDS queue runner; both registered queues (docs29, c2b) completed and Phase B is CLOSED on H2E, C4.3 uses a closed-form alpha + beta sweep not a DDS queue - keep as the record of how the adopted H2E runs were launched. |
| `presentation_guide.html` | scripts/build_deck.py (28-slide deck) + docs/27_presentation_script.md / docs/28_presentation_explained.md | Self-contained speaker guide (tracked) for the OLD 19-slide Phase-B-era deck — no C5, no H2E, no 0.25931; the live script now builds 28 slides ending on the ENSO contrast, so the guide no longer matches the deck it explains. Destination docs/archive/. |
| `results/README.md` | docs/20_reproduction_guide.md artifact table + data/processed/ and figures/report/ | Stale forward-looking stub: it promises results/hydrology, results/sediment, results/scenarios which never materialised - the real outputs live in data/processed/ (sim_calibrated_v2, c5_enso_contrast.json, ...) and figures/report/, so the directory is no longer meaningful; 265 B, mtime 2026-07-27 01:21. |
| `src/merge_chirps_gauges.py` | docs/18 §15.5 (H-CHIRPS REFUTED by its own volume gate); v3 does not exist — the adopted forcing is gauge-only v2 (repair_precip_selectivity.py + idw_forcing.py) | SUPERSEDED EXPERIMENT: the v3 CHIRPS-gauge merge, built, gated and NOT ADOPTED — LOOCV passed (r 0.447) but the volume gate failed twice (+7.5 %), the registered repair was a no-op, and docs/18 §15.5 rules that 'no route to a passing volume gate exists inside the merge code'. Imported by NOTHING; absent from the docs/20 §3 regeneration chain. Destination src/archive/ or scripts/archive/ — it must stay reachable because it is the code behind a live refutation; never delete. |
| `src/package_precip_dataset.py` | src/build_data_final.py, which packages the whole data holdings with a verified MANIFEST rather than the precip subset | One-off delivery packager for the precipitation share bundle. EXCLUDED FROM THE REPO by .gitignore:20 (untracked; the .gitignore comment itself calls these 'packaging scripts — not project source'). Imported by nothing and cited by NO doc — the only src module with zero external citations. Destination scripts/archive/; note the move is a no-op in git. |
| `docs/agents/handoff_t6_amendment_drafts.md` | docs/37_c3_closure.md A3.9 + docs/51_ls_freeze_decision.md Amendment 3 | Proposed-text drafts for the four surviving T6 findings; all four were enacted — verified docs/37 A3.9/A3.9.1/A3.9.2, docs/51 Amendment 3 at :230/:257/:351/:763, and the nb19 band retirement (be8a62a). |
| `docs/agents/review_2026-08-10_docs31.md` | docs/31_phase_c_workplan.md + docs/00_INDEX.md (stale-claim repair, 2026-08-12) | 2026-08-10 adversarial audit of docs/31 (and docs/30); its findings were absorbed into the stage docs and the plan text it critiques has since been repaired by journal_stale-plan-index. |
| `docs/agents/journal_c0.md` | docs/26_phase3_refit.md (2026-08-10 addendum) | Stage C0 execution log: freezing and reproducing H2E through the C0.2 reproduction gate. |
| `docs/agents/journal_c1-ssc.md` | docs/32_ssc_qc_audit.md | Stage C1 execution log: classifying all 79 SSC stations against the frozen pre-registration. |
| `docs/agents/journal_c2-contrast.md` | docs/34_observed_enso_contrast.md | Stage C2 execution log: the model-free observed ENSO sediment contrast. |
| `docs/agents/journal_prereg-c2b.md` | docs/33_c2b_preregistration.md | Log of writing and freezing the C2b pre-registration (H-BFI / H-PEAK / H-CHIRPS gates). |
| `docs/agents/journal_bfi.md` | docs/33_c2b_preregistration.md §6 | C2b.1 measurement log: Eckhardt BFI observed vs simulated at 63 gauges; H-BFI held. |
| `docs/agents/journal_peaks.md` | docs/33_c2b_preregistration.md §7 | C2b.2 measurement log: flood-peak bias and its MUSLE propagation; the run that refuted H-PEAK. |
| `docs/agents/journal_refit-launch.md` | docs/33_c2b_preregistration.md §8 | Log of implementing the pre-registered signature terms and detaching the H2E-S refit queue. |
| `docs/agents/journal_critic.md` | docs/33_c2b_preregistration.md §6–§8 | Adversarial verification of the whole C2b/C1/C2 run, including frozen-doc integrity checks. |
| `docs/agents/journal_chirps-merge.md` | docs/18_hydrology_journal.md §15.5 | Build log for the CHIRPS-gauge merged rainfall field: LOOCV gate passed, volume gate failed. |
| `docs/agents/journal_chirps-refit.md` | docs/18_hydrology_journal.md §15.5 | C2b.3 log: quantile-map refit on repaired series including inferred-dry days; the registered repair was a no-op. |
| `docs/agents/journal_chirps-gate-spec.md` | docs/18_hydrology_journal.md §15.5 | Hypothesis H-A of the parallel volume-gate diagnosis: was the gate itself mis-specified. |
| `docs/agents/journal_chirps-orographic.md` | docs/18_hydrology_journal.md §15.5 | Hypothesis H-B: orographic explanation of the +7.5 % volume surplus. |
| `docs/agents/journal_chirps-transfer.md` | docs/18_hydrology_journal.md §15.5 | Hypothesis H-C: quantile-map transfer failure as the cause of the volume-gate miss. |
| `docs/agents/journal_chirps-blend-arith.md` | docs/18_hydrology_journal.md §15.5 | Hypothesis H-D: the blend arithmetic itself as the cause of the surplus. |
| `docs/agents/journal_chirps-mundane.md` | docs/18_hydrology_journal.md §15.5 | Hypothesis H-E: the eight mundane causes of the +152.15 mm/yr miss, all ruled out. |
| `docs/agents/journal_verify-b.md` | docs/18_hydrology_journal.md §15.5 | Adversarial verification of the H-B (orographic) report, every load-bearing number recomputed. |
| `docs/agents/journal_verify-d.md` | docs/18_hydrology_journal.md §15.5 | Adversarial verification of the H-D (blend arithmetic) report. |
| `docs/agents/journal_verify-e.md` | docs/18_hydrology_journal.md §15.5 | Adversarial verification of the H-E (mundane causes) report. |
| `docs/agents/journal_calibration-launch.md` | docs/29_seed_expansion.md | Log of pre-registering and detaching the 10-job seed-expansion calibration queue. |
| `docs/agents/journal_et-stress.md` | docs/29_seed_expansion.md | Log of adding the opt-in FAO-56 ET stress function that became the adopted H2E configuration. |
| `docs/agents/journal_research-diagnose.md` | docs/36_peak_deficit_options.md | Peak-deficit research lens: empirical diagnosis of which flood events the daily model misses. |
| `docs/agents/journal_research-data.md` | docs/36_peak_deficit_options.md | Peak-deficit research lens: reconnaissance of sub-daily precipitation availability 2008–2018. |
| `docs/agents/journal_research-method.md` | docs/36_peak_deficit_options.md | Peak-deficit research lens: how the MGB/MUSLE literature handles the peak problem. |
| `docs/agents/journal_research-synthesis.md` | docs/36_peak_deficit_options.md | Adjudication of the three peak-deficit lenses into one ranked decision document. |
| `docs/agents/journal_c31-ls2d.md` | docs/37_c3_closure.md | C3.1 build log: the Desmet & Govers 2-D LS factor, the last static MUSLE input. |
| `docs/agents/journal_c32-cp.md` | docs/41_cfactor_evidence.md | C3.2 build log: MUSLE C and P factors per land class with sources. |
| `docs/agents/journal_c33-qpeak.md` | docs/35_qpeak_preregistration.md | C3.3 log: choosing and pre-registering the q_peak proxy plus its bias statement. |
| `docs/agents/journal_c34-sediment-engine.md` | docs/37_c3_closure.md | C3.4 build log for src/mgb_sediment.py and tests/test_sediment.py. |
| `docs/agents/journal_c36-first-run.md` | docs/37_c3_closure.md | C3.6 log: first uncalibrated basin erosion run as an order-of-magnitude and pattern gate. |
| `docs/agents/journal_decide-units.md` | docs/37_c3_closure.md | Decision log deriving MUSLE's required units for alpha 11.8 / beta 0.56 from source literature. |
| `docs/agents/journal_decide-ls-aggregation.md` | docs/46_ls_preregistration.md | Decision log: which LS statistic survives aggregation and at what scale MUSLE is applied. |
| `docs/agents/journal_decide-ls-resolution.md` | docs/46_ls_preregistration.md | Decision log: whether the 90 m LS2D level is defensible or needs a reference-resolution correction. |
| `docs/agents/journal_dimensional-audit.md` | docs/37_c3_closure.md | Independent first-principles audit of the MUSLE dimensional/scale chain in src/mgb_sediment.py. |
| `docs/agents/journal_recompute.md` | docs/37_c3_closure.md | Log of applying the resolved conventions and re-running the basin decade; the C3-open verdict. |
| `docs/agents/journal_fixer.md` | docs/37_c3_closure.md | Log of the single critical MUSLE area-unit contradiction fix, minimal-change discipline. |
| `docs/agents/journal_cite-sdr.md` | docs/40_sdr_evidence.md | Log of settling docs/37 residual 3 — the uncited SDR 0.05–0.30 band, cited or retired. |
| `docs/agents/journal_cite-cfactor.md` | docs/41_cfactor_evidence.md | Log of putting the MUSLE C (and P) factors on a citable footing per land class with ranges. |
| `docs/agents/journal_alpha-guard.md` | docs/42_c4_guards.md | Log of replacing the blind docs/35 §6 alpha guard with the G1–G9 guard set. |
| `docs/agents/journal_reverdict.md` | docs/37_c3_closure.md A1/A2 | Log of re-issuing the C3 closure verdict as a dated amendment after the citation runs. |
| `docs/agents/journal_adj-ratio.md` | docs/43_c3_c4_gate.md | Adjudication lens: whether the C3 gross-erosion residual is constant in time and cancels in the C5 ratio. |
| `docs/agents/journal_adj-alpha-role.md` | docs/43_c3_c4_gate.md | Adjudication lens: the role alpha plays and what it can absorb. |
| `docs/agents/journal_adj-c4-feasibility.md` | docs/43_c3_c4_gate.md | Adjudication lens: whether C4 is feasible at all given the station set and equifinality. |
| `docs/agents/journal_decide-c3-c4.md` | docs/43_c3_c4_gate.md | Adjudication of the three C3→C4 lenses into one gate decision. |
| `docs/agents/journal_verify-gate-logic.md` | docs/43_c3_c4_gate.md | Adversarial verification of the C3→C4 gate decision. |
| `docs/agents/journal_c41-transport.md` | docs/47_c4_entry_verdict.md | C4.1 build log: channel transport of the suspended load down the reach topology. |
| `docs/agents/journal_verify-transport.md` | docs/47_c4_entry_verdict.md | Adversarial verification by measurement of the C4.1 transport implementation. |
| `docs/agents/journal_refute-transport-code.md` | docs/47_c4_entry_verdict.md | Refutation attempt against the 'state= validated for shape only' finding in mgb_transport.py. |
| `docs/agents/journal_c42-prereg.md` | docs/45_c4_preregistration.md | Log of writing the frozen C4.2 sediment-calibration pre-registration. |
| `docs/agents/journal_verify-prereg.md` | docs/45_c4_preregistration.md | Adversarial check of whether docs/45 is a real pre-registration or leaves escape hatches. |
| `docs/agents/journal_refute-prereg-integrity.md` | docs/45_c4_preregistration.md | Refutation attempt against the 'deposition axis pre-satisfied' pre-registration finding. |
| `docs/agents/journal_c3gate-synthesis.md` | docs/47_c4_entry_verdict.md | Log of writing the C4 entry verdict and deciding whether C4.3 may start. |
| `docs/agents/journal_backannotate-47.md` | docs/47_c4_entry_verdict.md §9 | Log of back-annotating docs/47 in place and adding its §9 after later stages landed. |
| `docs/agents/journal_refute-gate-logic.md` | docs/48_pi_band_revision.md | Refutation of 'sigma_r = 0.465 is a noise floor 3.1–3.9x below the residual scatter'; the band was retired. |
| `docs/agents/journal_refute-gate-logic-alpha.md` | docs/42_c4_guards.md | Refutation of the 'fit-set-implied alpha is below C4's box floor' finding; records a filename collision with the sibling agent. |
| `docs/agents/journal-debt-b5-piband.md` | docs/48_pi_band_revision.md | docs/47 §6.1 repair B5: reproducing and replacing the +-38 % Pi band built on a falsified noise floor. |
| `docs/agents/journal_debt-b4-transcription.md` | docs/42_c4_guards.md §9 | docs/47 B4 bookkeeping repair: transcribing P1/P2/P3 into docs/42 §9 and resolving the k_min discrepancy. |
| `docs/agents/journal_gate-reexpression.md` | docs/45_c4_preregistration.md §8 | docs/47 B2: re-expressing the C4.3 gate into a unit that survives C3.1. |
| `docs/agents/journal_amend-45-piband-disclosure.md` | docs/45_c4_preregistration.md §8 | Two amendments to docs/45 §8 replacing the +-38 % Pi band and adding the disclosure. |
| `docs/agents/journal_amend-4243-piband.md` | docs/42_c4_guards.md §9 + docs/43_c3_c4_gate.md §7 | Enacting the docs/48 Pi-band revision into docs/42 §9 and creating the docs/43 §7 amendment slot. |
| `docs/agents/journal_amend-35-label.md` | docs/35_qpeak_preregistration.md §9.4 | T2b: the docs/35 §9.4 label amendment. |
| `docs/agents/journal_defect-45-residual.md` | docs/45_c4_preregistration.md §8 + docs/51_ls_freeze_decision.md | Agent A6: recomputing f_area(V4) and finishing the mislabelled 'Buarque eq. 14' defect. |
| `docs/agents/journal_defect-farea-amend.md` | docs/51_ls_freeze_decision.md | T2a: the f_area(V4) numeric disagreement and the ln-identity defect, amended into docs/51 and docs/46 §10. |
| `docs/agents/journal_nbgen-eq14-relabel.md` | src/nbgen/make_nb18.py + docs/43_c3_c4_gate.md §7 | T2b code/notebook share: relabelling min(m,0.5) away from 'Buarque eq. 14' in make_nb18.py. |
| `docs/agents/journal_ls-prereg.md` | docs/46_ls_preregistration.md | Log of drafting the LS-level pre-registration (not freezing it). |
| `docs/agents/journal_ls-evidence.md` | docs/46_ls_preregistration.md | Primary-source evidence gathering for which LS formulation the model should use. |
| `docs/agents/journal_ls-impact.md` | docs/51_ls_freeze_decision.md | Measurement of what the LS bracket does to every downstream number, without changing the adopted LS. |
| `docs/agents/journal_ls-variants-harness.md` | docs/46_ls_preregistration.md §3.1 | Build log for scripts/c3/ls2d_variants.py, the eight-variant LS harness behind the freeze decision. |
| `docs/agents/journal_audit-harness-code.md` | docs/51_ls_freeze_decision.md | Adversarial correctness audit of the LS variant harness code. |
| `docs/agents/journal_audit-harness-physics.md` | docs/51_ls_freeze_decision.md | Physical/arithmetic coherence audit of the LS harness's reported numbers. |
| `docs/agents/journal_ls-freeze-decision.md` | docs/51_ls_freeze_decision.md | Log of deciding whether docs/46 could be frozen and restating the LS bracket. |
| `docs/agents/journal_amend-46.md` | docs/46_ls_preregistration.md | Log of applying the docs/51 §5.6 amendment set and freezing docs/46 honestly as READ OUT. |
| `docs/agents/journal_defect-a.md` | docs/49_defect_a_resolution.md | Resolution of docs/46 §1.1 Defect A with numbers (corrected eq.14 lever, joint factor, area vs erosion fraction). |
| `docs/agents/journal_defect-b.md` | docs/50_defect_b_resolution.md | Resolution of docs/46 §1.1 Defect B: isolating the L-form lever and correcting the bracket endpoint. |
| `docs/agents/journal_bar-statistical.md` | docs/52_materiality_bar_decision.md | Blinded materiality-bar proposal, statistical angle (which noise quantity, if any, is defensible). |
| `docs/agents/journal_bar-decision-theoretic.md` | docs/52_materiality_bar_decision.md | Blinded materiality-bar proposal, decision-theoretic angle (what the bar is for). |
| `docs/agents/journal_bar-eliminative.md` | docs/52_materiality_bar_decision.md | Blinded materiality-bar proposal, eliminative angle — argue for NO numeric bar; the option that won. |
| `docs/agents/journal_bar-judge.md` | docs/52_materiality_bar_decision.md | Adjudication of the three bar proposals on principle, before Delta_shape was computed. |
| `docs/agents/journal_lens-blinding.md` | docs/52_materiality_bar_decision.md | T6 adversarial lens: was the bar-decision blinding actually held. |
| `docs/agents/journal_delta-shape.md` | docs/53_delta_shape_pretest.md | Computation of the docs/46 §6.1 Delta_shape pre-test (docs/47 O6, docs/51 blocking item 5). |
| `docs/agents/journal_panel-fidelity.md` | docs/37_c3_closure.md A3 | C3.1 ls_formulation decision panellist, fidelity lens, read-only. |
| `docs/agents/journal_panel-negative.md` | docs/37_c3_closure.md A3 | C3.1 ls_formulation decision panellist, NEGATIVE-prior lens. |
| `docs/agents/journal_panel-posthoc.md` | docs/37_c3_closure.md A3 | C3.1 ls_formulation decision panellist, post-hoc-hazard auditor lens. |
| `docs/agents/journal_c31-enactment.md` | docs/37_c3_closure.md A3 | Log of writing the C3.1 enactment amendment (docs/47 B1). |
| `docs/agents/journal_a3-enactment.md` | docs/37_c3_closure.md A3 | T1 log of the docs/37 A3 amendment — the C3.1 enactment that materialised and adopted V4_dg. |
| `docs/agents/journal_lens-a3-overreach.md` | docs/37_c3_closure.md A3.9.2 | T6 adversarial lens: does docs/37 A3 claim more than it may about unblocking C4.3. |
| `docs/agents/journal_lens-freeze-honesty.md` | docs/37_c3_closure.md A3.9.1 | T6 adversarial lens: is the docs/46 freeze honest about what was pre-registered. |
| `docs/agents/journal_lens-dangling-refs.md` | docs/54_c3_1_closure_and_c4_entry_status.md §7 | T6 adversarial lens: superseded numbers still presented as current, with no supersession pointer. |
| `docs/agents/journal_lens-new-bar-costume.md` | docs/54_c3_1_closure_and_c4_entry_status.md §7 | T6 adversarial lens: is the replacement band the retired error in a new costume. |
| `docs/agents/journal_refute-t6-1.md` | docs/37_c3_closure.md A3.9.1 | Refuter of the T6 'exercised' overclaim finding; survived, and was enacted. |
| `docs/agents/journal_refute-t6-2.md` | src/nbgen/make_nb19.py (be8a62a) | Refuter of the CRITICAL 'retired +-38 % / 0.1644 band alive in make_nb19.py' finding; survived and drove commit be8a62a. |
| `docs/agents/journal_refute-t6-3.md` | docs/37_c3_closure.md A3.9 | Refuter of the 'docs/37:1258 unstruck 0.1644 SE' finding. |
| `docs/agents/journal_refute-t6-4.md` | docs/51_ls_freeze_decision.md Amendment 3 | Refuter of the 'docs/51 verdicts read against the struck bar with no docs/52 pointer' finding; survived and was enacted. |
| `docs/agents/journal_refute-t6-5.md` | docs/45_c4_preregistration.md §7.1 | Refuter of the docs/45 §7.1 'Amendments THREE vs four' registration-card finding. |
| `docs/agents/journal_refute-t6-6.md` | docs/37_c3_closure.md A3.9.2 | Refuter of the docs/37 A3.1.3 fallback-branch conflation finding; survived and was enacted. |
| `docs/agents/journal_refute-t6-7.md` | src/nbgen/make_nb19.py | Refuter of the CRITICAL dangling-reference finding against make_nb19.py / nb19 (shortest journal in the set). |
| `docs/agents/journal_refute-t6-8.md` | src/nbgen/make_nb18.py | Refuter of the 'nb18 equifinality figure draws the superseded 2.0–9.9 band and 4.45 reference' finding. |
| `docs/agents/journal_nb15.md` | src/nbgen/make_nb15.py | Build log for src/nbgen/make_nb15.py and the executed nb15 (stages C0 and C1). |
| `docs/agents/journal_nb16.md` | src/nbgen/make_nb16.py | Build log for src/nbgen/make_nb16.py and nb16 (stage C2, the observed contrast). |
| `docs/agents/journal_nb17.md` | src/nbgen/make_nb17.py | Build log for src/nbgen/make_nb17.py and nb17 (stage C2b, BFI and the H-PEAK refutation). |
| `docs/agents/journal_nb18.md` | src/nbgen/make_nb18.py | Build log for src/nbgen/make_nb18.py and nb18 (stage C3, MUSLE construction). |
| `docs/agents/journal_nb19.md` | src/nbgen/make_nb19.py | Build log for src/nbgen/make_nb19.py and nb19 (the C3 gate and C4 setup). |
| `docs/agents/journal_verify-nb19.md` | notebooks/19_c3_gate_and_c4_setup.ipynb | Verification lens: do nb19's prose claims match its stored executed outputs. |
| `docs/agents/journal_refute-nb19-claims.md` | src/nbgen/make_nb19.py | Refutation attempt against the nb19 cell-32 runoff-ratio contradiction finding. |
| `docs/agents/journal_refute-my-fixes.md` | src/mgb_hydrology.py (108fbc0) | Refutation attempt against the 'relative-path default survives in mgb_hydrology.py' finding. |
| `docs/agents/journal_verify-fixes.md` | src/mgb_hydrology.py (108fbc0) | Hostile audit of the cwd-independent-paths / nb19 mini_ids commit. |
| `docs/agents/journal_nb-banner-1011.md` | src/nbgen/make_nb10.py + make_nb11.py | Agent A2: status banners and inline annotations on nb10/nb11 via their generators. |
| `docs/agents/journal_nb-banner-1217.md` | src/nbgen/make_nb12.py … make_nb17.py | Agent A3: status banners on notebooks 12–17. |
| `docs/agents/journal_consolidate.md` | docs/00_INDEX.md | Log of building docs/00_INDEX.md as the single entry point and mapping every doc's status. |
| `docs/agents/journal_hygiene.md` | docs/00_INDEX.md | Repository-root/top-level cleanup log under a concurrent-workflow ownership constraint. |
| `docs/agents/journal_hygiene-critic.md` | docs/39_contradiction_audit.md | Verification that the hygiene/contradictions/consolidate pass lost no knowledge and invented no contradictions. |
| `docs/agents/journal_contradictions.md` | docs/39_contradiction_audit.md | Cross-documentation contradiction hunt with a number ledger. |
| `docs/agents/journal_stale-plan-index.md` | docs/00_INDEX.md + docs/30_phase_c_plan.md | Agent A1: repairing stale claims in docs/30 and docs/00, and writing the canonical v1/v2/v3 forcing definition. |
| `docs/agents/journal_sweep-core.md` | docs/00_INDEX.md | Agent A4: read-only stale-claim sweep of docs/00–docs/31 plus the unnumbered trackers. |
| `docs/agents/journal_sweep-phasec.md` | docs/54_c3_1_closure_and_c4_entry_status.md | Agent A5: read-only stale-claim sweep of the Phase C documents. |
| `docs/agents/journal_fix-prereads-registers.md` | docs/00_INDEX.md + CLAUDE.md | Agent A10: repairing the two mandatory pre-reads and the three open registers. |
| `docs/agents/journal_tracker.md` | progress_map.html | Log of rebuilding progress_map.html to show the true current state. |
| `docs/agents/journal_docs.md` | docs/20_reproduction_guide.md + docs/21_project_state_and_handoff.md | 2026-08-03 documentation closeout so the repo explains itself without conversation history. |
| `docs/agents/journal_packaging.md` | docs/20_reproduction_guide.md | 2026-08-03 packaging log: requirements/environment/pyproject/CITATION and the first pytest suite. |
| `docs/agents/journal_housekeeping.md` | docs/20_reproduction_guide.md | 2026-08-03 log of making every presentation deliverable regenerable and staging a clean commit. |
| `docs/agents/journal_commit.md` | git history | Commit-agent log for the C2b/C1/C2 run, in the pre-registered commit order (largest journal in the set). |
| `docs/agents/journal_commit-1.md` | git history | 2026-08-03 commit-agent log turning five build agents' work into separated commits. |
| `docs/agents/journal_commit-2.md` | git history | 2026-08-03 final-sweep commit-agent log with explicit paths only. |
| `docs/agents/journal_x59-overlap.md` | docs/59_cross_implementation_comparison.md | M1 evidence run: station-code and ENSO-window overlap between the two implementations. |
| `docs/agents/journal_x59-bridge.md` | docs/59_cross_implementation_comparison.md | M2 evidence run: whether their concentration contrast can bridge arithmetically to our flux contrast. |
| `docs/agents/journal_x59-inputs.md` | docs/59_cross_implementation_comparison.md | M3 evidence run: the side-by-side input table behind the shared-inputs inference. |
| `docs/agents/journal_x59-theirnumbers.md` | docs/59_cross_implementation_comparison.md | M4 evidence run: independent reproduction of every number docs/59 quotes from their repo. |
| `docs/agents/journal_x59-write.md` | docs/59_cross_implementation_comparison.md | Writer-phase log for docs/59 itself. |
| `docs/agents/journal_x59-lens-overclaim.md` | docs/agents/FIXLIST_docs59.md | Adversarial lens on docs/59 overclaim; its default posture came back NOT SUSTAINED. Cited by the still-open FIXLIST — archive together with it. |
| `docs/agents/journal_x59-lens-numbers.md` | docs/agents/FIXLIST_docs59.md | Adversarial lens that re-derived ~218 docs/59 figures (211 clean). Cited by the still-open FIXLIST — archive together with it. |
| `docs/agents/journal_x59-refute-1.md` | docs/agents/FIXLIST_docs59.md A1 | Refuter of the HIGH 'narrowing to K is contradicted by their committed code' finding; confirmed it, becoming FIXLIST A1. |
| `docs/agents/journal_x59-refute-2.md` | docs/agents/FIXLIST_docs59.md | Refuter of the '§2 table sets the two scores side by side' finding. |
| `docs/agents/journal_x59-refute-3.md` | docs/agents/FIXLIST_docs59.md A2 | Refuter of the 'bundle archive is on this disk' finding; confirmed it, becoming FIXLIST A2. |
| `docs/agents/journal_x59-refute-4.md` | docs/agents/FIXLIST_docs59.md A3 | Refuter of the 'Fagundes bar cited to docs/45 §84, which contains no bar' finding; becoming FIXLIST A3. |
| `docs/agents/journal_x59-refute-5.md` | docs/agents/FIXLIST_docs59.md A4 | Refuter of the 'DEM-archive row cites docs/15 L24/L31' finding; becoming FIXLIST A4. |
| `data/processed/merge_loocv_report.csv` | docs/18 §15.5 read-out; merge_loocv_report_v2.csv for the later run | Pre-refit CHIRPS-merge LOOCV scores; the merge is a closed dead end (no route to a passing volume gate) but this records how that was measured. |
| `data/processed/merge_loocv_report_v2.csv` | docs/18 §15.5 (the owning read-out) | The C2b.3 merge LOOCV run (r 0.447 passed, volume gate failed twice); historical evidence for a v3 forcing that does not exist. |
| `data/processed/_calib_cache/run_{H1,H2}_2026090{1,2}.bat` | the queue driver in src/calib_v2.py | Four 214-byte launchers for the original H1/H2 DDS queue; historical run provenance, no longer invoked. |
| `data/processed/_calib_cache/logs/` | - | 21 DDS run logs (incl. four zero-byte .err files and queue_runner.log) - the timing/provenance record of the calibration queue, not part of any live path. |
| `data/raw/climate/era5land_2009_*.nc .. era5land_2016_*.nc (78 bare-named files)` | data/raw/climate/era5land_basin_*.nc (src/download_era5.py PREFIX = 'era5land_basin_') | First-generation ERA5 pull, 392 MB, ~4.7 MB/month vs 35 MB for the era5land_basin_* files that replaced them; no code in src/ or notebooks reads the bare naming. |
| `data/raw/climate/_corrupt/era5land_ext_2008_M06.nc.corrupt` | - | 43 MB quarantined bad download, deliberately set aside; docs/18 notes nb11's PET_READY hole still traces to it, so the quarantine itself is the record. |
| `data/raw/dem/rasters_COP90.tar.gz` | data/raw/dem/rasters_COP90_Correcte_Corrdinatzs.tar.gz | 214 MB earlier COP90 pull with the uncorrected coordinates; only progress_journal.md's first DEM note refers to it. |
| `data/raw/dem/rasters_COP90.tar.gz.properties` | data/raw/dem/rasters_COP90_Correcte_Corrdinatzs.tar.gz | 54-byte size manifest for the superseded tarball; archive with it. |
| `data/raw/observed/precip/dhime/_cordoba_x/` | data/raw/observed/precip/dhime/regions/cordoba/ | Single descargaDhime.csv under an underscore/_x quarantine name - a set-aside Cordoba pull kept out of the region consolidation. |
| `data/raw/observed/concentracion_diaria_santander.csv` | data/raw/observed/sedimento/ssc_santander_1968_2014.csv and ssc_santander_2015_2018.csv | 38 KB early one-off Santander concentration pull sitting loose at the observed/ root, outside the sedimento/ layout the pipeline reads. |
| `data/raw/observed/Reporte de información Hidrometeorológica de DHIME generado (1).zip` | the organised pulls under data/raw/observed/precip/dhime/ and caudal/ | 3.7 KB stray DHIME portal export with a browser-generated name and a '(1)' duplicate suffix; nothing reads it. |

## 4 - KEEP: the live tree, by subtree

### docs-numbered - 48 live

| path | why it is live |
|---|---|
| `docs/00_INDEX.md` | The live map and single entry point (RULE 0 precedence rules, WHERE-IS-IT table); stale only in that its §3 table stops at 53 and omits 54-59. |
| `docs/00_objectives_and_hypotheses.md` | Framing doc that still states the main/specific objectives and H1-H4 the whole study is answering. |
| `docs/01_scientific_background.md` | Live framing: what MGB/MGB-SED are and the Fagundes et al. approach being transposed. |
| `docs/04_model_structure.md` | Live framing: the model graph (inputs -> preprocessing -> URH -> hydrology -> sediment) still describes the built pipeline. |
| `docs/07_enso_years.md` | Still the basis of the La Nina 2011 vs El Nino 2015-16 pairing (ONI grounds), re-decided and kept in docs/30 §1. |
| `docs/08_download_guide.md` | Part of the reproduction chain — the portal click-paths are live recipes; only its quoted pilot bbox is superseded (annotated in place). |
| `docs/10_ideam_download_recipe.md` | Live procedural doc: the validated manual DHIME click-path, the only way to re-fetch IDEAM series. |
| `docs/15_domain_correction.md` | Owns the authoritative locked domain box (Xmin -77.0, Xmax -72.3, Ymin 1.4, Ymax 11.4) that every raster and ERA5 mosaic inherits. |
| `docs/16_forcing_pipeline_audit.md` | The forcing knowledge base — §4 data defects, §5 development errors, §6 traps (CLAUDE.md forbids touching precip/ERA5 code before reading it); status framing back-annotated in place. |
| `docs/17_discharge_qc_audit.md` | Live discharge QC record: the broken gauge->minibacia mapping (§3.1) and §4 checked-and-cleared, still the owner of discharge data provenance. |
| `docs/18_hydrology_journal.md` | The Phase B record — §5 verdict, §6 refutations, §7 traps, §8 open items, §15/§15.5 the CHIRPS merge read-out that closes the v3 question. |
| `docs/19_sediment_qc_audit.md` | Live SSC QC record: §3.1 the DHIME date trap (a general ingest rule), the unit trap, and §3.9 the honest ceiling on the sediment phase. |
| `docs/20_reproduction_guide.md` | The reproduction chain: environment, regeneration order, gitignored artifacts, calibration monitor/resume — required to rebuild anything unversioned. |
| `docs/22_dry_phase_diagnosis.md` | LIVE and mandatory before touching calibration: the three refuted El Nino hypotheses and §4.7 the r ~= 0.57 ceiling that Phase B closed on. |
| `docs/23_gauge_geometry.md` | LIVE: the IDW order-dependence fix, gauge triage, and §13.2 the unreliable catchment areas that are the source of the t/km2/yr embargo. |
| `docs/24_presentation_outline.md` | The delivered August deck outline, still tracking the current deck (updated 2026-08-13 with the Phase C ending); pairs with scripts/build_deck.py. |
| `docs/26_phase3_refit.md` | The parameter record: the v2 refit, H2 - H1, §5.1 the fitted-parameter caveat, and the addendum adopting/freezing H2E (F = 0.25931). |
| `docs/27_presentation_script.md` | Live delivery artifact — the spoken script, with six superseded passages struck and 'SAY THIS INSTEAD' replacements annotated 2026-08-12. |
| `docs/28_presentation_explained.md` | Live companion explaining every deck term/number for a non-modeller, with corrected numbers annotated in place (incl. the -0.0005 skill correction). |
| `docs/29_seed_expansion.md` | Frozen pre-registration plus its read-out: H1 vs H2 not separated, H2E succeeded — the evidence H2E's adoption rests on. |
| `docs/30_phase_c_plan.md` | The ACTIVE plan: records the team decision closing Phase B on the input ceiling with H2E, and the C0-C5 staging. |
| `docs/31_phase_c_workplan.md` | The live execution-level work breakdown (In/Out/Gate per subtask, paste-prompts, risk register); its 'C5 results take number 54' pointer is stale — C5 is docs/56. |
| `docs/32_ssc_qc_audit.md` | Stage C1 frozen pre-registration + appended results: 79/79 SSC stations classified, 18 usable, one Magdalena-trunk station 21237020. |
| `docs/33_c2b_preregistration.md` | Frozen C2b pre-registration (H-BFI / H-PEAK / H-CHIRPS gates) with its read-out in §6-§8; amendable only via its own slot. |
| `docs/34_observed_enso_contrast.md` | Frozen §1 registration + results: the model-free observed ENSO contrast (22/22 station ratios) that is C5's target and a standalone result. |
| `docs/35_qpeak_preregistration.md` | Frozen C3.3 registration of the q_peak proxy, its signed bias and the C4 anti-compensation rule; §9 is its amendment slot (carries the adopted convention). |
| `docs/36_peak_deficit_options.md` | Live adjudication of the structural peak deficit with a standing recommendation (do not pursue a fix) and an independent confirmation of the r-ceiling. |
| `docs/37_c3_closure.md` | The C3 record — verdict OPEN plus amendments A1/A1.9/A2/A3 carrying the live load numbers (299.5387 vs 248.7298 Mt/yr) and the LS enactment. |
| `docs/38_data_acquisition_protocols.md` | The only surviving record of six retired binary operator hand-offs; irreplaceable transcription plus §7's contradiction list. |
| `docs/40_sdr_evidence.md` | Owns the SDR question: the 0.05-0.30 band ruled UNCITABLE and replaced by an evaluable gross-erosion-rate clause still used as the gate. |
| `docs/41_cfactor_evidence.md` | Owns the provenance of urh_cp_factors.csv (8/8 rows sourced and ranged) and the x1.2043 revision behind the adopted 299.5387 Mt/yr. |
| `docs/42_c4_guards.md` | Frozen C4 guard set G1-G9 (seven scalars = one product Pi, condition number inf); §9 is the amendment slot — never archive a frozen registration. |
| `docs/43_c3_c4_gate.md` | Live decision record: C3 STAYS OPEN / C4 PROCEEDS CONDITIONALLY plus the G9 coverage disclosure; only its C4.3 clause is narrowed (not replaced) by docs/47. |
| `docs/45_c4_preregistration.md` | Frozen C4.2 pre-registration: the F_report bar, alpha/beta boxes, CAL-8 set, windows, seeds and the eight ADOPT conditions docs/55 was judged against. |
| `docs/46_ls_preregistration.md` | Frozen (READ OUT) LS-level registration with §9 the registration card and §10 the amendment slot; it fixes the Branch-B ordering gate C4.3 ran under. |
| `docs/47_c4_entry_verdict.md` | The named authority on C4.3 entry (C4.3-BLOCKED-UNTIL-LS-LANDS) with §9's 2026-08-12 back-annotation ledger; still the doc that owns the block condition. |
| `docs/48_pi_band_revision.md` | Owns the Pi band and the meaning of sigma_r (observer-vs-observer, not a model-obs residual sd); retires the +/-38 % band still cited elsewhere. |
| `docs/49_defect_a_resolution.md` | Owns the Defect A measurement (real as a reading defect, immaterial as a level correction) that docs/46 froze on; corrections annotated in place. |
| `docs/50_defect_b_resolution.md` | Owns the decomposition of the published x0.790 into an L-form factor and an S swap — the number docs/46/51 build the LS bracket from. |
| `docs/51_ls_freeze_decision.md` | Owns the corrected LS bracket f_LS in [0.25146, 0.43194] erosion-weighted (2.3151x-3.9768x) that the adopted V4_dg field and the C4.3 block rest on. |
| `docs/52_materiality_bar_decision.md` | Owns the live rule that there is NO numeric materiality bar (0.1644 ln struck, not rescaled) — decided blind to Delta_shape, so it must stay readable. |
| `docs/53_delta_shape_pretest.md` | Owns the computed Delta_shape = 0.1299456916752905 and the resulting mandatory Branch B, cited by docs/55 as the reason no surface was rescaled. |
| `docs/55_c43_verdict.md` | Live result doc: the C4.3 verdict RAILED / EXPLORATORY, not adopted (median KGE_ln -0.118 / +0.139, alpha wants ~0.48 below the box floor). |
| `docs/56_c5_enso_application.md` | Live result doc and the project headline: the model reproduces the observed ENSO contrast, 18/18 stations, median rate ratio 3.05x. |
| `docs/57_b5_gauge_expansion.md` | Live result doc: 46 SSC sites geocoded, 0 with same-code discharge — the measured hard limit on growing the flux gauge set past 18. |
| `docs/58_rainfall_ceiling_bound.md` | Live result doc: bounds the last rainfall lever at <= +0.006 r, closing the r ~= 0.57 ceiling as structural. |
| `docs/59_cross_implementation_comparison.md` | Live result doc (untracked in git — needs committing): the R1 vs R2 cross-implementation comparison and what grade it establishes. |
| `docs/git_workflow.md` | Live procedural quick-reference (commit routine, secrets/gitignore gotchas, stale-lock fix); overlaps but does not conflict with CLAUDE.md's commit-style rule. |

*Agent note:* SUBTREE TOTALS: 65 files at docs/ top level (docs/agents/ excluded — separate subtree). 48 KEEP, 17 ARCHIVE, 0 DELETE. Nothing in this subtree is dead weight: every file is either authored narrative, a frozen registration, a result doc, or a dated record of how a verdict was reached.  NUMBERING GAPS / COLLISIONS (verified against `ls docs/*.md`): - **44 was never assigned** — the sequence skips 43 -> 45. This is deliberate and documented in docs/00_INDEX.md §3 (row "44 \| — \| never assigned") and in docs/progress_journal.md's banner. Not a missing file. - **00 is intentionally duplicated**: 00_INDEX.md and 00_objectives_and_hypotheses.md, so the index sorts first (docs/00_INDEX.md "Numbering discipline" says this is the only intentional duplicate). - 33/34 collided once historically (resolved; correction note carried in docs/31), and docs/33 §5.2's reservation of number 36 was taken first by docs/36 — both already annotated in-place. - No other gaps: 00-43 and 45-59 are all present.  MISMATCHES AGAINST CANONICAL STATE (found while reading headers; report only, nothing edited): 1. **The live map is ~6 documents behind.** docs/00_INDEX.md §3's table ends at 53; docs/54-59 (including the three result docs 55/56/57 and the headline C5 result) are absent from it. Its own §7 item 11 predicted exactly this failure mode ("the §3 table is the thing most likely to be stale in this file"). If docs/00_INDEX.md is to stay the entry point, adding 54-59 is the single highest-value doc fix in this subtree. 2. **"C4.3 BLOCKED" / "C5 not started" survives widely** — docs/47 (by design; it owns the block), docs/54, docs/PROGRESS.md, docs/31's stage pointer and docs/00_INDEX.md §4. Canonically C4.3 has since RUN (docs/55, railed/exploratory, not adopted) and C5 is COMPLETE (docs/56). docs/55 states it discharges docs/47 O5, but no doc back-annotates docs/47 or docs/00_INDEX.md with the post-run state. 3. **Stale test count**: docs/54 quotes "140 passed" twice (lines 56 and 137, one of them as an action item). Canonical is 154 passed — already correct in docs/35 (line 1278) and docs/37 (line 2395). This is one of the reasons docs/54 is filed ARCHIVE. 4. **docs/31's numbering note is stale in a load-bearing way**: it says "the C5 results document is unwritten and takes the next free number, 54". 54 was taken by the C3.1 status doc and C5 is docs/56 — a stale `Out:` target in a paste-prompt writes the wrong file, which docs/31's own banner warns about. 5. **docs/59 is untracked in git** (`?? docs/59_cross_implementation_comparison.md`), as are all the x59-* and nbc1-* agent journals it cites. A live result doc sitting outside history is a real loss risk — it should be committed before any reorganization moves files.  ARCHIVE-DESTINATION CAUTION: docs/progress_journal.md, docs/PROGRESS.md and docs/21 are linked from CLAUDE.md and/or docs/00_INDEX.md. Moving them to docs/archive/ requires updating those pointers in the same change, or the entry point breaks.  HOUSE-STYLE NOTE: many ARCHIVE candidates already carry an explicit "STATUS — STALE/HISTORICAL/SUPERSEDED" banner naming their successor (02, 03, 05, 09, 11, 12, 13, 14, 21, 25, PROGRESS, open_questions, era5_download_checklist). I took those banners as the primary evidence and reproduced their named successors in superseded_by. Kill-list words inside strike-through blocks (e.g. docs/49/50/52's "~~_DRAFT.md~~", docs/37's struck brackets) were read as correct house style, not defects.

### scripts - 21 live

| path | why it is live |
|---|---|
| `scripts/build_deck.py` | The live PPTX deliverable builder (MGB-SED_Magdalena_FIGURES.pptx), wired into Makefile `deck`; assembles 28 figures, generates none itself. |
| `scripts/build_report_pdf.py` | The live PDF deliverable builder (MGB-SED_complete_report.pdf) - reportlab + mathtext, consumes figures/report/fig1-5; its OUT/EQD paths are stale sandbox absolutes that must be fixed before it runs here. |
| `scripts/c2_compute.py` | Only regenerator of the C2 observed-contrast tables (c2_station_window_flux/c2_rate_ratios/c2_monthly_shape.csv under gitignored data/processed/c2/) that docs/34 and generate_enso_figures.py depend on. |
| `scripts/c2_consistency_figs.py` | Produces the three docs/34-listed C2 figures (gen_c2_ratio_dotplot / flux_timeseries / monthly_shape) into gitignored figures/deck/, so it is the sole regenerator. |
| `scripts/extract_notebook_figures.py` | Still used - build_deck.py places 15 notebook-derived PNGs (01_c003_1 .. 14_c031_3) and figures/deck/ is gitignored, so this is the only way to repopulate them; Makefile `figures` calls it first. |
| `scripts/generate_enso_figures.py` | Untracked but LIVE - sole generator of gen_obs_contrast_detail.png and gen_sensitivity_detail.png, both consumed by build_deck.py (lines 493, 526); should be committed. |
| `scripts/generate_report_figures.py` | Sole generator of the five committed report figures (figures/report/fig1-5) used by build_report_pdf.py, and the upstream of four deck gen_*.png that were copied from it by hand. |
| `scripts/make_deck_charts.py` | Sole generator of the four Phase-B deck charts (gen_attempts / gen_skill_clim / gen_recession / gen_h2_h1) from sim_calibrated_v2/metrics_fleet.csv; Makefile `figures` calls it. |
| `scripts/c1/b5_geocode_ssc.py` | The docs/57 B5 harness that established the gauge limit (46 SSC sites geocoded, 0 with discharge => the flux gauge set cannot grow past ~18); writes ssc_recovered_coords.csv. |
| `scripts/c2b/bfi_figure.py` | Only regenerator of figures/deck/gen_bfi.png, the figure cited by docs/33 §6 for the H-BFI read-out; hardcodes the repo root as an absolute Windows path. |
| `scripts/c2b/bfi_measure.py` | The docs/33 §2.1 measurement driver behind the H-BFI verdict (held) - reproduction chain for a live, still-quoted number. |
| `scripts/c2b/peaks_measure.py` | The docs/33 §2.3 peak-bias driver that refuted H-PEAK; docs/36 §775 still instructs re-derivation from it - but its OUT constant points at a dead per-session scratchpad path. |
| `scripts/c3/ls2d.py` | The C3.1 LS2D builder - the anchored source that ls2d_variants.py and ls2d_defect_b.py import rather than copy, and the origin of urh_ls2d.csv. |
| `scripts/c3/ls2d_defect_b.py` | Decomposes the published x0.790 defect; its ls2d_defect_b.json V4_over_V0 is a live independent cross-check of the adopted factor in docs/37 A3 and docs/43. |
| `scripts/c3/ls2d_variants.py` | Produces urh_ls2d_variants.csv, the ADOPTED LS product the engine now defaults to (ls2d_column V4_dg, src/mgb_sediment.py:924) - core live pipeline. |
| `scripts/c3/ls_erosion_weights.py` | Computes the erosion-weighted f_LS = 0.25146 that decides the adopted LS formulation, gated on 299.5387 Mt/yr; still owes a one-word f_area header fix per docs/43 amendment 8. |
| `scripts/c3/ls_stratified_report.py` | Discharges the last owed item of docs/47 §9.2 blocker 4 (docs/46 §10 Amendment 3) - the stratified LS report by elevation band, slope tercile and station. |
| `scripts/c3/qpeak.py` | HARD DEPENDENCY - src/mgb_sediment.py:_import_qpeak loads it by literal path and raises ImportError if absent, and tests/test_qpeak.py imports it; the docs/35-registered q_peak proxy. Must not move. |
| `scripts/c4/o5_calibration_profile.py` | The docs/55 C4.3 harness that produced the RAILED/EXPLORATORY verdict (median KGE_ln -0.118 / +0.139, alpha wants ~0.48) and report_C4.json. |
| `scripts/c5/c5_enso_contrast.py` | The docs/56 harness behind the headline result - modelled ENSO contrast, 18/18 stations, median rate ratio 3.0468; writes c5_enso_contrast.{json,md}. |
| `scripts/c5/c5_sensitivity.py` | The docs/56 robustness sweep (beta x window pair) proving the contrast direction never reverses; writes c5_sensitivity.csv, which generate_enso_figures.py reads. |

*Agent note:* 29 files: 22 .py + 7 .pyc. Dispositions: 21 KEEP, 1 ARCHIVE (calib_queue_runner.py), 7 DELETE (all __pycache__). No byte-duplicate .py files exist in the subtree (md5 check, 0 collisions). One file is untracked: scripts/generate_enso_figures.py.  === OVERLAP VERDICT: the four figure/deck scripts do NOT duplicate each other === Outputs are disjoint; the apparent overlap is a naming/copy gap, not duplicated code. - build_deck.py (583 L) generates NO figures. It imports only pathlib + python-pptx and assembles 28 pre-existing PNGs from figures/deck/. It is an assembler, not a chart script. - make_deck_charts.py (202 L) -> figures/deck/gen_attempts.png, gen_skill_clim.png, gen_recession.png, gen_h2_h1.png (Phase B, from sim_calibrated_v2/metrics_fleet.csv). - generate_report_figures.py (133 L) -> figures/report/fig1_enso_contrast.png, fig2_kge_alpha_rail.png, fig3_kge_vs_r.png, fig4_ceiling_bound.png, fig5_oni_windows.png (C5 / C4.3 / ceiling / ONI, from c5_enso_contrast.json + o5_calibration_profile.json). These five ARE tracked in git. - generate_enso_figures.py (76 L) -> figures/deck/gen_obs_contrast_detail.png, gen_sensitivity_detail.png (from c2/c2_rate_ratios.csv + c5_sensitivity.csv). Genuine but minor duplication: each of the three chart scripts re-declares its own palette + save() helper, and the palettes have DRIFTED - make_deck_charts.py uses NAVY #1F3564, the other two use #1a3a5c. Subject overlap only between fig1_enso_contrast.png (modelled + observed) and gen_obs_contrast_detail.png (observed with CIs) - different views of the same C5/C2 data, not the same figure.  === WHICH SCRIPT DOES THE LIVE DECK BUILD CALL? === The live chain is the Makefile, not any in-script orchestration (build_deck.py contains no subprocess call):   make figures -> scripts/extract_notebook_figures.py ; scripts/make_deck_charts.py   (Makefile:12-13)   make deck    -> scripts/build_deck.py                                               (Makefile:16) DEFECT FOUND - the Makefile `figures` target is STALE relative to build_deck.py. build_deck.py asks for six figures that `make figures` cannot produce:   gen_obs_contrast_detail.png, gen_sensitivity_detail.png  -> only generate_enso_figures.py makes them, and it is NOT in the Makefile (and is untracked);   gen_kge_rail.png, gen_enso_contrast.png, gen_ceiling_bound.png -> nothing in scripts/ writes those names at all. I verified by md5 that those three (plus gen_kge_vs_r.png, which build_deck.py does not use but docs/24:420 lists) are BYTE-IDENTICAL copies of figures/report/fig2, fig1, fig4, fig3 respectively - i.e. someone copied+renamed generate_report_figures.py's output into figures/deck/ by hand. figures/deck/ is gitignored, so on a clean clone `make figures && make deck` silently yields "[missing figure: ...]" placeholders on 5-6 slides (build_deck.py:78-82 degrades rather than failing). Fix during reorg: add generate_report_figures.py + generate_enso_figures.py to the Makefile `figures` target and script the four report->deck copies (or have build_deck.py read figures/report/ directly).  === IS extract_notebook_figures.py STILL USED? YES === build_deck.py places 15 notebook-derived PNGs (04_c005_1, 06_c035_4, 06_c046_9, 08_c010_3, 10_c003_1, 11_c003_1, 11_c014_6, 12_c008_1, 12_c011_2, 13_c011_2, 13_c022_5, 13_c032_6, 13_c037_7, 13_c043_9, 14_c018_1, 14_c031_3), figures/deck/ is gitignored, and this is the only regenerator. Keep and keep it first in the chain.  === IS calib_queue_runner.py STILL USED? NO === Its own docstring records both registered queues as finished ("docs29 - COMPLETE, all 10 final .npz on disk"; c2b = the docs/33 §3.3 H2E-S refit, which failed 2 of 3 conditions and closed Phase B a second time). Phase B is CLOSED on H2E; reproduction of the adoption is now `python3.10 src/report_h2e.py` (F = 0.25931). C4.3 sediment calibration does not use it - o5_calibration_profile.py solves alpha in closed form and sweeps beta. Its only live-doc reference is docs/29:51, itself a completed pre-registration read-out. ARCHIVE (its docstring explicitly asks to be kept verbatim so the job list that produced adopted H2E stays readable); do not DELETE.  === TASK 5 - PROPOSED PHASE HOME FOR EACH LOOSE SCRIPT, WITH EVERY PATH REFERENCE TO UPDATE === 1) scripts/build_deck.py -> STAYS AT TOP LEVEL (deliverable builder). Refs if ever moved: Makefile:16 · docs/20_reproduction_guide.md:43 · docs/21_project_state_and_handoff.md:252 · docs/progress_journal.md:38 · docs/agents/journal_housekeeping.md:10,22,27,28 · docs/agents/journal_hygiene.md:62,64. 2) scripts/build_report_pdf.py -> STAYS AT TOP LEVEL (deliverable builder). Refs: docs/agents/PROMPT_report_pdf_desktop.md:15,40 · docs/agents/PROMPT_final_presentation.md:130 · docs/agents/journal_nbc1-nb06-inventory.md:98. Not in the Makefile - add a `report` target. 3) scripts/c2_compute.py -> scripts/c2/ (new dir; C2 observed contrast, docs/34). Refs: docs/48_pi_band_revision.md:97 · docs/agents/journal_commit.md:77,115,154 · docs/agents/journal_refute-gate-logic.md:33. (docs/agents/journal_c2-contrast.md:52 names `scratchpad/c2_compute.py`, a historical pre-copy path - do not rewrite.) 4) scripts/c2_consistency_figs.py -> scripts/c2/. Refs: docs/agents/journal_commit.md:77,116. Note docs/34:447-449 names its three output figures but not the script path. 5) scripts/calib_queue_runner.py -> scripts/archive/ per its ARCHIVE disposition (if you prefer strict phase grouping instead, it is Phase B -> scripts/b/). Refs: docs/29_seed_expansion.md:51 · docs/agents/journal_calibration-launch.md:16,51,56 · docs/agents/journal_commit-2.md:6,23,36 · docs/agents/journal_commit.md:42,123,155 · docs/agents/journal_refit-launch.md:11,122,150 · AND its own docstring line 37, which embeds the launch path 'scripts\\calib_queue_runner.py' in a Start-Process example. 6) scripts/extract_notebook_figures.py -> HAS NO PHASE (it scans all notebooks 01-19). Proposed scripts/deck/ so the deck chain stays in one directory. Refs: Makefile:12 · docs/20_reproduction_guide.md:42 · docs/21_project_state_and_handoff.md:252 · docs/progress_journal.md:37 · docs/agents/journal_housekeeping.md:8,22,23 · docs/agents/journal_hygiene.md:64. 7) scripts/make_deck_charts.py -> content is Phase B, but the chain is the deck. Proposed scripts/deck/ (alternative if you group strictly by phase: scripts/b/, which splits the deck chain across three dirs). Refs: Makefile:13 · docs/20_reproduction_guide.md:42 · docs/21_project_state_and_handoff.md:252 · docs/progress_journal.md:37 · docs/agents/journal_c0.md:248 · docs/agents/journal_housekeeping.md:9,22,25 · docs/agents/journal_hygiene.md:64. 8) scripts/generate_enso_figures.py -> scripts/c5/ (reads c2_rate_ratios.csv + c5_sensitivity.csv; both figures are C2/C5). Path references to update: NONE - it is untracked and appears in zero docs. Commit it and add it to the Makefile in the same move. 9) scripts/generate_report_figures.py -> CROSS-PHASE (fig1 = C5, fig2/3 = C4.3, fig4 = Phase-B ceiling, fig5 = ONI). Proposed: STAYS AT TOP LEVEL beside build_report_pdf.py (it is the report's figure half), or a new scripts/report/ holding both. Path references to update: NONE - zero doc references anywhere, despite producing the five committed figures/report/*.png from commit 3e9b464.  === MOVE CONSTRAINTS (do not break these) === - scripts/c3/qpeak.py is IMMOVABLE: src/mgb_sediment.py:580 builds the literal path `<repo>/scripts/c3/qpeak.py` and raises ImportError if missing; tests/test_qpeak.py:16 does the same. docs/35 §7 records that it lives outside src/ deliberately. - scripts/c5/c5_sensitivity.py:20 does `sys.path.insert(0, REPO/"scripts"/"c3")`, and scripts/c3/{ls2d_variants,ls2d_defect_b}.py insert their own dir to `import ls2d` - scripts/c3/ must keep its name and layout. - src/mgb_sediment.py:246,251,420,460 and docs/42:499, docs/46:182,219,502,548,676,729 quote `scripts/c3/*.py` paths (several with line numbers), so any c3 move is a large doc-rewrite.  === OTHER DEFECTS WORTH A TICKET (all read-only observations, nothing changed) === - scripts/build_report_pdf.py:20,21,57 hardcode `/sessions/epic-sleepy-mendel/mnt/magdalena-mgb-sed/...` - a dead sandbox root; the script cannot run on this box until OUT/EQD/FIGDIR are re-anchored to Path(__file__).resolve().parent.parent (the pattern every other script here uses). PROMPT_report_pdf_desktop.md already warns about this. - scripts/c2b/peaks_measure.py:11 writes OUT to a dead per-session scratchpad path (`.../3d81998f-.../scratchpad`). - scripts/c2_compute.py:6, scripts/c2_consistency_figs.py:8, scripts/c2b/bfi_measure.py:10, scripts/c2b/bfi_figure.py:11 hardcode `c:\dev\magdalena-mgb-sed` instead of anchoring to __file__ - they break on any clone or rename. - scripts/c3/ls_erosion_weights.py:174 prints an untagged `f_area` header; docs/43 §amendment 8 names this as the likely channel by which 0.42147514 entered the corpus as "f_area(V4)", and explicitly assigns the one-word fix (`f_area_urhfrac`) to scripts/'s owner. Still outstanding. - Makefile has no `report` target and no pytest count assertion; `make test` runs `python3.10 -m pytest tests/ -q` (canonical: 154 passed).

### root - 20 live

| path | why it is live |
|---|---|
| `README.md` | Repo front door and the only public-facing overview (tracked) — but BADLY STALE: §5 still says 'calibration not started', sediment 'Blocked', scenario comparison 'Not started', and §6 still asks which El Nino years to use; must be rewritten to the closed-B / C5-complete state, not archived. |
| `CLAUDE.md` | Live agent/contributor contract (tracked); its Phase-C stage table is stale (says C4.3 BLOCKED and 'C5 not started' — C4.3 ran RAILED/EXPLORATORY and C5 is complete per docs/55, docs/56). |
| `CONTRIBUTING.md` | Live setup/pipeline/commit conventions (tracked); one stale line — 'Notebooks 10–14 are generated' should read 10–19 (src/nbgen/make_nb10.py … make_nb19.py). |
| `CITATION.cff` | Live citation metadata (tracked), but every author field and the Fagundes article title are literal 'TODO' placeholders — needs filling before any release. |
| `LICENSE` | MIT licence text (tracked); referenced by pyproject.toml license field. |
| `Makefile` | Live entry points (test / figures / deck / watch) wired to python3.10 and the scripts/ contract — part of the reproduction chain. |
| `pyproject.toml` | Live project metadata; explicitly documents that the repo is run-in-place, not an installable package. |
| `requirements.txt` | Live pinned environment for the pipeline (tracked); missing reportlab + Pillow, which scripts/build_report_pdf.py imports. |
| `environment.yml` | Live conda mirror of requirements.txt (tracked); same reportlab/Pillow gap. |
| `.gitignore` | Live ignore policy (tracked, currently modified-uncommitted to add *.zip + data/raw/colleague_share/); note it does NOT cover _eq/ or .pytest_cache/, which is why both show up as untracked clutter. |
| `.gitattributes` | Live LF normalisation + binary-asset rules (tracked); the fix for the CRLF churn on this Windows box. |
| `watch_calib.py` | Live read-only DDS calibration monitor (Makefile `watch` target; cited by docs/20 §Monitor, docs/21 §316, docs/29 §111) — reads data/processed/_calib_cache, never writes. |
| `progress_map.html` | Designated live status tracker (CLAUDE.md and docs/00_INDEX.md both say the tracker wins for status); tracked. Stale by ~1 week — it knows C4.3/C5/buarque/0.25931 but has no docs/55, docs/56, 18/18 or 3.05x. |
| `MGB-SED_Magdalena_FIGURES.pptx` | THE live deck — the only filename scripts/build_deck.py writes (build_deck.py:19 OUT = REPO / 'MGB-SED_Magdalena_FIGURES.pptx'), and the name every doc cites (docs/20, docs/21, docs/27, docs/28). Gitignored (.gitignore:44 *.pptx), never tracked, regenerable via `make deck`. On-disk copy is itself stale: 23 slides vs the 28 the current script builds — rebuild before any hand-off. |
| `MGB-SED_complete_report.pdf` | Live deliverable (the complete explanatory report); gitignored (.gitignore:15 *.pdf), never tracked, regenerable from scripts/build_report_pdf.py — but that script hardcodes an absolute /sessions/epic-sleepy-mendel/... output path and will not rebuild on this box until the path is made repo-relative. |
| `Explanation_script_MGB_SA_Magdalena.pdf` | Gitignored external reference PDF, but docs/38 §5 already adjudicated it: 'KEEP, do not delete' — actively cited twice by docs/35 (the 30 m window and the MUSLE/q_peak absence). Keep on disk, do not commit. |
| `magdalena_share_for_colleague.zip` | NOT regenerable and load-bearing: the sole on-disk copy of the 2026-08-13 cross-implementation exchange bundle that docs/59 pins its R2 numbers to, 306,200,202 B, 20/20 member hashes verified against data/raw/colleague_share/input_hashes.txt (docs/agents/FIXLIST_docs59.md). Gitignored (.gitignore:52 *.zip), never tracked. Consider relocating under data/raw/colleague_share/ rather than the root. |
| `cds_keys.txt` | Local CDS API secret; gitignored (.gitignore:36) and verified never committed (git log --all returns 0 commits touching it). Never archive, never commit. |
| `.claude/settings.json` | Shared Claude Code project config (tracked, per .gitignore's 'share project config' comment). |
| `.claude/settings.local.json` | Personal local overrides; gitignored by design (.gitignore:41) — local config, keep on disk, never commit. |

*Agent note:* SCOPE: 21 loose files at the repo root, plus the three root directories the brief named (_eq/ 7 files, .pytest_cache/ 5 files, results/ 1 file) and .claude/ (2 files, root-level config). Directories data/, docs/, figures/, notebooks/, scripts/, src/, tests/ belong to other subtrees and are not catalogued here.  TRACKING: exactly 13 root files are tracked (.gitattributes, .gitignore, CITATION.cff, CLAUDE.md, CONTRIBUTING.md, LICENSE, Makefile, README.md, environment.yml, presentation_guide.html, progress_map.html, pyproject.toml, requirements.txt, watch_calib.py — 14 counting watch_calib.py). Everything else at root is gitignored (cds_keys.txt :36, *.pdf :15, *.pptx :44, *.zip :52, Explanation_script_*.pdf :49) EXCEPT _eq/ and .pytest_cache/, which are untracked and unignored — the only two genuine gitignore gaps at root.  THE PPTX QUESTION (answered from source, not mtime): scripts/build_deck.py:19 sets OUT = REPO / "MGB-SED_Magdalena_FIGURES.pptx" and builds slides 2–28 (28 slides). scripts/generate_enso_figures.py writes only PNGs into figures/deck/ — it builds no deck at all. Nothing in the repo mentions "FIGURES_CORRECTED" or "paperdepth"; every doc (docs/20, 21, 27, 28) cites MGB-SED_Magdalena_FIGURES.pptx. Slide counts on disk: FIGURES 23, CORRECTED 23, paperdepth 28. So the live deck is MGB-SED_Magdalena_FIGURES.pptx by name, the other two are orphan one-off renders (DELETE), and the on-disk FIGURES.pptx is itself a stale 23-slide build that must be regenerated with `make deck` before delivery.  SECRET: cds_keys.txt is KEEP (local secret) and is clean — `git log --all -- cds_keys.txt` returns zero commits, so it has never entered history. Same verification run for all five other gitignored root artifacts: none ever tracked.  CANONICAL-STATE MISMATCHES FOUND AT ROOT (all in KEEP files, flagged for rewrite rather than removal): 1. README.md is the worst offender — its status table still reads "calibration not started" (Phase B is CLOSED on H2E, F = 0.25931), sediment "Blocked: no mainstem sediment data" (docs/32 §R6 identified trunk station 21237020; C5 is COMPLETE, 18/18, median 3.05x), scenario comparison "Not started", and §6 still asks the advisor to confirm the El Nino years. Its repository map is also pre-Phase-C (notebooks 01–05 only; 01–19 exist). All the doc paths it links do still exist. 2. CLAUDE.md Phase-C block says "C4.3 ... is BLOCKED" and "C5 not started" — superseded by docs/55 (C4.3 RAILED/EXPLORATORY, estimator (a) median KGE_ln -0.118 / (b) +0.139) and docs/56 (C5 complete). It also does not yet name docs/55–59 or the adopted LS (buarque_2015_dg, f_LS 0.25146 erosion-weighted). 3. CONTRIBUTING.md says notebooks 10–14 are generated; it is 10–19. 4. progress_map.html — designated live tracker but ~1 week behind: has C4.3/C5/buarque/0.25931, has no docs/55, docs/56, 18/18 or 3.05. 5. .pytest_cache/v/cache/ still records the pre-move src/test_mgb_hydrology.py layout and one failing test, both contradicted by the canonical 154-passed state — extra reason to drop it.  TWO REPRODUCIBILITY DEFECTS worth routing to whoever owns scripts/: scripts/build_report_pdf.py hardcodes absolute sandbox paths (OUT, EQD, FIGDIR all under /sessions/epic-sleepy-mendel/mnt/magdalena-mgb-sed/), so MGB-SED_complete_report.pdf cannot currently be rebuilt on this box; and neither requirements.txt nor environment.yml pins reportlab or Pillow, which that script imports.  ONE JUDGEMENT CALL: presentation_guide.html is tracked and a previous hygiene pass called it ESSENTIAL, but it documents a 19-slide deck that no longer exists (the current build_deck.py ends on slides 18–28: Phase C, the railed sediment calibration, the 18/18 contrast, the limits). I marked it ARCHIVE rather than KEEP because it would mislead a presenter today; it has clear audit value as the Phase-B-era narrative and must not be deleted. If instead it is regenerated to match the 28-slide deck, flip it back to KEEP.  ONE BORDERLINE DELETE: results/README.md is the only file in results/ and describes a hydrology/sediment/scenarios layout that was never created. I called it DELETE (tracked, so history retains it), but a reorganisation that wants results/ as the home for MGB-SED_complete_report.pdf and the deck could legitimately KEEP and rewrite it instead. Deleting it also strands .gitignore's results/*.tif and results/*.gtb lines.  NOTHING at root falls under the preserve-superseded-material rule — there are no strike-through blocks, refuted-hypothesis registers or agent journals in this subtree; those all live under docs/.

### notebooks-figures-tests - 44 live

| path | why it is live |
|---|---|
| `notebooks/01_dem.ipynb` | Hand-written (01-09, no src/nbgen/make_nb01.py); didactic DEM->minibacias derivation on the 6x6 toy DEM; 285,318 B, mtime 2026-07-27 01:21:39. |
| `notebooks/02_urh.ipynb` | Hand-written (01-09); URH soil x land-cover crossing and Frechet bounds derivation; 129,980 B, mtime 2026-07-27 01:21:12. |
| `notebooks/03_hydrology.ipynb` | Hand-written (01-09); the derivation of the Python water balance CLAUDE.md names as the Phase B diagnostic; 159,887 B, mtime 2026-07-27 21:55:05. |
| `notebooks/04_real_dem_eda.ipynb` | Hand-written (01-09); records why the lower-Magdalena box cannot be delineated (a live constraint); 459,614 B, mtime 2026-07-28 05:51:54. |
| `notebooks/05_landcover_soils_reclass.ipynb` | Hand-written (01-09); WorldCover + IGAC reclass into the real URH map (Phase A input chain); 384,974 B, mtime 2026-08-02 09:04:58. |
| `notebooks/06_data_inventory.ipynb` | Hand-written (01-09); source of the station inventory numbers and of the docs/35-cited Explanation_script PDF; 858,473 B, mtime 2026-07-29 03:47:51. |
| `notebooks/07_preprocessing_minibacias.ipynb` | Hand-written (01-09); the 8,672-minibacia preprocessing step of the Phase A chain; 1,310,135 B, mtime 2026-07-30 18:57:59. |
| `notebooks/08_urh.ipynb` | Hand-written (01-09); real-data URH build (24 types) feeding soil params; 223,227 B, mtime 2026-07-30 21:51:03. |
| `notebooks/09_soil_parameters.ipynb` | Hand-written (01-09); Wm/K soil parameters, last hand-written link before the generated 10-19 chain; 889,535 B, mtime 2026-07-30 22:28:48. |
| `notebooks/10_rainfall_dataset_comparison.ipynb` | GENERATED by src/nbgen/make_nb10.py (never hand-edit); rainfall dataset comparison; 512,748 B, mtime 2026-08-12 11:45:18. |
| `notebooks/11_rainfall_pet_forcing.ipynb` | GENERATED by src/nbgen/make_nb11.py; builds the v2 gauge-only forcing (its prose still uses the older CHIRPS-inclusive sense - code wins); 1,795,641 B, mtime 2026-08-12 11:45:19. |
| `notebooks/12_model_input_assembly.ipynb` | GENERATED by src/nbgen/make_nb12.py; assembles model_inputs_v2/ (registers a 7,200 s timeout - do not casually re-execute); 1,447,468 B, mtime 2026-08-12 11:49:10. |
| `notebooks/13_baseline_run.ipynb` | GENERATED by src/nbgen/make_nb13.py; the baseline water-balance run behind sim_baseline_v2/; 2,784,744 B, mtime 2026-08-12 11:49:11. |
| `notebooks/14_calibration.ipynb` | GENERATED by src/nbgen/make_nb14.py; the discharge calibration that produced the adopted H2E fit (F = 0.25931); 387,304 B, mtime 2026-08-12 11:49:11. |
| `notebooks/15_ssc_quality_gate.ipynb` | GENERATED by src/nbgen/make_nb15.py; the C1 deliverable (79/79 SSC stations classified, 18 usable) behind docs/32; 2,876,852 B, mtime 2026-08-12 11:49:12. |
| `notebooks/16_observed_enso_contrast.ipynb` | GENERATED by src/nbgen/make_nb16.py; the C2 model-free observed contrast (22/22 station ratios) behind docs/34; 2,643,078 B, mtime 2026-08-12 11:49:12. |
| `notebooks/17_runoff_signatures.ipynb` | GENERATED by src/nbgen/make_nb17.py; the C2b runoff/BFI/peak signatures where H-PEAK was refuted (docs/33); 2,565,077 B, mtime 2026-08-12 11:49:13. |
| `notebooks/18_musle_construction.ipynb` | GENERATED by src/nbgen/make_nb18.py; MUSLE hillslope erosion construction (C3) - see notes, its generator was edited in a LATER commit than this notebook; 1,871,384 B, mtime 2026-08-12 12:39:42. |
| `notebooks/19_c3_gate_and_c4_setup.ipynb` | GENERATED by src/nbgen/make_nb19.py; the C3 gate and C4 setup, newest notebook in the tree; 1,883,373 B, mtime 2026-08-13 03:44:09. |
| `notebooks/README.md` | The notebook index, but STALE: it describes only 01-05 and never mentions 06-19 or the src/nbgen generation rule for 10-19; 1,477 B, mtime 2026-07-28 05:45:18. |
| `figures/01_dem.png` | Tracked hand-saved export from notebook 01 (initial commit 591e0f5); the toy-DEM illustration for the didactic chain; 43,248 B. |
| `figures/urh_croisement.png` | Tracked hand-saved export from notebook 02 (initial commit 591e0f5); the URH crossing illustration; 47,915 B. |
| `figures/igac_vs_soilgrids.png` | Tracked; the sole visual evidence for the IGAC-vs-SoilGrids coverage decision, explicitly cited by docs/38 §206 as where those percentages live; 128,788 B. |
| `figures/precip_auto_viability.png` | Tracked evidence image from the automatic-precip-station viability screen (commit 81ae293); records how that rejection was reached, so never DELETE; 110,892 B. |
| `figures/precip_auto_vs_dhime.png` | Tracked evidence image comparing automatic stations against DHIME (commit 81ae293); part of the input-QC audit trail; 112,450 B. |
| `figures/precip_gauges_coverage.png` | Tracked coverage map of the DHIME precip gauge network underpinning the v2 forcing; 136,899 B. |
| `figures/deck/yb_1_availability.png` | NOT regenerable from this repo - it comes from the team's second-implementation repo (docs/20 §42, the old n*.png), yet lives only inside the gitignored figures/deck/ and is consumed by scripts/build_deck.py; mtime 2026-08-03 05:42. |
| `figures/deck/yb_2_enso_contrast.png` | Second-implementation-repo figure, not rebuildable here; explicitly placed by scripts/build_deck.py:169; at risk because figures/deck/ is gitignored; mtime 2026-08-03 05:42. |
| `figures/deck/yb_3_rating_curves.png` | Second-implementation-repo figure (rating curves), not rebuildable here; only copy is in a gitignored directory; mtime 2026-08-03 05:42. |
| `figures/deck/yb_demo_synthetic_overview.png` | Second-implementation-repo figure, not rebuildable here; placed by scripts/build_deck.py:431; mtime 2026-08-03 05:42. |
| `figures/deck/yb_rs_retrieval_sentinel2_msi_nir.png` | Second-implementation-repo remote-sensing figure, not rebuildable here; placed by scripts/build_deck.py:429; mtime 2026-08-03 05:42. |
| `figures/report/fig1_enso_contrast.png` | Tracked; the headline C5 figure (modelled vs observed ENSO ratios per station), consumed by scripts/build_report_pdf.py:383; regenerated by scripts/generate_report_figures.py; 119,013 B, mtime 2026-08-13 07:04. |
| `figures/report/fig2_kge_alpha_rail.png` | Tracked; documents the C4.3 alpha rail (alpha wants ~0.48, below the box floor), used at build_report_pdf.py:310; 86,069 B, mtime 2026-08-13 07:04. |
| `figures/report/fig3_kge_vs_r.png` | Tracked; the KGE-vs-r evidence for the structural ceiling, used at build_report_pdf.py:332; 55,526 B, mtime 2026-08-13 07:04. |
| `figures/report/fig4_ceiling_bound.png` | Tracked; the docs/58 rainfall-ceiling bound (<= +0.006 r), used at build_report_pdf.py:412; 78,784 B, mtime 2026-08-13 07:04. |
| `figures/report/fig5_oni_windows.png` | Tracked; the NOAA ONI window figure recording the neutral-core CAL window, used at build_report_pdf.py:359; 89,772 B, mtime 2026-08-13 07:04. |
| `tests/conftest.py` | Live: the only thing putting src/ on sys.path for the whole suite - deleting it breaks every test; 212 B, 0 test functions. |
| `tests/test_dhime_dates.py` | Live; CI-facing subset of the DHIME date-parsing smoke tests, incl. the refusals that must RAISE rather than guess; ~7 test functions. |
| `tests/test_engine_mass_balance.py` | Live; pins the water-balance budget identity to residual_relative < 1e-10 on a synthetic 3-minibacia chain; ~2 test functions (1 parametrized). |
| `tests/test_forcing_npy.py` | Live; guards the truncation defect where pd.read_csv silently returned a prefix of a 180 MB forcing CSV (docs/16 trap); ~4 test functions. |
| `tests/test_idw_order_invariance.py` | Live; regression test for the order-dependent IDW fixed in docs/23 §11, using the exact co-located-gauge tie that caused it; ~2 test functions. |
| `tests/test_qpeak.py` | Live; pins the registered MUSLE q_peak proxy and the numbers docs/35 quotes (bias column, scale table, C4 hard stops); ~30 test functions; 10,817 B. |
| `tests/test_sediment.py` | Live; engine-grade gates for src/mgb_sediment.py from docs/31 §C3.4, updated by commit 7be2ce6 for the adopted V4_dg LS default; ~37 test functions (6 parametrized); 42,732 B. |
| `tests/test_transport.py` | Live; the five C4.1 channel-transport gates incl. exact mass conservation at k_dep = 0; ~43 test functions (1 parametrized); 28,231 B. |

*Agent note:* SCOPE: 147 files on disk (notebooks 21, figures top-level 6, figures/deck 96, figures/report 5, results 1, tests 8 .py + 10 .pyc). 49 entries: every file is individually catalogued except figures/deck's 71 notebook-extracted PNGs and 20 gen_*.png, aggregated as instructed, and tests/__pycache__ (10 .pyc). Working tree is clean for all of notebooks/, figures/, results/, tests/.  NOTEBOOKS. All 19 are KEEP. 01-09 are hand-written: src/nbgen/ contains make_nb10.py .. make_nb19.py ONLY, so there is no generator for 01-09 and those .ipynb files ARE the source of truth. 10-19 are generated - never hand-edit them; edit the generator, re-run it, then nbconvert.  DRIFT FLAG (nb18). Commit 7be2ce6 ("c3.1 ACT 2: switch engine default LS to adopted source field V4_dg") modified src/nbgen/make_nb18.py and make_nb19.py but did NOT re-commit notebooks/18_musle_construction.ipynb. On disk make_nb18.py is 2026-08-12 16:00 while the notebook is 2026-08-12 12:39 - the generator is 3.5 h NEWER than its output. nb19 was regenerated after (2026-08-13 03:44 vs generator 03:43), so only nb18 is suspect. Both files do contain V4_dg/buarque_2015_dg strings, so this is a "verify before quoting nb18 numbers" flag, not a proven mismatch. CLAUDE.md's "verified 2026-08-12, generators reproduce their committed notebooks source-identically" may predate that 16:00 edit. I did not re-run any generator (read-only).  notebooks/README.md is KEEP but materially stale: it documents notebooks 01-05 only, says nothing about 06-19, and does not mention the "10-19 are generated, never hand-edit" rule that CLAUDE.md treats as load-bearing. Worth rewriting during the reorg rather than archiving.  notebooks/06_data_inventory.html is NOT a stale export - it is a faithful, content-current nbconvert dump of the tracked .ipynb (54 cells vs 54 jp-Cell divs, same run strings, exported 7 h after the notebook). It is nonetheless DELETE: untracked, gitignored, no automated producer, nothing depends on it. Keep the distinction: the PDF Explanation_script_MGB_SA_Magdalena.pdf, also derived from this notebook, IS cited twice by docs/35 and must stay - do not confuse the two artifacts. This matches the prior adjudication in docs/agents/journal_nbc1-nb06-inventory.md §1.  FIGURES/DECK - THE ONE REAL RISK. figures/deck/ is gitignored (.gitignore "regenerable presentation artifacts"), but it is NOT 100% regenerable. 91 of 96 files rebuild (71 via scripts/extract_notebook_figures.py, 20 gen_*.png via make_deck_charts.py / c2_consistency_figs.py / c2b/bfi_figure.py / c3/ls2d.py / generate_enso_figures.py). The 5 yb_*.png come from the team's second-implementation repo (docs/20 §42 calls them the n*.png, "the one input not rebuildable from this repo alone"). Deleting figures/deck/ wholesale destroys them. RECOMMENDATION: move the 5 yb_*.png to a tracked location (e.g. figures/external/) and add a .gitignore exception before any cleanup; scripts/build_deck.py references them at lines 169, 429, 431 and would need its path updated.  The 71 extracted deck PNGs are stale by two generations: all stamped 2026-08-03 20:44, i.e. before notebooks 10-19 were regenerated on 2026-08-12/13, and there is not a single figure from notebooks 15-19 (the whole C1/C2/C2b/C3 body of work) in the deck. extract_notebook_figures.py has no notebook filter, so re-running it would add them. Any deck rebuilt from the current directory is showing pre-C-phase figures.  FIGURES/REPORT is regenerated by scripts/generate_report_figures.py ("No fit, no engine run - reads the JSON/CSV outputs only"), which reads data/processed/c5_enso_contrast.json et al. and writes the 5 fig*.png; they are consumed by scripts/build_report_pdf.py. Unlike deck, these 5 ARE tracked in git. Adjacent defect for the scripts/ agent: build_report_pdf.py:57 hardcodes FIGDIR = /sessions/epic-sleepy-mendel/mnt/magdalena-mgb-sed/figures/report - a dead sandbox path that will not resolve on this box.  FIGURES top level: 6 tracked PNGs, none produced by any current script (no src/*.py or scripts/*.py writes to figures/ root) - they are hand-saved exports carried since commits 591e0f5 and 81ae293. All KEEP: igac_vs_soilgrids.png is the named evidence for docs/38's coverage percentages, and the three precip_* images record how the automatic-station rejection was reached. Verified none is a byte-duplicate of anything in figures/deck/.  RESULTS/ is no longer meaningful. It holds only README.md, which promises hydrology/, sediment/, scenarios/ subdirectories that were never created; .gitignore still carries results/*.tif and results/*.gtb rules for content that does not exist. The real artifacts went to data/processed/ and figures/report/. ARCHIVE rather than DELETE only because the README records the original output plan - if the reorg drops the directory, note that the repo-root README.md:119 tree diagram still lists it and would need editing.  TESTS. 8 tracked files. rg "^def test_\|^    def test_" counts: conftest 0, dhime_dates 7, engine_mass_balance 2, forcing_npy 4, idw_order_invariance 2, qpeak 30, sediment 37, transport 43 = 125 function definitions. That does not equal the canonical 154 passed, and both numbers are consistent: (a) src/test_mgb_hydrology.py, OUTSIDE this subtree, adds 13 more defs (138 total), and (b) 8 @pytest.mark.parametrize decorators (1 engine_mass_balance, 6 sediment, 1 transport) expand at collection. There is no pytest.ini / pyproject testpaths config, so a bare `python3.10 -m pytest -q` from the repo root discovers src/test_mgb_hydrology.py as well - whoever owns the src/ subtree should be told that file is part of the 154.

### src - 39 live

| path | why it is live |
|---|---|
| `src/mgb_hydrology.py` | LIVE ENGINE, byte-frozen: the Phase B water balance (F = 0.25931); its SHA is recorded in sim_calibrated_v2/calibration_v2.json. Imported by calib_v2, mgb_drivers, report_h2e, build_h2e_drivers, mgb_transport, nbgen/make_nb13, make_nb14, tests/test_engine_mass_balance.py and src/test_mgb_hydrology.py. |
| `src/mgb_sediment.py` | LIVE ENGINE (MUSLE hillslope, stage C3.4) and it carries the CANONICAL adopted state: ls2d_column default 'V4_dg' / ls_formulation buarque_2015_dg, DEFAULT_CP_REVISION 'cited_central_2026_08_11' → 299.5387 Mt/yr. Imported by nb18, nb19, tests/test_sediment.py, tests/test_transport.py, scripts/c3/*, scripts/c4/o5_calibration_profile.py, scripts/c5/*. |
| `src/mgb_transport.py` | LIVE ENGINE (channel advection + k_dep deposition, stage C4.1); deliberately separate from mgb_sediment so the hillslope/channel boundary that the double-counting warning rests on stays true. Imported by nb19, nbgen/make_nb19.py, tests/test_transport.py; cited by docs/42, 45, 47, 51, 53, 54, 59. |
| `src/mgb_drivers.py` | LIVE: drives mgb_hydrology's own private kernels from outside to record per-minibacia daily fields without editing the frozen engine, and verify_against_engine asserts bit-identity. Imported by build_h2e_drivers.py; cited by docs/26. |
| `src/calib_v2.py` | LIVE: the frozen H1/H2/H2E objective, bounds, recession and peak terms plus the DDS worker entry point (module-level so Windows `spawn` can import it). Imported by nb14, nb17, report_h2e.py, build_h2e_drivers.py, baseflow.py, scripts/calib_queue_runner.py; named in docs/20 §2 as the ensure_cache() rebuild route. |
| `src/report_h2e.py` | LIVE reproduction gate: `python3.10 src/report_h2e.py` re-evaluates the archived best x and must match F = 0.25930593639066796 to 1e-8 — the standing verification bar in docs/20 §4 and CLAUDE.md. Consumed by nb15, cited by docs/00, 20, 37, 46, 52, 53. |
| `src/build_h2e_drivers.py` | LIVE pipeline stage C0.5: writes sim_calibrated_v2/h2e_drivers.npz (546 MB), the frozen sediment drivers mgb_sediment reads so sediment evaluation never re-runs hydrology. Named as the regeneration command in docs/20 §2 and §3-F. |
| `src/baseflow.py` | LIVE: the Eckhardt BFI filter frozen by docs/33 §2.1 (the H-BFI gate, which HELD). Imported by scripts/c2b/bfi_measure.py, nbgen/make_nb17.py; cited by nb03, nb06, nb13, nb15, nb17, nb18, docs/33. |
| `src/forcing_npy.py` | LIVE and load-bearing: the .npy path that exists because pd.read_csv SILENTLY truncates the 180 MB wide forcing CSVs (docs/18 trap 19). Imported by nb12, nb13, calib_v2, mgb_sediment, merge_chirps_gauges, tests/test_forcing_npy.py. |
| `src/idw_forcing.py` | LIVE pipeline stage: the ONE deterministic gauge→minibacia interpolator (lexsort tie-break + co-located-gauge handling) that produced the adopted v2 forcing. Imported by nb11, nbgen/make_nb11.py, merge_chirps_gauges, tests/test_idw_order_invariance.py; cited by docs/23 §11. |
| `src/dhime_dates.py` | LIVE shared utility: evidence-based DHIME date-format detection that raises rather than guessing (the ssc_cundinamarca DD/MM/YYYY transposition trap). Imported by build_precip_gauges, build_discharge_gauges, build_sediment_gauges and tests/test_dhime_dates.py (7 tests). |
| `src/organize_precip_regions.py` | LIVE pipeline stage A-1: consolidates the raw DHIME precipitation downloads into regions/ that build_precip_gauges.py reads. Listed in CLAUDE.md Pipeline commands and docs/20 §3. |
| `src/build_precip_gauges.py` | LIVE pipeline stage A-2 (precip QC v1, value screens + catalogue join). Listed in CLAUDE.md Pipeline commands and docs/20 §3; consumed by nb10, nb11. |
| `src/repair_precip_zero_suppression.py` | LIVE pipeline stage A-3 and REQUIRED (docs/16 §4.1): repairs the 70 rain-only gauge series that no value screen can see; produces the `_qc` files whose 'Inferido_seco' approval flag the whole project depends on. In CLAUDE.md Pipeline commands and docs/20 §3. |
| `src/repair_precip_selectivity.py` | LIVE pipeline stage A-4 (QC v3, selectivity detector → 153 stations / 240,158 inferred-dry days) writing precip_gauges_daily_qc_v2.csv — the file nb11 actually reads and the ADOPTED v2 forcing is built from. In docs/20 §3, but MISSING from CLAUDE.md's Pipeline commands block (see notes). |
| `src/build_discharge_gauges.py` | LIVE pipeline stage B: consolidates all DHIME discharge exports into discharge_daily.csv + inventory. In CLAUDE.md Pipeline commands and docs/20 §3-B; owning audit docs/17. |
| `src/build_sediment_gauges.py` | LIVE pipeline stage G: builds sediment_daily.csv / sediment_inventory.csv, the input to the C1 SSC quality gate (docs/32, nb15 — 79/79 stations classified, 18 usable). In docs/20 §3-G; owning audit docs/19. |
| `src/build_soil_layer.py` | LIVE Phase A stage: the IGAC-primary / SoilGrids-gap-fill texture-family raster that the URH crossing rests on. Consumed by notebooks 05, 08, 09; cited by docs/38 §4. |
| `src/fix_gauge_minibacia_mapping.py` | LIVE repair stage: re-snaps discharge gauges by runoff-coefficient matching after docs/17 found the point-in-cell snap corrupted 54–79 of 159 stations (the ARMENIA 652,196 mm/yr case). Consumed by nb12 and build_sediment_gauges.py; cited by docs/23. |
| `src/fetch_station_coords.py` | LIVE Phase A acquisition: pulls discharge-station (lon, lat) from the IDEAM Socrata catalogue into stations_discharge_coords.csv, used by notebook 07 step 6 and referenced by build_sediment_gauges.py, docs/17, docs/19, docs/31. |
| `src/download_precip_automatic.py` | LIVE for its catalogue half: dump_inventory() writes stations_precip_catalog.csv, which build_precip_gauges.py hard-codes as CATALOG. Its 10-min automatic series was measured and rejected in favour of DHIME conventional (docs/38 §1.4) — the rejection is documented, the script still produces a consumed artefact. |
| `src/download_igac_soils.py` | LIVE Phase A acquisition: ArcGIS-REST pull of the 18 department UCS soil surveys; docs/38 §4 records that it SUPERSEDES the QGIS click-path in docs/08. Feeds build_soil_layer.py and notebook 05. |
| `src/download_soilgrids.py` | LIVE Phase A acquisition: fetches the 5 SoilGrids rasters on the corrected domain box (docs/15). Only self-cited by module name, but its outputs (soilgrids_*.tif) still gap-fill ~14 % of the basin in build_soil_layer.py and are read by notebooks 05, 06, 08, 09 — so it is a live stage, not an orphan. |
| `src/download_era5.py` | LIVE Phase A acquisition: resumable multi-key CDS pull of the 108 ERA5-Land basin-box months. In docs/20 §3-C; cited by docs/08, docs/16, docs/era5_download_checklist.md, notebook 06. |
| `src/download_era5_strip.py` | LIVE Phase A acquisition: the east-strip extension (lon −72.8..−72.3) that fixes the clipped upper Sogamoso/Chicamocha. In docs/20 §3-C; owning doc docs/15 domain correction. |
| `src/mosaic_era5.py` | LIVE Phase A stage: concatenates basin + east strip into era5land_ext_*.nc, the domain the model actually uses. In CLAUDE.md Pipeline commands and docs/20 §3-C; consumed by nb11. |
| `src/download_chirps.py` | LIVE acquisition: basin-subset CHIRPS yearly netCDFs. Still consumed by notebook 10 (the live rainfall dataset comparison) and named in CLAUDE.md Pipeline commands and docs/20 §3-C — it survives even though the CHIRPS MERGE below did not. |
| `src/test_mgb_hydrology.py` | LIVE regression suite for the frozen engine (13 tests) and a named verification bar in docs/20 §4. VERIFIED by `pytest --collect-only -q`: bare `python3.10 -m pytest -q` from the repo root collects 154 tests, 13 of them from this file — so it IS collected, and it accounts for the canonical 154 exactly. RECOMMEND MOVING to tests/test_mgb_hydrology.py (see notes for the one line that must change). |
| `src/build_data_final.py` | LIVE packaging tool, but EXCLUDED FROM THE REPO by .gitignore:19 (untracked on disk only). It is the named rebuild command for data_Final/ + data_Final.zip in docs/20 §2, and is cited by docs/16 and docs/59 — so it is live even though git cannot see it. A reorganization that 'moves' it will not show up in git. |
| `src/nbgen/make_nb10.py` | LIVE generator of notebooks/10_rainfall_dataset_comparison.ipynb; CLAUDE.md forbids hand-editing nb10-19 and records (2026-08-12) that every generator reproduces its committed notebook source-identically. In docs/20 §3-D. |
| `src/nbgen/make_nb11.py` | LIVE generator of notebooks/11_rainfall_pet_forcing.ipynb — the IDW rainfall + PET forcing that produced the adopted v2 fields; imports idw_forcing as the ONE interpolator. In docs/20 §3-D. |
| `src/nbgen/make_nb12.py` | LIVE generator of notebooks/12_model_input_assembly.ipynb → data/processed/model_inputs_v2/. Re-execution registers a 7,200 s timeout (CLAUDE.md) — cost check before regenerating. |
| `src/nbgen/make_nb13.py` | LIVE generator of notebooks/13_baseline_run.ipynb (sim_baseline_v2, 2008 warm-up / 2009-2018 scored); imports mgb_hydrology. 7,200 s registered timeout. |
| `src/nbgen/make_nb14.py` | LIVE generator of notebooks/14_calibration.ipynb (the H1/H2 pre-registered cells); imports calib_v2 and mgb_hydrology. 28,800 s registered timeout — do not casually re-execute. |
| `src/nbgen/make_nb15.py` | LIVE generator of notebooks/15_ssc_quality_gate.ipynb — stage C1, the 79/79 SSC station classification (18 usable) that docs/32 owns. |
| `src/nbgen/make_nb16.py` | LIVE generator of notebooks/16_observed_enso_contrast.ipynb — stage C2, the model-free observed contrast (22/22 station-ratios) that docs/34 owns and that C5 had to reproduce. |
| `src/nbgen/make_nb17.py` | LIVE generator of notebooks/17_runoff_signatures.ipynb — stage C2b; imports baseflow (frozen Eckhardt) and calib_v2 (frozen objective). Owning read-out docs/33 §6-§8. |
| `src/nbgen/make_nb18.py` | LIVE generator of notebooks/18_musle_construction.ipynb; imports mgb_sediment. The MUSLE construction behind docs/37 and its four amendments. |
| `src/nbgen/make_nb19.py` | LIVE generator of notebooks/19_c3_gate_and_c4_setup.ipynb; imports mgb_sediment and mgb_transport. The C3 gate / C4 setup notebook behind docs/43, 45, 47. |

*Agent note:* SUBTREE SHAPE: 44 entries = src/README.md + 31 top-level .py + 10 src/nbgen/make_nb*.py + 2 __pycache__ dirs (listed at directory level as instructed; their 15 member files are enumerated in the reasons). Verdict split: 39 KEEP, 2 ARCHIVE, 3 DELETE. src/ is the healthiest subtree in the repo — no *_backup.*, no *_ORIG_*, no .bak/.orig/.tmp/editor cruft, no zero-byte files, and md5sum across all 42 source files shows ZERO byte-duplicates. Nothing here is uncommitted: `git status` is clean for src/.  (1) PYTEST COLLECTION — ANSWERED BY MEASUREMENT, NOT INFERENCE. pyproject.toml has NO [tool.pytest.ini_options] (no testpaths, no rootdir config); the only conftest.py in the repo is tests/conftest.py, which just prepends src/ to sys.path. I ran `python3.10 -m pytest --collect-only -q` from the repo root: **154 tests collected**, of which **13 are src/test_mgb_hydrology.py::test_01_mass_balance … test_12_adversarial**. So (a) src/test_mgb_hydrology.py IS collected by the bare command, and (b) it is exactly what makes the canonical 154 add up (141 under tests/ + 13 in src/).  (2) THE MAKEFILE SILENTLY DROPS THOSE 13. `make test` runs `$(PY) -m pytest tests/ -q` — that path collects 141 and never touches the engine's regression suite. So the project's own convenience target is NOT the command the canonical 154 came from. Whoever reorganizes should reconcile these two, not just move the file.  (3) MOVE RECOMMENDATION (concrete). Move src/test_mgb_hydrology.py → tests/test_mgb_hydrology.py. It does NOT duplicate anything under tests/: tests/test_engine_mass_balance.py is a 2-test parametrized budget check on a 3-minibacia synthetic chain, while the src file is a 13-test suite (real-topology performance, restart/warm-up, numpy-vs-numba router equality, a verbatim scalar transcription of notebook 03 cell 7, adversarial inputs) that prints every number it rests on. Overlap is intent-level only. ONE LINE MUST CHANGE on the move: line 24 is `sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))` — self-directory. Under pytest that becomes redundant (tests/conftest.py already inserts src/), but docs/20 §4 names `python src/test_mgb_hydrology.py` as a standing verification bar and the file has a real `if __name__ == "__main__": raise SystemExit(main())` runner, so the insert must be rewritten to `pathlib.Path(__file__).resolve().parents[1] / "src"` or the standalone invocation breaks. Also update the four docs that cite the old path (docs/18, 19, 20 §4, and nbgen/make_nb13.py's prose).  (4) THE TWO GITIGNORED MODULES — CONFIRMED. `git check-ignore -v` reports .gitignore:19 → src/build_data_final.py and .gitignore:20 → src/package_precip_dataset.py, under the comment "Generated bundles, reference documents and their packaging scripts — not project source". `git ls-files src` confirms both are absent from the index: they exist ONLY on this disk. Consequence for the reorganization: any move/delete of these two is invisible to git and unrecoverable from history. I split them because their evidence differs — build_data_final.py is the named rebuild command in docs/20 §2 and is cited by docs/16 and docs/59, so it is live; package_precip_dataset.py is the ONLY module in src/ with zero external citations of any kind (nothing imports it, no doc names it) and is therefore the one clean ARCHIVE candidate under the stated rule.  (5) ONE DOC/CODE MISMATCH FOUND (not a src defect, but it lives on this boundary). CLAUDE.md's "Pipeline commands" block lists organize_precip_regions → build_precip_gauges → repair_precip_zero_suppression and stops; it OMITS `python src/repair_precip_selectivity.py`. docs/20 §3-A does list it as "QC v3: selectivity detector; 153 stations, 240,158 inferred-dry days", and the adopted v2 forcing demonstrably reads its output: nb11 line 73 is `pd.read_csv(proc/'precip_gauges_daily_qc_v2.csv')` and repair_precip_selectivity.py line 229 is the only writer of that file. Anyone following CLAUDE.md alone rebuilds a DIFFERENT (v1-repair) forcing and will not reproduce H2E. Fix belongs in CLAUDE.md, not in src.  (6) WHY merge_chirps_gauges.py IS ARCHIVE AND NOT DELETE. It is imported by nothing (`rg` over src/scripts/tests finds only its own `Run:` line), it is absent from the docs/20 §3 chain, and its hypothesis is refuted — but it is precisely the code behind a documented verdict (LOOCV 0.447 PASS / volume 2,188.5 vs [2,016.0, 2,056.8] FAIL, re-run bit-identical, diagnosed cause wrong). Under the house rule that superseded ≠ irrelevant, it must stay reachable; only its shelf changes. Note it imports forcing_npy and idw_forcing, so archiving it must not break those two live modules (it is the dependent, not the dependency).  (7) MODULES THAT LOOK ORPHANED BY NAME-GREP BUT ARE NOT — I checked each rather than trusting the grep. download_soilgrids.py is cited by no doc by filename, but its soilgrids_*.tif outputs still gap-fill ~14 % of the basin inside build_soil_layer.py and are read by notebooks 05/06/08/09. download_precip_automatic.py's 10-minute series was measured and rejected (docs/38 §1.4), but its dump_inventory() writes stations_precip_catalog.csv, which build_precip_gauges.py hard-codes as its CATALOG constant. Both are KEEP on artefact evidence, not on citation evidence.  (8) NO STALE-STATE CONTRADICTIONS IN THE ENGINE CODE. I spot-checked mgb_sediment.py against the canonical state: its `load_geometry` default is cp_revision='cited_central_2026_08_11' (248.730 → 299.539 Mt/yr, ×1.2043), ls2d_column defaults to "V4_dg" with the comment "ADOPT-SOURCE buarque_2015_dg (docs/37 A3, docs/54)", and f_LS 0.25146 appears as the erosion-weighted point. The engine matches the adopted state; no src module quotes a load without its convention and cp_revision.  (9) SIZE FLAG (informational, no disposition change). Four files exceed the project's 800-line guidance: mgb_sediment.py 1,583, test_mgb_hydrology.py 1,270, mgb_hydrology.py 1,128, calib_v2.py 1,044 — plus every nbgen generator (make_nb17.py is 3,434 lines). mgb_hydrology.py is byte-frozen with its SHA on record, so it MUST NOT be split; the generators are literal notebook source and splitting them would break the source-identical reproduction property CLAUDE.md relies on. Flagging only so a reorganization does not "tidy" these.

### docs/agents - 21 live

| path | why it is live |
|---|---|
| `docs/agents/PROMPT_repo_reorganization.md` | The prompt currently being executed (TASK 0 authorship rewrite through TASK 5); mtime 2026-08-19, the live work order. |
| `docs/agents/PROMPT_notebook_coherence.md` | PENDING — only its Phase 1 (T1 read-only audit) ran, producing the 16 journal_nbc1-* files on 2026-08-13; Phase 2 (edit the 10 generators, re-execute, re-verify source-identity) never ran, no commit touches notebooks/ or src/nbgen/ after be8a62a; it is the spec reorg TASK 2 builds on. |
| `docs/agents/PROMPT_report_pdf_desktop.md` | PENDING — measured unfixed: §5 issue 8 (scripts/build_report_pdf.py:20,21,57 still hold /sessions/epic-sleepy-mendel/ sandbox paths), issue 2 ('57% accuracy' at :123), issue 1 ('matching independent measurements (~3–5×)' at :131), issue 6 (no zero-suppression/refuted-hypothesis beats anywhere in the script); the root PDF is a pre-finish draft. |
| `docs/agents/PROMPT_final_presentation.md` | PARTLY PENDING — TASK 1 (ONI), 2 (deck ending), 3 (adopted-config figures), 4 (estimator (b)) are committed as 9fae1ad/5edfcc9/3112689, but TASK 5 (apply FIXLIST_docs59) is verifiably not done; also the only place the 08-13 state table is written down. |
| `docs/agents/FIXLIST_docs59.md` | UNDISCHARGED pre-verified work order — A1 unapplied (docs/59:501 still grades K 'YES — the one suspect the argument reaches' and 'both implementations read the same numbers') and A2 unapplied (docs/59:169 still says 'no bundle archive is present under data/raw/'). |
| `docs/agents/journal_nbc1-defaults.md` | Notebook-coherence T1 audit: the single authority on which notebooks' executed outputs are stale after the ACT 2 move to V4_dg — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-sweep.md` | Notebook-coherence T1 mechanical kill-list sweep over all 19 notebook extracts plus generator-vs-notebook divergence — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-nb01-03.md` | T1 coherence audit of hand-written nb01 (DEM) and nb03 (hydrology) — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-nb02-08-urh.md` | T1 coherence audit adjudicating the nb02/nb08 URH structural duplicate — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-nb04-05.md` | T1 coherence audit of nb04 (DEM EDA) and nb05 (land cover/soils) — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-nb06-inventory.md` | T1 coherence audit of nb06 data inventory plus the stray notebooks/06_data_inventory.html — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-nb07-09.md` | T1 coherence audit of nb07 (preprocessing) and nb09 (soil parameters) — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-nb10-11-forcing.md` | T1 coherence audit of nb10/nb11 forcing, where nb11's prose contradicts its own code on the v2 definition — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-nb12-assembly.md` | T1 coherence audit of nb12 model-input assembly against make_nb12.py — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-nb13-baseline.md` | T1 coherence audit of nb13 baseline run, adjudicating the mechanical sweep hits — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-nb14-calibration.md` | T1 coherence audit of nb14 calibration (the 8 h re-execute) — active input to reorg TASK 2 and to its cost decision. |
| `docs/agents/journal_nbc1-nb15-ssc.md` | T1 coherence audit of nb15 SSC quality gate (85 cells) against docs/32 — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-nb16-observed.md` | T1 coherence audit of nb16 observed ENSO contrast against docs/34 — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-nb17-runoff.md` | T1 coherence audit of nb17 runoff signatures against frozen docs/33 — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-nb18-musle.md` | T1 coherence audit of nb18 MUSLE construction for kill-list numbers, yield embargo and missing cp_revision — active input to reorg TASK 2. |
| `docs/agents/journal_nbc1-nb19-c3gate.md` | T1 coherence audit of nb19 C3 gate / C4 setup against make_nb19.py — active input to reorg TASK 2. |

*Agent note:* SUBTREE TOTALS: 157 files (not ~95). 21 KEEP, 136 ARCHIVE, 0 DELETE. Breakdown: 4 PROMPT_*, 1 FIXLIST, 1 handoff_*, 1 review_*, 150 journal_*. Every journal is ARCHIVE-or-KEEP; none proposed for deletion, per the hard rule.  DESTINATION: docs/archive/agents/ for all 136 ARCHIVE entries. Recommend preserving the flat filenames (several docs cite journals by exact path).  === MEASURED FINDINGS (each verified on disk, not inferred) ===  1. FIXLIST_docs59.md is UNDISCHARGED. Its two highest-severity items are unapplied in the working copy of docs/59: line 501 still grades the K row "YES - the one suspect the argument reaches" and still contains "both implementations read the same numbers" (A1 orders UNRESOLVED (X13) and deletion of that clause); line 169 still reads "no bundle archive is present under `data/raw/`" (A2 orders replacement). This is PROMPT_final_presentation.md TASK 5, the only one of its five tasks not committed - TASK 1 (9fae1ad, ONI), TASK 3+2 (5edfcc9, deck ending + adopted-config figures) and TASK 4 (3112689, estimator (b)) all landed.  2. PROMPT_report_pdf_desktop.md was NOT executed. Its §5 lists eight known draft issues; four are still present verbatim in the committed builder: issue 8 - scripts/build_report_pdf.py lines 20, 21 and 57 still point at /sessions/epic-sleepy-mendel/mnt/magdalena-mgb-sed/ (a foreign sandbox), issue 2 - "river flow to about 57% accuracy" at :123, issue 1 - "matching independent measurements (~3-5x)" with no window at :131, issue 6 - no zero-suppression / order-dependent-IDW / refuted-hypothesis narrative anywhere in the script. CONSEQUENCE FOR THE CANONICAL STATE: "MGB-SED_complete_report.pdf ... regenerable from scripts/" is currently FALSE - the builder cannot run in this repo without repairing those three paths, and the 534 KB PDF at the repo root is the pre-finish draft carrying the §5 issue-1 overclaim. Worth surfacing to whoever owns the deliverables.  3. PROMPT_notebook_coherence.md is at Phase 1 of N. The 16 journal_nbc1-* files are all explicitly "T1 AUDIT ONLY / READ-ONLY / fix nothing" and are dated 2026-08-13 06:13-06:23. No commit touches notebooks/ or src/nbgen/ after be8a62a, so Phase 2 (edit ten generators, regenerate, re-execute, re-verify source-identity) never ran. All 16 are KEEP as the diagnosis reorg TASK 2 builds on.  4. handoff_t6_amendment_drafts.md IS discharged (hence ARCHIVE, not KEEP). Its four proposed fixes are enacted: docs/37 A3.9 exists at :2389 with A3.9.1 and A3.9.2, the ":241 exercised" site carries an inline A3.9 annotation, docs/51 Amendment 3 exists at :763 with the struck-bar annotations at :230/:257/:351, and the nb19 band retirement is commit be8a62a.  === POINTER HAZARDS IF THESE FILES MOVE ===  - FIXLIST_docs59.md lines 3-5 cite journal_x59-lens-overclaim.md, journal_x59-lens-numbers.md and journal_x59-refute-1..5.md by `docs/agents/...` path. Those 7 are marked ARCHIVE (the FIXLIST itself says "do not re-audit", so they are provenance rather than active input) - but the move must update those pointers, or apply the FIXLIST first and archive the whole x59 cluster together. - PROMPT_notebook_coherence.md and PROMPT_final_presentation.md cross-reference each other and the report prompt by `docs/agents/` path; all four PROMPTs are KEEP so the paths stay valid. - Several docs/NN files and CLAUDE.md refer to "findings that still live only in docs/agents/" (docs/00_INDEX.md carries such a list). Grep docs/ and CLAUDE.md for `docs/agents/` before moving anything and rewrite the hits to docs/archive/agents/.  === GROUPING USED (the table is per-file, but these are the natural clusters) ===  Phase C stage execution (c0, c1-ssc, c2-contrast, prereg-c2b, bfi, peaks, refit-launch, critic) - 8. CHIRPS volume-gate diagnosis H-A..H-E + their adversarial verifiers (chirps-*, verify-b/d/e) - 10, all reading out to docs/18 §15.5. Peak-deficit research track (research-*) - 4 -> docs/36. C3 construction and conventions (c3*-, decide-units, decide-ls-*, dimensional-audit, recompute, fixer, cite-*, alpha-guard, reverdict) - 14 -> docs/37 and its evidence docs. C3->C4 adjudication (adj-*, decide-c3-c4, verify-gate-logic) - 5 -> docs/43. C4 entry, guards, Pi-band debt (c41/c42, verify/refute-transport, verify/refute-prereg, c3gate-synthesis, backannotate-47, refute-gate-logic*, journal-debt-b5-piband, debt-b4, gate-reexpression, amend-*) - 16. LS freeze and the materiality bar (ls-*, audit-harness-*, defect-a/b, bar-*, lens-blinding, delta-shape, panel-*, *-enactment) - 21 -> docs/46/49/50/51/52/53 and docs/37 A3. T6 adversarial lenses and their refuters (lens-*, refute-t6-1..8) - 12 -> docs/37 A3.9 / docs/51 amd 3 / docs/54 §7. Notebook generators and their audits (nb15..nb19, nb-banner-*, verify/refute-nb19, verify/refute-my-fixes, nbgen-eq14-relabel) - 12. Documentation hygiene, index, sweeps, trackers, commits, packaging - 15. Cross-implementation comparison (x59-*) - 12 -> docs/59 / FIXLIST. Notebook-coherence T1 audit (nbc1-*) - 16, all KEEP.  === HOUSE-STYLE NOTE === Many journals contain kill-list terms (+-38 %, sigma_r = 0.465, 0.1644, 2.0-9.9, "~2x under-erosive"). In every case sampled these appear as the SUBJECT of a refutation or inside a superseded block - correct house style, not a defect, and not a reason to downgrade any disposition. The refuted-hypothesis journals are the audit trail the project deliberately preserves; archiving keeps them, and none should be deleted.

### data - 72 live

| path | why it is live |
|---|---|
| `data/` | Root of the gitignored ~16 GB input/output tree (data/processed 2.9 GB, data/raw 13 GB); only 4 files inside are git-tracked. |
| `data/README.md` | Git-tracked; the only versioned description of what belongs in raw/ and processed/ and where to get it. |
| `data/processed/` | 2.9 GB, 207 files - all derived model inputs, simulation outputs and the C1-C5 machine records; live. |
| `data/processed/.gitkeep` | Git-tracked placeholder that keeps the ignored directory present in a fresh clone. |
| `data/processed/urh_ls2d.csv` | KEEP-FROZEN - the adopted LS field (ls_formulation = buarque_2015_dg / V4_dg) materialised by ACT 1; never modify. |
| `data/processed/minibacia_ls2d.csv` | KEEP-FROZEN - minibacia-level companion of the adopted LS field, f_LS = 0.25146 erosion-weighted; never modify. |
| `data/processed/urh_ls2d_variants.csv` | KEEP-FROZEN - the LS variant sweep that the freeze decision (docs/51) selected V4_dg from; never modify. |
| `data/processed/minibacia_ls2d_variants.csv` | Minibacia-level sibling of the frozen variants table, written by the same scripts/c3/ls2d_variants.py; treat as frozen with it. |
| `data/processed/sim_calibrated_v2/` | KEEP-FROZEN in full (530 MB) - the H2E adoption bundle plus the H1/H2 attribution cells; nothing in here may be modified. |
| `data/processed/sim_calibrated_v2/parameters_H2E.csv` | KEEP-FROZEN - the adopted Phase B parameter set; the F = 0.25931 gate in src/report_h2e.py checks against it. |
| `data/processed/sim_calibrated_v2/q_gauge_H2E.npz` | KEEP-FROZEN - adopted H2E simulated discharge at the gauges; the input every C3-C5 sediment result stands on. |
| `data/processed/sim_calibrated_v2/report_H2E.json` | KEEP-FROZEN - the machine record of the H2E adoption, regenerated only by python3.10 src/report_h2e.py. |
| `data/processed/sim_calibrated_v2/h2e_drivers.npz` | KEEP-FROZEN - 546 MB of per-minibacia H2E drivers built by src/build_h2e_drivers.py; the sediment engine's forcing. |
| `data/processed/sim_calibrated_v2/metrics_fleet.csv` | KEEP-FROZEN - fleet metrics incl. the H2E rows; src/report_h2e.py asserts pre-existing rows were never altered. |
| `data/processed/sim_calibrated_v2/{calibration_v2.json,feasibility_H1.csv,feasibility_H2.csv,h2_minus_h1.csv,parameters_H1.csv,parameters_H2.csv,q_gauge_H1.npz,q_gauge_H2.npz,search_archive_H1.npz,search_archive_H2.npz,recession_validation.csv,README.md}` | KEEP-FROZEN - the H2 minus H1 attribution record (docs/26 §5.1: volume moved, correlation did not); the evidence for the r-ceiling verdict. |
| `data/processed/o5_calibration_profile.json` | C4.3 machine record discharging docs/47 O5; the railed/exploratory verdict and the two estimator medians (-0.118 / +0.139) live here. |
| `data/processed/o5_calibration_profile.md` | Human-readable twin of the O5 profile, cited by docs/55 and read by scripts/generate_report_figures.py. |
| `data/processed/report_C4.json` | The registered C4 machine record; carries oni_2012_2014 (neutral-core CAL window, recorded 2026-08-13) required by docs/45 and docs/47. |
| `data/processed/c5_enso_contrast.json` | The headline result in machine form - 18/18 stations, median rate ratio 3.05x; docs/56 and the report figures both read it. |
| `data/processed/c5_enso_contrast.md` | Human-readable C5 contrast read-out backing docs/56 and the cross-implementation comparison in docs/59. |
| `data/processed/c5_sensitivity.csv` | C5 robustness table across beta and both window pairs; the basis for the 1.62-4.85 range claim. |
| `data/processed/ssc_recovered_coords.csv` | B5 geocoding output (scripts/c1/b5_geocode_ssc.py) - 46 SSC sites located, 0 with discharge; the evidence for the ~18-gauge ceiling in docs/57. |
| `data/processed/precip_gauges_daily_qc.csv` | TRAP, not a deletion: CLAUDE.md says 'use the _qc files', but this is the 70-station v1 repair - the adopted v2 forcing reads precip_gauges_daily_qc_v2.csv instead (nb11, src/merge_chirps_gauges.py). |
| `data/processed/precip_gauges_daily_qc_v2.csv` | The rainfall file the adopted v2 forcing was actually built from (153 stations, 240,158 inferred-dry days); the real 'use this one'. |
| `data/processed/precip_gauges_inventory_qc.csv` | Post-repair station inventory; no v2 counterpart exists, so this one is unambiguously the live inventory. |
| `data/processed/precip_gauges_daily.csv` | TRAP but required: the pre-repair file sits beside the _qc files and must never be used for analysis - yet it is the mandatory INPUT to both repair scripts, so it stays. |
| `data/processed/precip_gauges_inventory.csv` | Same trap/necessity pair as above - pre-repair inventory, read by src/repair_precip_zero_suppression.py and src/repair_precip_selectivity.py. |
| `data/processed/{precip_zero_suppression_report.csv,precip_selectivity_report.csv,precip_colocated_gauges.csv,energy_floor_triage.csv}` | The rainfall-defect evidence tables (docs/16 §4.1, docs/18 §10, docs/23 §11-§12) read live by nb10, nb12 and nb15. |
| `data/processed/_c1_extremes.csv, _c1_geom.csv, _c1_screens.csv, _c1_window_counts.csv, _c1_zeros.csv` | Underscore-prefixed but not scratch: the C1 SSC screening tables cited by docs/37, docs/42, docs/45 and docs/53; no producer survives in src/ or scripts/, so they are irreplaceable. |
| `data/processed/{ls_defect_a.json,ls2d_defect_b.json,ls2d_variants_summary.json,ls_stratified_report.json,ls_stratified_report.md}` | The LS defect-A/defect-B diagnoses and the stratified LS report behind docs/46, docs/49 and docs/51; produced by scripts/c3/. |
| `data/processed/{urh_cp_factors.csv,urh_erosion_weights.csv,urh_fractions.csv,minibacia_topo.csv,minibacia_soil_params.csv,minibacias.csv}` | Live sediment/hydrology parameter tables; urh_cp_factors.csv is the cp_revision source that every load figure must be quoted against. |
| `data/processed/{forcing_minibacia_precip_v2.csv,forcing_minibacia_pet_v2.csv,forcing_minibacia_provenance_v2.csv}` | The v2 (gauge-only) forcing that H2E was fitted on - 355 MB, the definition of 'v2 forcing'. |
| `data/processed/{forcing_minibacia_precip.csv,forcing_minibacia_pet.csv,forcing_minibacia_provenance.csv}` | v1 forcing, 324 MB - superseded for results but still a live dependency: nb14's H1 cell re-runs the objective on it for the H2 minus H1 attribution. |
| `data/processed/{forcing_precip_v2.npy,forcing_pet_v2.npy,forcing_precip_v2_dates.npy,forcing_precip_v2_ids.npy,forcing_pet_v2_dates.npy,forcing_pet_v2_ids.npy}` | 279 MB of dense .npy accelerators nb12 asserts the presence of; regenerable via src/forcing_npy.py --version v2 but cheap to keep and required by the notebook chain. |
| `data/processed/{discharge_daily.csv,discharge_inventory.csv,stations_discharge.csv,stations_discharge_coords.csv,rating_curves.csv}` | Consolidated discharge products from src/build_discharge_gauges.py plus the coordinate table it left-joins. |
| `data/processed/{sediment_daily.csv,sediment_inventory.csv,sediment_daily_qc.csv,sediment_inventory_qc.csv,sediment_coverage_census.csv,stations_sediment.csv}` | The SSC record and its QC pass (docs/19, docs/32) - 79/79 stations classified, 18 usable; nb15/nb16 read these. |
| `data/processed/{ssc_rating_fits.csv,ssc_station_eras.csv,ssc_sampling_selectivity.csv}` | Frozen SSC rating fits, era boundaries and the flow-selectivity statistic; nb16 cross-checks its own recomputation against them. |
| `data/processed/{cop30_dem.tif,dem_coarse.tif,minibacias.tif,urh_30m.tif,landcover_hydro_30m.tif,soils_hydro_30m.tif,soil_depth_igac.tif,soil_drainage_igac.tif,soil_family_igac.tif,soils_magdalena_merged_4326.gpkg}` | Aligned raster/vector stack (~390 MB) behind the minibacia and URH construction; expensive to rebuild, all live Phase A inputs. |
| `data/processed/{gauge_minibacia.csv,gauge_minibacia_remap_report.csv}` | The corrected gauge-to-minibacia mapping and the per-station before/after audit trail (docs/17 - 20 remapped, 10 excluded as distributaries). |
| `data/processed/model_inputs/` | 86 MB v1 bundle - NOT dead: src/calib_v2.py's H1 cell binds to bundle='model_inputs' for the H2 minus H1 attribution. |
| `data/processed/model_inputs_v2/` | 104 MB - the adopted bundle; notebooks 13 and 14 and every H2/H2E/H2E-S cell run on it. |
| `data/processed/sim_baseline/` | 109 MB v1 uncalibrated baseline; nb13 states it was deliberately left untouched because nb14's H1 cell needs it. |
| `data/processed/sim_baseline_v2/` | 120 MB - the live uncalibrated baseline on v2 forcing that the calibration is scored against. |
| `data/processed/sim_calibrated/` | 3.5 MB v1 Config B run; nb14 re-scores its stored q_gauge.npz as the reference cell and src/calib_v2.py inherits its Morris screening constants. |
| `data/processed/_calib_cache/` | 275 MB DDS search workspace; the dds_*.npz seed archives inside are primary evidence for docs/26 and docs/29. |
| `data/processed/_calib_cache/dds_{H1,H2,H2E,H2E-S}_2026090*.npz` | 16 per-seed DDS search archives - the seed-expansion evidence (H1 vs H2 not separated, H2E off its rail); src/build_h2e_drivers.py reads dds_H2E_<best seed>.npz directly. |
| `data/processed/c2/` | The C2 observed-contrast tables (rate ratios, estimator agreement, monotonicity, monthly shape) - the 22/22 model-free result docs/34 and C5 are checked against. |
| `data/processed/c2b/` | BFI and peak-flow measurements behind docs/33 (H-BFI held, H-PEAK refuted). NOTE: it also holds three .py files (bfi_measure.py, bfi_figure.py, peaks_measure.py) - code living in the data tree, a reorg candidate for scripts/c2b/. |
| `data/processed/peakgap/` | 1.3 MB of peak-deficit evidence (events, per-gauge, match sensitivity, sub-daily inventory) adjudicated in docs/35 and docs/36. |
| `data/processed/worldcover/` | 233 MB - the four ESA WorldCover 10 m tiles that urh_30m.tif and landcover_hydro_30m.tif are built from. |
| `data/raw/` | 13 GB, 847 files of external source data; everything downstream is rebuildable only from here. |
| `data/raw/.gitkeep` | Git-tracked placeholder for the ignored raw tree. |
| `data/raw/climate/` | 12 GB - CHIRPS (11 files, 92 MB), era5land_basin_* (132, 4.7 GB), era5land_ext_* (132, 5.4 GB, the domain the model actually uses) plus the strip inputs. |
| `data/raw/climate/era5land_ext_*.nc` | The 132 mosaicked basin+strip files (5.4 GB) that ARE the climate forcing domain -77.0..-72.3; the model period is bounded by these. |
| `data/raw/climate/era5land_basin_*.nc` | 132 basin-box downloads (4.7 GB) - one of the two inputs to src/mosaic_era5.py; deleting them makes era5land_ext_* unrebuildable without a fresh CDS pull. |
| `data/raw/climate/strip/` | 132 east-strip files (750 MB), the second input to the mosaic; src/download_era5_strip.py writes them. |
| `data/raw/climate/chirps_basin_*.nc` | 11 basin-clipped CHIRPS years - the input to the merge experiment; keep even though the merge failed, since docs/58's bound on the last lever rests on them. |
| `data/raw/colleague_share/` | 24 KB - ANSWERS_C1_C2_C3.md, MANIFEST.md and input_hashes.txt from the colleague exchange that docs/59's cross-implementation comparison is built on. |
| `data/raw/dem/rasters_COP90_Correcte_Corrdinatzs.tar.gz` | 249 MB - the adopted DEM archive (member output_hh.tif, 0.000833 deg); docs/59 confirms both implementations use this same archive and member. |
| `data/raw/landcover/terrascope_download_20260728_153218.zip` | 653 MB Terrascope delivery - the source archive for the four ESA WorldCover tiles in data/processed/worldcover/. |
| `data/raw/observed/` | 293 MB of IDEAM/DHIME source series (discharge, precipitation, sediment); the only provenance for every gauge product. |
| `data/raw/observed/caudal/` | 45 files - the per-department discharge CSV/ZIP pulls consumed by src/build_discharge_gauges.py. |
| `data/raw/observed/caudal/OMAR_CAUDAL/` | 12 colleague-supplied discharge ZIPs (Antioquia, Santander, Magdalena, ...) folded into the same consolidation. |
| `data/raw/observed/precip/` | 251 files - DHIME precip pulls, station catalogues, the auto-download outputs and the resume ledger (_auto_done.txt). |
| `data/raw/observed/precip/precip_coords_supplement.csv` | The ONLY data file tracked in git besides README.md and the .gitkeeps - hand-recovered coordinates that cannot be re-downloaded; do not move it out of version control. |
| `data/raw/observed/precip/dhime/` | 237 files - the raw DHIME zips organised by src/organize_precip_regions.py into regions/. |
| `data/raw/observed/precip/dhime/regions/` | 98 files across 20 departments - the consolidated regional layout that src/build_precip_gauges.py reads. |
| `data/raw/observed/precip/dhime/yOUSSEF DATASET/` | 68 files of colleague-supplied precipitation; content is live input, though the directory name (stray capitalisation, spaces) is a rename candidate in the reorg. |
| `data/raw/observed/sedimento/` | 24 files - the departmental SSC pulls that sediment_daily.csv and the 79-station C1 classification are built from. |
| `data/raw/refs/buarque2015.pdf` | The source paper for the adopted LS formulation buarque_2015_dg; docs/46 and docs/51 cite it directly. |
| `data/raw/refs/yben409_sediment_repo.bundle` | 82 MB git bundle of the colleague's implementation - the primary evidence behind the cross-implementation comparison in docs/59. |
| `data/raw/soils/` | 301 MB - 5 SoilGrids rasters plus 19 IGAC departmental GeoPackages; the source of Wm/K and the merged soils GPKG. |

*Agent note:* SHAPE. data/ = 1,055 files, 16 GB (data/raw 13 GB / 847 files, data/processed 2.9 GB / 207 files). du was fast, no skip needed. Only 4 files under data/ are git-tracked: data/README.md, data/processed/.gitkeep, data/raw/.gitkeep, and data/raw/observed/precip/precip_coords_supplement.csv (an explicit .gitignore exception - hand-recovered coordinates; do not move it out of version control during the reorg). data_Final/ and delivery/ DO NOT EXIST on this machine (both are gitignored and regenerable; data_Final via src/build_data_final.py). _eq/ does exist at the repo root but is outside this subtree.  DELETE list is short - only 3 entries, ~278 MB, and the size is almost all machine cache:  1. data/processed/gauge_minibacia_ORIG_backup.csv - I verified this rather than assuming: awk-stripping the minibacia column makes it byte-identical to gauge_minibacia.csv, and gauge_minibacia_remap_report.csv carries original_minibacia for all 159 stations plus final_rc and the action taken. The backup is fully reconstructible, so deleting it destroys no record of the remap verdict. docs/17 line 539 names it, so that line needs an edit if it goes.  2. data/processed/soils_magdalena_merged_4326.gpkg-journal.DISABLED - 512-byte orphaned GeoPackage rollback journal, manually renamed to neutralise it.  3. data/processed/_calib_cache/{H2_precip,H2_pet,H2_dates}.npy - 278 MB forcing cache. Re-run python3.10 src/report_h2e.py after removing it and confirm F still matches 0.25931 to 1e-8 before trusting the tree. No __pycache__, no *.bak, no *.tmp, no editor cruft anywhere under data/. The only other *_backup/*_ORIG_/*_old hit in the whole 1,055-file tree was the one named above.  TWO TRAPS WORTH FLAGGING TO THE REORG OWNER.  (a) The pre-repair precip files DO still sit beside the _qc files (precip_gauges_daily.csv 25 MB, precip_gauges_inventory.csv 42 KB). They are NOT deletion candidates - src/repair_precip_zero_suppression.py and src/repair_precip_selectivity.py both read them as their required input. They are a use-the-wrong-file trap only.  (b) A CLAUDE.md MISMATCH. CLAUDE.md says to use precip_gauges_daily_qc.csv, but that is the 70-station v1 repair. The adopted v2 forcing was built from precip_gauges_daily_qc_v2.csv (153 stations, 240,158 inferred-dry days) - src/nbgen/make_nb11.py L73, src/merge_chirps_gauges.py L114, src/nbgen/make_nb15.py L186, and make_nb10.py L97 says so explicitly: "That v2 file - not the precip_gauges_daily_qc.csv this notebook reads - is what notebook 11 and the adopted forcing use." So three generations of the same file sit side by side and the middle one is the one CLAUDE.md points at. The inventory has no v2, so precip_gauges_inventory_qc.csv is unambiguous.  WHAT LOOKS DEAD BUT IS NOT - do not let a reorg sweep these.  - model_inputs/, sim_baseline/, sim_calibrated/ (the v1 trio, ~199 MB) are LIVE. src/calib_v2.py binds H1 to bundle='model_inputs'; make_nb13.py L1875 comments "sim_baseline_v2, NOT sim_baseline: ... notebook 14's cell H1 needs it"; make_nb14.py L156/L533 re-scores sim_calibrated/{calibration.json,q_gauge.npz} as the reference cell and inherits its Morris constants. The v1 forcing CSVs (324 MB) are the same story.  - The _c1_*.csv files look like scratch (underscore prefix, no producer anywhere in src/ or scripts/) but are cited by docs/37, 42, 45 and 53. They cannot be regenerated. KEEP.  CODE IN THE DATA TREE. data/processed/c2b/ contains three Python files - bfi_measure.py, bfi_figure.py, peaks_measure.py - that produced the H-BFI/H-PEAK read-out in docs/33. They are the only source files under data/ and are a natural move to scripts/c2b/ in the reorg (a move, not a delete; they are how a refuted hypothesis was measured).  BIGGEST SAFE SPACE WIN, if that matters: the 78 bare-named era5land_2*.nc files (392 MB, marked ARCHIVE). They are the first-generation pull at ~4.7 MB/month against 35 MB/month for the era5land_basin_* files that replaced them, and no code in src/ or the notebook generators reads the bare naming. I archived rather than deleted them only because I could not confirm the variable set is a strict subset without opening the netCDFs.  NO CONFLICTS with canonical state were found in this subtree. The frozen artifacts all exist and are intact: urh_ls2d.csv, minibacia_ls2d.csv, urh_ls2d_variants.csv (plus a minibacia_ls2d_variants.csv sibling not on the brief's list - same producer, treat as frozen too), the whole 530 MB sim_calibrated_v2/, the five-file H2E bundle, and all seven C4/C5 machine records. Note that metrics_fleet.csv exists in BOTH sim_calibrated/ (2.7 KB, v1) and sim_calibrated_v2/ (8.1 KB, the H2E one) - always qualify which.
