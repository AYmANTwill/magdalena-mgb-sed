# src

Reusable Python extracted from the notebooks (future). Intended contents:
- `preprocessing.py` — fill (Planchon-Darboux), D8 flow direction, accumulation, stream/segmentation, catchments.
- `urh.py` — soil × land-cover crossing and per-minibacia composition.

For now the reference implementations live in `../notebooks/`.
