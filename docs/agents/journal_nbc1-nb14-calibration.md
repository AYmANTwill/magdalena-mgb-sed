# journal — nbc1 — nb14_calibration coherence audit (T1, READ-ONLY)

Agent: nbc1. Owns ONLY this file. Phase 1 = audit, fix nothing.

Assignment: audit `notebooks/14_calibration.ipynb` (40 cells), generated by
`src/nbgen/make_nb14.py`. Mechanical sweep hits reported by orchestrator:
"0.421" x3, "99.7" x1, "t/km" x3, "km2/yr" x3, "CHIRPS" x10.
Check H1/H2/H2E story, "fitted is not validated", narrative beats 2 and 4,
YIELD EMBARGO (t/km2/yr), kill-list numbers, executed-output staleness vs
engine default LS move (c3fdb55).

## Log

- [start] Created journal. Extract is 141,460 bytes at
  scratchpad/nbtext/14_calibration.txt. Reading in full next.

## Sweep 1 — full read of the extract (all 40 cells, 0..39)

Read `scratchpad/nbtext/14_calibration.txt` end to end (2,298 lines). Structure:
40 cells, 19 markdown / 21 code, `code_unexecuted=0`, `cells_with_error=0`.
Cell 0 = a 2026-08-12 STATUS banner by agent `nb-banner-1217`; cells 1..39 = the
original notebook, unrewritten (the banner says so explicitly).

## Mechanical hits — each one checked, verdict recorded

- **"0.421" x3** (lines 1218, 1570, 2219) — FALSE POSITIVE. All three are the H1-fitted
  `VAL all` median KGE (+0.421). Nothing to do with the retired `x0.333 - x0.421` LS bracket.
- **"99.7" x1** (line 1071) — FALSE POSITIVE. `H1: 99.7 % of the gain was in hand at 90 % of
  the budget`. Not the retired 99.7 Mt/yr proxy load.
- **"t/km2/yr" x3** (lines 198 cell 4; 2176 cell 38 source; 2269 cell 38 executed output)
  — **NOT yield-embargo violations. They are embargo COMPLIANCE statements**, e.g. cell 4:
  "No per-gauge specific yield in t/km2/yr is published anywhere in this notebook." and
  cell 38 README item 3: "No per-gauge sediment yield in t/km2/yr is published anywhere,
  per `docs/23` s13.2". Both cite docs/23 §13.2 correctly. No numeric t/km2/yr appears.
- **"CHIRPS" x10** — lines 36-37 (banner, annotated), 71 (cell 1), 1929/1931 (cell 35),
  1971 (cell 37 code, written into calibration_v2.json on disk), 2016 (cell 37 code),
  2103/2169 (cell 38 README source), 2196/2262 (cell 38 executed output). The banner
  retires the claim; the body still states it as live. Follow-up below.

## Kill-list grep — negative

`0.1644 | 0.465 | 1.9618 | 2.37 | 3.00x | 0.05-0.30 | 11.8 | 348.4 | 0.00216 | 0.0209 |
0.0104 | 1.34762 | under-erosive | 299.5 | 248.7 | 104.8 | 82.8 | 126.1 | 75.32 | 129.38 |
0.333 | 0.25146 | 0.43194 | 3.9768 | 2.3151 | 35.4 | +-38` -> **zero hits**.
`mgb_sediment | ls2d | cp_revision | V4_dg | musle | erosion | Mt/yr | SDR | Buarque |
Williams` -> **zero hits**. nb14 is a *hydrology* notebook; it never touches the sediment
engine.

## MEASURED: executed-output staleness

nb14 records and asserts source hashes. Measured today:

| file | nb14 recorded (executed output, cell 3) | on disk now | changed by |
|---|---|---|---|
| `src/mgb_hydrology.py` | `cdea026afe796f6d` | **`93b180a9113f5946`** | `80a7c10` 2026-08-03 "FAO-56 threshold ET stress (opt-in) + pre-registered cell H2E" |
| `src/calib_v2.py` | `3342728f2a38bac1` | **`30ab13a630278d5d`** | `19dce32` 2026-08-10 "wire the C2b peak term into calib_v2 ... cell H2E-S" |

Cross-checked against the stored artifacts (not gitignored-away): 
`data/processed/sim_calibrated/calibration.json` -> engine `cdea026afe796f6d`;
`data/processed/sim_calibrated_v2/calibration_v2.json` -> engine `cdea026afe796f6d`,
search `3342728f2a38bac1`, cells H1/H2, F_new_segment H1 0.236772238, H2 0.253365184,
kc_mult H1 1.98174, H2 1.89573, railed_global 2 and 2.

**Consequence, measured not assumed:** cell 3 contains
`assert ENG_SHA == V1['engine']['sha256']`. With the engine now at `93b180a9`, that assert
would FAIL on re-execution. So nb14 is not re-executable in its committed form, and its
outputs are stale relative to the engine that produced the ADOPTED H2E fit. I am NOT
executing it (28,800 s timeout, explicit prohibition).

## Engine entry points — MEASURED, not assumed

Grepped the extract for `mgb_sediment | ls2d | cp_revision | V4_dg | musle | erosion |
Mt/yr | SDR | Buarque | Williams`: **zero hits**. nb14 imports `mgb_hydrology as mgb` and
`calib_v2 as CV` (cell 3) and reaches the engine only through
`calib_v2.Cell.run_full` -> `mgb.simulate` (`src/calib_v2.py:772,777`) and
`Cell.run_seg` -> `mgb.simulate` (`:768`), plus `subprocess.Popen([... src/calib_v2.py
--cell --seed --budget --out])` (cell 18).

Therefore the engine-default LS move (`c3fdb55`, `ls2d_column "ls2d_hs" -> "V4_dg"` at
`src/mgb_sediment.py:925`) **does not reach nb14 at all**, and nb14 has no `cp_revision`.
Verified `src/mgb_sediment.py`:818 and :862 still default `ls2d_hs`, :925 defaults `V4_dg`
— but nb14 calls none of the three.

## Owning docs read

- `docs/26` §1-§7 + Addendum A.1-A.6 — the parameter record. §5's 3/9 table matches nb14
  cell 28 exactly. §5.1 is the railing failure. §6 owns the climatology-benchmark trap
  (nb14 cell 16). §7 is the CHIRPS "one lever" paragraph — **now spent**.
  A.2: H2E `kc_mult` **1.6625** (77.5 % of range). A.4: H2E **3/9** on nb14's own criteria,
  and *"Adoption was on the docs/29 rules, which H2E passed; it was never a claim that the
  pre-registered adequacy criteria were met."* A.5: **El Nino skill over climatology
  -0.0005**.
- `docs/29` Results + Rule (a)/(b) + Amendment 1 — gap **0.00915** vs max spread **0.05082**;
  H2E kc 1.662/1.836; caveat *"kc came OFF THE RAIL but is not yet plausible: 1.662/1.836
  against the FAO-56 plausibility target of <= 1.2"*; n = 2 seeds for H2E.
- `docs/18` §15 (title: *"The CHIRPS-gauge merge: built, validated, and NOT adopted"*) and
  §15.5 — LOOCV **0.447 > 0.429 PASSES**, volume **2,188.5 mm/yr +7.47 % FAILS**,
  **DO NOT ADOPT**, *"no route to a passing volume gate exists inside the merge code"*.
- `docs/58` (**committed `fc9a29d`, 2026-08-13 03:29**) — the 139 rain-selective route
  bounded at **dr = +0.0058**, verdict *"r 0.57 -> ~0.576"*. `src/nbgen/make_nb14.py` was
  last touched by `57f9761`, **2026-08-12 14:55** — i.e. the banner *predates* docs/58 and
  cannot have carried it. Not an accusation, a dating fact.

## MEASURED: nb14 cell 37 destructively overwrites a shared, appended-to artifact

`SUM.to_csv(OUTD / 'metrics_fleet.csv', index=False)` (cell 37, generator line ~1428) is an
unconditional full overwrite. Measured on disk:
`data/processed/sim_calibrated_v2/metrics_fleet.csv` has **39 rows / 4 configurations**
(H1 10, H2 12, H2E 12, ref 5). nb14's own `SUM` has **27 rows / 3 configurations** (the
cell-37 round-trip prints `metrics_fleet rows 27`). `src/report_h2e.py`:323-345 **appends**
the 12 H2E rows and asserts the pre-existing rows are unaltered. nb15 (`SIM/'metrics_fleet.csv'`,
"4 configurations x 6 periods"), nb17 and nb18 read that file.
=> **Re-executing nb14 would silently delete the 12 H2E rows** three downstream notebooks
depend on. The generator docstring still advertises the notebook as *"cheap to re-execute"*.

## Defect list assembled — 13 rows. Writing structured output now.

False positives cleared and NOT to be "fixed": `0.421` (H1 VAL-all KGE, x3),
`99.7` (percent of DDS gain at 90 % budget, x1), `t/km2/yr` (x3, all embargo-COMPLIANCE
prose citing docs/23 §13.2, no number attached).
No prose-vs-code "v2 forcing" contradiction in nb14: every use of "v2" resolves to the
gauge-only `model_inputs_v2/` bundle, and cell 1 keeps the CHIRPS merge strictly separate
as the dropped cell H3.
