# journal — nbc1 — nb13 baseline_run coherence audit (T1, READ-ONLY)

Agent: nbc1. Owns ONLY this file. Phase 1 = audit, fix nothing.
Assignment: audit `notebooks/13_baseline_run.ipynb` (50 cells), generated by `src/nbgen/make_nb13.py`.
Mechanical sweep hits to adjudicate: "0.333" x1, "2.37" x2, "0.465" x1, "CHIRPS" x1.
Checks: r~0.57 stated as INHERITED FROM RAINFALL FIELD (not model failure)?
        El Nino skill-over-climatology = -0.0005 caveat carried?
        Narrative beat 2.

## Log

- [start] Created journal. Next: read the pre-extracted text of nb13.

### Mechanical sweep adjudication (grep over the extract, line numbers of the .txt)
- `0.333` — **1 hit, line 2421**: `Q4 large  0.333  0.679 ...` = a Pearson **r** in a per-quartile
  skill table (cell 45 region). NOT the retired `x0.333-x0.421` LS bracket. **FALSE POSITIVE.**
- `2.37` — **2 hits**: line 1512 `2.37e-17` (a mass-balance relative residual, cell 30/33 annual
  table) and line 2419 inside `312.375` (a mean-flow m3/s figure). NOT "our LS is 2.37x-3.00x".
  **BOTH FALSE POSITIVES.**
- `0.465` — **1 hit, line 815**: `equilibrium W/Wm  area-weighted 0.4656` = mean-field soil
  saturation fraction. NOT the retired per-station residual sd `sigma_r = 0.465`. **FALSE POSITIVE.**
- `CHIRPS` — **1 hit, line 26**, inside the 2026-08-12 STATUS banner (cell 0): *"not a CHIRPS merge -
  that product was later built and **not** adopted (`docs/18` §15)"*. Correct and current.
  **NOT A DEFECT.**
- `0.57` r-ceiling: only in the banner (cell 0), quoting docs/22 §4.7 `0.556-0.572`.
- `-0.0005` El Nino skill-over-climatology: **ZERO hits anywhere in nb13**.

### Measurements (not assumptions)
1. **Sediment engine: NOT called.** Raw-JSON scan of all 50 cells of
   `notebooks/13_baseline_run.ipynb` for `mgb_sediment|ls2d|cp_revision|V4_dg|t/km|MUSLE`
   -> **0 hits** (source AND outputs). nb13 imports only `mgb_hydrology`. So the
   `c3fdb55` engine-default LS move (`ls2d_column` V4_dg) is **irrelevant** to nb13.
   No `cp_revision` exists to quote. **No yield-embargo surface at all.**
2. **Water engine provenance IS stale.** nb13 cell 3 executed output prints
   `src/mgb_hydrology.py 44.0 kB sha256 cdea026afe796f6d`. Measured on disk today:
   **47,097 B = 47.0 kB, sha256 93b180a9113f5946**. The engine changed at `80a7c10`
   (2026-08-03, "FAO-56 threshold ET stress (opt-in) + pre-registered cell H2E"), i.e.
   AFTER nb13's execution commit `74eb324` (2026-08-02). The new fields
   (`et_stress="linear"`, `theta_crit=0.6`) default to the old behaviour, so the numbers
   MAY still reproduce — but that is unverified (re-execution prohibited in T1).
   `engine exports 16 names` still matches (`__all__` has 16).
3. **"59/59 assertions" is CORRECT, not stale.** Ran `python3.10 src/test_mgb_hydrology.py`
   (read-only self-check; grepped first — it writes nothing): **`59/59 assertions PASSED`,
   0 aborted, 82.3 s.** The only post-nb13 change to that file (`git diff 74eb324 HEAD`)
   adds two constructor kwargs, no assertions. Claim stands.

4. **THREE v1 LEFTOVER PROSE NUMBERS — proven by diffing the v1 executed notebook.**
   `git show 0388930:notebooks/13_baseline_run.ipynb` (the v1 run, 49 cells) prints:
   - `24027070 minibacia 9923 A 1,563 km2 KGE 0.770 r 0.771 alpha 0.992 beta 1.019`
   - `BASELINE VERDICT: Q = 1,574 mm/yr against an observed 874 mm/yr, i.e. 1.80x too much water`
   The CURRENT (v2, commit 74eb324 -> 57f9761) executed notebook prints:
   - `24027070 minibacia 9923 A 1,563 km2 KGE 0.747 r 0.766 alpha 0.927 beta 0.937`
   - `BASELINE VERDICT: Q = 1,464 mm/yr against an observed 912 mm/yr, i.e. 1.63x too much water`
   So the prose "KGE ~ 0.77" (cells 1, 40), "alpha and beta both within 2 % of 1" (cell 40),
   "basin bias is roughly +80 %" (cell 40) and "roughly a factor of two" (cells 34, 48) are all
   **v1 numbers that the v2 re-execution silently invalidated**. Measured now: KGE 0.747,
   alpha within 7.3 %, beta within 6.3 %, basin bias +63 % (1.63x). This directly falsifies the
   notebook's own working rule 4 (cell 1): *"No number appears here that this notebook did not
   compute."* Generator anchors: make_nb13.py:87, :1343, :1583-1584, :2152.
5. **Section 7 heading vs code.** cell 46 md says `data/processed/sim_baseline/`; cell 47 writes
   `OUTD = PROC / 'sim_baseline_v2'`. The 2026-08-12 banner discloses it with a strike-through,
   but the section text itself still reads live. Generator: make_nb13.py:1858.
6. **Uncited thresholds driving printed categorical verdicts** (docs/52 §7 rule):
   make_nb13.py:1437 (`+0.02` KGE margin -> *"docs/17 q_spec envelope carried real information"*;
   measured margin 0.234-0.212 = **0.022**, i.e. it clears the uncited bar by 0.002);
   :1817 (`0.25`/`0.45` -> `no structure`/`WEAK`/`STRONG`); :1259/:1964/:2129 (the uncited
   "10-20 % of P" tropical-forest interception band, which is written into `parameters.json`
   as `known_deficiency`); :1479 (`0.7` -> `CONFIRMED`).
7. **r-ceiling check.** Body cell 49 item 4 gets the DIRECTION right without the number
   ("the timing information is largely in the forcing ... and in a routing scheme that cannot
   represent the Mompos floodplain"). Only the retro-fitted banner (cell 0) carries
   docs/22 §4.7's `0.556-0.572`. No pointer to docs/58 (route bounded at max +0.006 r).
8. **-0.0005 caveat: ABSENT.** Zero occurrences of `0.0005` in the notebook.
9. **Areas.** cell 35 stores `gauge_area_km2` (IDEAM) and `model_area_km2` (derived) side by side
   in `metrics_gauge.csv` / `q_gauge.npz` and **never compares them** — the exact comparison
   docs/23 §13.2 later made (>2x disagreement on 31 of 85 gauges), which is the origin of the
   yield embargo. All qspec figures use `model_area_km2` for BOTH obs and sim, so no number is
   wrong; the missed check is the gap.

### Sweep completeness
All **50 of 50** cells read in the extract (lines 1-2812). No cell skipped.

### Owning docs (from evidence)
- `docs/18` §"item 18" / INDEX row: the Phase B record, names **notebooks 13/14** as its subject.
- `docs/26` §intro: *"Commits `74eb324` (nb13)..."* — owns the run that produced nb13's CURRENT
  executed state, and §5.1 is the fitted-parameter caveat the banner points to.
- `docs/22` §4.7 — owns the r ~ 0.57 ceiling that nb13's Summary item 4 anticipated.
- `docs/20` §(regeneration chain) lines 83-84, 136 — owns how to re-execute nb13 and what must pass.
- `docs/00_INDEX.md` has **no nb13 row at all** (grepped: zero hits for nb13/notebook 13/13_baseline).

### Verdict
Structurally the strongest notebook in the set (mass balance to 0.1 machine eps, five independent
recomputations, a registered pre-run prediction, a leak test with a permutation null, and a
self-attack on its own best gauge). Its defects are all **prose that the v2 re-execution left
behind**, plus a stale engine provenance stamp, plus missing forward pointers to the Phase B close.
No kill-list hit, no yield-embargo surface, no sediment engine call.

--- END T1 AUDIT (read-only; nothing outside this journal was written) ---
